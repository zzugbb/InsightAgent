# InsightAgent

InsightAgent 是一个可观测 AI Agent 平台，目标是把「会话 → 任务执行 → 轨迹解释 → Memory/RAG」做成可调试、可回放、可扩展的工程闭环。

## 当前开发进度（按完整计划对齐）

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| W1 主链路 | 已完成 | 会话/任务/消息持久化、SSE 流、trace 回放与 delta、前端工作台闭环 |
| W2 可观测 + Memory 最小闭环 | 已完成（已收口） | 轨迹时间线+流程图、TraceStep 契约、Chroma 会话 memory 状态/写入/检索、`trace/delta` 流式阶段增量持久化（`seq` 递增） |
| W3 Tool + ReAct | 已完成（mock 范围） | mock 工具调用循环、可恢复重试/致命失败语义、SSE/Trace 对齐 |
| W4 RAG + 成本展示 | 已完成（已收口） | RAG ingest/query/status、执行链路 RAG 命中回填、Token/Cost 估算与 UI 展示、`compose.full.yml` |
| 阶段 5+ 产品化 | 进行中 | 除 `full-data-auth` 与 PostgreSQL 运行时收敛外，`RBAC-lite`、`rag-rbac-lite`、任务取消/超时、running task 恢复、任务/会话导出、主链路 e2e 与 CI 诊断护栏均已落地；`tool-runtime-productionization` 运行时基础设施已完成并归档，当前主缺口转向默认工具去 mock 化、真实工具接入、治理深化与队列化可靠性 |

当前默认策略：`mock` 模式仍作为低成本默认演示路径；`remote` 模式已支持 OpenAI-compatible `/chat/completions`，可按用户配置 `base_url + api_key + model` 接入真实厂商。

2026-06-04 收尾说明：`tool-runtime-productionization` 已完成运行时基础设施收口。两份 spec
`docs/superpowers/specs/2026-05-13-tool-runtime-productionization-design.md` 与
`docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md`
现保留为 archived 历史记录，不再作为高频维护中的活跃开发文档；后续默认开发重心转向默认工具去
mock 化与真实工具接入。

2026-06-04 下一阶段进展：默认 planner 的用户可见暴露已完成第一步去 mock 化。当前后端仍保留内部兼容名
`mock_plan`，但默认 plan 摘要、tool trace/action 文案、tool observation 与前端流式/Inspector 展示已统一优先显示
`Task Planner`；这一步先解决页面默认出现 `mock_plan` 的问题，同时保留现有 registry/配置链兼容性。

2026-06-04 下一阶段继续推进：默认主链里的 planner 已进一步从 `mock_plan` 切到 `task_plan`。当前实现采用
“`task_plan` 默认产出 + `mock_plan` 兼容 alias”的方式，既让默认内部链路继续去 mock 化，又不打碎现有
profile / override / extra tool template 对旧名字的兼容依赖。

2026-06-04 下一阶段继续推进：默认主链里的 retrieval 入口也已从 `mock_retrieve` 切到 `task_retrieve`。
当前实现采用“`task_retrieve` 默认产出 + `mock_retrieve` 兼容 alias”的方式；默认摘要与默认 tool 运行文案会显示
`Knowledge Retrieval`，但旧的 profile / disable / template 配置仍然可以继续通过 `mock_retrieve` 生效。

2026-06-04 下一阶段继续推进（canonical registry 收口）：默认 registry / profile / provider / loader /
file-manifest 主链现已统一切到 `task_plan / task_retrieve`；旧的 `mock_plan / mock_retrieve` 只保留为兼容别名，
用于历史 `disabled_tool_names`、`template`、`overrides` 与旧 trace 回放路径，不再作为默认注册表中的一等键名。

2026-06-04 下一阶段继续推进（canonical runtime outward 收口）：即便旧配置仍通过 `mock_plan / mock_retrieve`
触发兼容执行，默认 tool start/meta/trace step 的 `tool.name` 也已统一落成 canonical `task_plan / task_retrieve`；
因此兼容层继续保留，但用户可见轨迹不再被 legacy `mock_*` 名字污染。

2026-06-04 下一阶段继续推进（internal mock semantics 收口）：后端内部 registration `kind` 现也已从
`mock_planner / mock_retrieval` 收到 `task_planner / knowledge_retrieval`，unknown-tool 错误文案同步去掉 `mock`
前缀；兼容 alias 继续负责 lookup/执行，但默认运行时语义已不再把 planner / retrieval 定义成 mock。

2026-06-04 下一阶段继续推进（generic runtime markers 收口）：默认开发期 marker 已从
`[mock-tool-error] / [mock-tool-fatal] / [mock-multi-tool]` 切到
`[tool-error] / [tool-fatal] / [multi-tool]`；旧 marker 仍继续兼容，但后续调试与文档均以 generic marker 为准。

2026-06-04 下一阶段继续推进（provider-assisted planner 首版）：默认 `build_tool_plan()` 已新增可选
provider 规划分支；当运行在非 `mock` provider 下时，后端会先尝试向远端模型请求受限 JSON tool plan，再将结果约束回
现有 `task_plan / task_retrieve / calc_eval` schema，若远端返回无效 JSON、未知工具或异常，则静默回退到既有规则 planner。
因此页面与 SSE/trace 形状保持不变，但 remote 模式已具备“真实 provider 参与规划”的第一条生产化入口。

2026-06-04 下一阶段继续推进（planning usage / overall usage 对齐）：planning provider 调用现已回传 planning artifacts，
`chat_execution_service.py` 会把 planning 阶段的 token/cost 写入 planning trace step meta，并在 `done.usage`
额外补充 `planning_*` 与 `overall_*` 字段；旧的 `prompt_tokens / completion_tokens / cost_estimate` 仍保持原有最终回答语义，
因此现有前端与 e2e 断言不会被打断。

2026-06-05 前端 e2e 稳定性修复：针对 `frontend/e2e/usage-dashboard.spec.ts` 在 CI chromium 上的回归，本轮先停下主线功能，
改为优先修复测试链本身的脆弱交互。当前 `model settings` 相关用例已不再依赖 `getByTitle(...)` 选择 Ant Design
下拉项，而是改成“打开当前可见 dropdown 后显式点击可见 option”；保存 helper 关闭 modal 也从键盘 `Escape`
切换成显式点击 `.ant-modal-close`。这一步命中的是 CI 上 `retrieval_suite / calculator_suite / planning_suite`
选项点击超时，以及 `calculator_only` 保存后 modal 未关闭的共同脆弱点。

2026-06-12 主线续推（settings 默认回退治理单源化）：后端 `PUT /api/settings` 与
`POST /api/settings/validate` 在 payload 与 user-stored settings 都未显式给出
`tool_registry_profile / tool_registry_provider_source` 时，现会先通过 shared
`get_tool_registry_profile_name_from_settings()` /
`get_tool_registry_provider_source_name_from_settings()` 规范化全局默认值，再进入校验、保存与预览链。
这样 settings 默认回退口径也并进了当前 registry/profile/provider-source 单源化主干，同时保持外部
SSE / trace / e2e 契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`393/393`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（task governance 摘要单源化）：后端
`chat_persistence_service._normalize_task_governance_dict()` 现也会把 `profile /
provider_source` 统一退回到 shared `_normalize_governance_filter()`，不再原样透传 trace/task row
里抽出的治理值。这样 task export / task response / session summary / usage dashboard 读取到的治理摘要，
开始和 task filter、usage compare、settings 保存入口共用同一套归一化口径，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`394/394`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（session governance 摘要单源化）：后端
`chat_persistence_service._normalize_session_governance_summary_dict()` 现也会把
`profiles / provider_sources` 列表逐项退回到 shared `_normalize_governance_filter()`，并过滤空值。
这样 session summary / session export / task list session governance 聚合读到的治理摘要，开始和 task governance、
task filter、usage compare、settings 保存入口共用同一套归一化口径，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`395/395`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（usage dashboard route 治理单源化）：后端
`GET /api/tasks/usage/dashboard` route 现在也会先对 `tool_registry_profile /
tool_registry_provider_source` query params 走 shared `_normalize_governance_filter()`，再透传给
`get_tasks_usage_dashboard()`。这样 usage dashboard route 与 task list route、service compare/filter、
task/session governance 摘要链开始更完整地共用同一套 profile/source 归一化入口，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`395/395`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（task governance 提取入口单源化）：后端
`_extract_task_governance_from_trace_steps()` 与 `_extract_task_governance_from_task_row()` 现在只保留“是否为空”的判定，
不再上游手工 `.strip()` profile/source；原始字符串会直接交给 shared `_normalize_task_governance_dict()` 去做真正的
profile/provider-source 规范化。这样 trace-step、persisted-row、task/session governance 摘要、usage compare/filter、
settings 保存入口开始更完整地共用同一套归一化主干，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`397/397`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（session governance merge 单源化）：后端
`_merge_session_governance_summary()` 在把 task governance 合并进 session summary 时，现也会对
`profile / provider_source` 统一复用 shared `_normalize_governance_filter()`，不再继续自己做
`strip()` 清洗。这样 session merge、session summary/export、task/session governance 摘要、usage compare/filter、
task list/usage route 与 settings 保存入口几条治理主链开始更完整地共用同一套归一化口径，同时保持外部
SSE / trace / e2e 契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`398/398`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（allowed-tool 列表单源化）：后端新增 shared
`_normalize_governance_string_list()`，并让 trace-step/task-row 提取、task/session governance normalize、
session merge 里的 `allowed_tool_names / allowed_tool_labels` 一起退回到这条 helper，不再在多个入口各自手工
`strip()` 列表值。这样 allowed-tool sibling 链开始更完整地和 profile/source 治理主干站到同一类单源化结构上，同时保持外部
SSE / trace / e2e 契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`400/400`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（session governance summary 列表稳定化）：后端新增 shared
`_normalize_governance_summary_string_list()` 与 `_normalize_governance_filter_list()`，并让
`_normalize_session_governance_summary_dict()` 对 `profiles / provider_sources / allowed_tool_names / allowed_tool_labels`
统一输出“去重 + 稳定排序”后的摘要列表。这样 session summary / session export / task list session governance 聚合在继续复用既有
governance normalizer 的同时，也开始具备更稳定的前端展示与导出顺序，而不需要改动外部 SSE / trace / e2e 契约。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`401/401`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-12 主线续推（session export trace fallback 单源化）：后端
`backend/app/api/routes/sessions.py` 的 task governance trace fallback 现已不再本地手工
`json.loads + parse_trace_steps`，而是优先复用 shared `chat_persistence_service._extract_task_governance_from_trace_json()`；
同时 task-level governance dict 的 clone 形状也收口到单一 helper。这样 session export 的 task-level governance、
session-level governance summary 与 persistence service 的 trace parser 主干进一步对齐，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`402/402`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-15 主线续推（session-level governance outward clone 单源化）：后端新增 shared
`chat_persistence_service._clone_session_governance_summary_dict()`，并让 `tasks.py` 的
`_build_task_usage_session_governance_summary_from_dict()` 与 `sessions.py` 的
`_build_session_export_governance_summary_from_dict()` 一起退回到这条 helper，不再各自手工拷贝
`profiles / provider_sources / allowed_tool_names / allowed_tool_labels`。这样 usage dashboard 会话榜与 session export
在 session-level governance outward 形状上也开始共用同一套 clone 主干，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`404/404`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（usage dashboard governance outward 单源化收尾）：后端
`backend/app/api/routes/tasks.py` 的 `GET /api/tasks/usage/dashboard` route 现在会直接信任
`chat_persistence_service.get_tasks_usage_dashboard()` 已产出的 `by_session / top_tasks governance` dict，
不再在 route 层重复走 session governance normalizer，也不再为 top-task governance 追加 task-row fallback。
这样 usage dashboard route 的 outward 组装口径进一步和 service 层治理主干对齐，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`453/453`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（governance filter normalize 单源化收尾）：后端
`backend/app/api/routes/tasks.py` 的 `GET /api/tasks` 与 `GET /api/tasks/usage/dashboard` route
现已不再先对 `tool_registry_profile / tool_registry_provider_source` 做一遍 route-level normalize，
而是直接把 raw filter 透传给 `list_tasks / count_tasks / get_tasks_usage_dashboard`，统一交给 service 层继续走 shared
governance filter normalizer。这样任务列表与 usage dashboard 两条治理筛选入口，也进一步收口到同一条 service 主干，
同时保持外部 SSE / trace / e2e 契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`453/453`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（task row governance 产出下沉到 service）：后端
`backend/app/services/chat_persistence_service.py` 的 `get_task()`、`list_tasks()` 与 `get_session_tasks()`
现在会直接在 task row 上补齐规范化后的 `governance` 字段；对应 `backend/app/api/routes/tasks.py`
里的 `GET /api/tasks` 与 `GET /api/tasks/{task_id}` 已改为只信任 service 返回的 `governance`，不再在 route
层额外调用 `_extract_task_governance_from_task_row()` 做 fallback。这样 task list / detail 两条 task-level governance outward
链，也继续向“service 负责解析、route 只负责对外组装”的主干收口，同时保持外部 SSE / trace / e2e 契约不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`454/454`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（session export task governance 也信任 service task row）：后端
`backend/app/api/routes/sessions.py` 的 `_build_session_export_payload()` 现在也不再自己调用
`_extract_task_governance_from_task_with_parsed_trace_steps()` 决定 task governance 来源，而是直接消费
`get_session_tasks()` 返回 task row 上已有的 `governance`；route 只继续负责 trace preview、RAG 统计和 session-level
governance merge。这样 session export 这条 sibling 链也进一步回到“service 负责治理解析、route 只做 outward 组装”的主干，
同时保持外部 SSE / trace / e2e 契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`454/454`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（task export governance 也信任 service task row）：后端
`backend/app/api/routes/tasks.py` 的 `_build_task_export_payload()` 现在也不再自己调用
`_extract_task_governance_from_task_with_parsed_trace_steps()` 决定 export governance 来源，而是直接消费 task row
上已有的 `governance`；route 只继续负责读取 parsed trace steps、RAG 导出统计与 outward export model 组装。这样 task export
这条 sibling 链也进一步回到“service 负责治理解析、route 只做 outward 组装”的主干，同时保持外部 SSE / trace / e2e 契约不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`454/454`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（settings registry selection 也收口回 shared helper）：后端
`backend/app/api/routes/settings.py` 的 `SettingsUpdateRequest` 现已不再在 request model 阶段直接复用
`_normalize_governance_filter()` 预规范化 `tool_registry_profile / tool_registry_provider_source`；payload 里的非空 raw
profile/source 会先原样保留，只有空白字符串会在 `_resolve_effective_tool_registry_selection()` 里被视为缺失值，再统一交给
`get_tool_registry_profile_name_from_settings()` /
`get_tool_registry_provider_source_name_from_settings()` 做真正的 canonical name 解析。这样 settings 保存/校验入口
也继续向“route 只负责缺失值决策，shared registry helper 负责命名规范化”的主干收口，同时保持外部 SSE / trace / e2e
契约不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`456/456`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（dead parsed-trace governance helper 清理）：随着
`tasks / sessions / settings` 三条 route 主链都已经不再复用旧的 task+parsed-trace governance fallback，
后端 `backend/app/services/chat_persistence_service.py` 里残留的
`_extract_task_governance_from_task_with_parsed_trace_steps()` 也已正式删除；focused tests 同步改为直接锁定这条 legacy helper
不再暴露，避免 route 主干已经收口但 service 里还挂着过期 sibling 分叉继续误导后续开发。外部 SSE / trace / e2e 契约保持不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`456/456`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（dead parsed-trace governance adapter 再收薄一层）：在上一步删掉
`_extract_task_governance_from_task_with_parsed_trace_steps()` 之后，后端
`backend/app/services/chat_persistence_service.py` 里只剩测试覆盖、生产代码已经不再使用的
`_extract_task_governance_from_parsed_trace_steps()` 也已正式删除；focused tests 同步改为直接锁定这条 legacy parsed-trace adapter
不再暴露。这样 service 内与 parsed-trace governance 相关的过期 sibling helper 又少了一层，继续降低后续误把旧 trace 适配分支当成活主干的风险。
外部 SSE / trace / e2e 契约保持不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`456/456`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（trace-json governance wrapper 再并回 task-row 主干）：在删掉 parsed-trace adapter 之后，后端
`backend/app/services/chat_persistence_service.py` 里只剩一处生产调用的
`_extract_task_governance_from_trace_json()` 也已不再单独保留；`_extract_task_governance_from_task_row()` 在 columns 没拿到治理信息时，
现在会直接复用 shared `_load_trace_steps_from_trace_json()` + `_extract_task_governance_from_trace_steps()` 完成 fallback，而不是再经过一层
legacy `trace_json -> governance` wrapper。focused tests 同步改为直接锁定这条 helper 不再暴露。外部 SSE / trace / e2e 契约保持不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`456/456`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（usage dashboard top-tasks dead trace payload 清理）：后端
`backend/app/services/chat_persistence_service.py:get_tasks_usage_dashboard()` 组装 `top_tasks` 行时，已不再继续携带内部 dead
`trace_json` 字段；当前 route 侧早已不再消费这份 trace payload，所以 focused tests 先新增 1 条红灯，锁定 `top_tasks`
不该再暴露 `trace_json`，随后实现上直接删除这份多余字段。这样 usage dashboard 的 service payload 也继续向“只返回 route 仍在消费的治理/用量摘要”收口。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`457/457`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（service task-row payload 裁掉 raw governance source columns）：后端
`backend/app/services/chat_persistence_service.py:_with_task_governance()` 在补完规范化 `governance` 之后，现已不再继续把
`tool_registry_profile / tool_registry_provider_source / allowed_tool_names_json / allowed_tool_labels_json`
这组内部 raw 治理源列往上传播给 `get_task / list_tasks / get_session_tasks` 的返回 row。focused tests 先新增 1 条红灯锁定这条 payload
不该再暴露 raw governance source columns，再做最小实现收口。这样 service task-row payload 也继续向“外层只消费规范化治理摘要、内部源列留在 persistence 边界内”推进。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`458/458`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（usage dashboard 也并回 shared task-row helper）：后端
`backend/app/services/chat_persistence_service.py:get_tasks_usage_dashboard()` 现已不再自己直连
`_extract_task_governance_from_task_row(dict(row))` 做 governance 解析，而是先统一走 `_with_task_governance(dict(row))`，再消费其中的
规范化 `governance`。focused tests 先新增 1 条红灯，锁定 dashboard 不该再跳过 shared task-row helper 直接重走 row parser；随后实现上把
dashboard 的 by-session / top-tasks 聚合一起并回这条主干。这样 usage dashboard 也开始共享同一份 task-row 规范化与 raw 列裁剪结果，
继续降低 service 内 sibling 解析分叉。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`459/459`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（reconnect stream terminal step-id 补全不再重查全量 trace）：后端
`backend/app/api/routes/tasks.py:stream_running_task_reconnect()` 在任务已结束、且当前轮询没有新 trace step 导致
`last_emitted_step_id` 仍为空时，现已不再额外调用 `get_task_trace_steps(task_id, user_id)` 按 task id 重查一次全量 trace；
而是直接复用手头这份 `task` row 走 shared `get_task_trace_steps_from_task(task)` 来补终态 `done/error` 事件里的 `step_id`。focused tests
先新增 1 条红灯，锁定 reconnect stream 不该再重复按 task id 拉全量 trace，再做最小实现收口。外部 SSE/trace/e2e 契约保持不变。
最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`460/460`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（task trace detail 也不再按 task id 重读 trace）：后端
`backend/app/api/routes/tasks.py:get_task_trace_detail()` 在已经拿到 `task` row 的前提下，现已不再继续走
`get_task_trace_steps(task_id, user_id)` 按 task id 再读一次 trace，而是直接复用 shared
`chat_persistence_service.get_task_trace_steps_from_task(task)`。focused tests 先新增 1 条红灯，锁定 trace detail route 不该再做这次重复读取，
再做最小实现收口。外部 `GET /api/tasks/{task_id}/trace` 响应契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`461/461`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（dead `get_task_trace_steps(task_id, user_id)` helper 退场）：后端
`backend/app/services/chat_persistence_service.py` 里只剩历史兼容意义、生产调用已经归零的
`get_task_trace_steps(task_id, user_id)` 现已正式删除；当前 route/export/reconnect/trace detail 这些链路在已经拿到 `task` row
后，都统一直接复用 shared `get_task_trace_steps_from_task(task)`。focused tests 先补红灯，锁定 service 与 route module 不该再继续暴露这条 dead helper，
再做最小实现收口。外部 SSE/trace/e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`461/461`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（session export governance summary 退回 shared task-row helper）：后端
`backend/app/api/routes/sessions.py:_build_session_export_payload()` 现已不再在 route 层自己循环每个 `task_row` 调
`_merge_session_governance_summary(...)` 汇总 session governance，而是统一复用
`chat_persistence_service.get_task_rows_governance_summary(task_rows)`。focused tests 先补红灯，锁定 service 要提供这条 helper，
且 session export route 要直接走它而不是逐行 merge；随后再做最小实现收口。这样 session export 这条治理摘要链也开始和 shared task-row/service 主干站到同一处。
外部 session export / SSE / trace / e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`462/462`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（trace delta route 不再双读整份 trace）：后端
`backend/app/api/routes/tasks.py:get_task_trace_delta_detail()` 现已不再先调
`get_task_trace_delta_steps_from_task(task)` 再额外走 `_latest_seq_from_task(task)` 把整份 trace 再读一遍来算 `lag_seq`；
当前改为统一复用新的 shared `chat_persistence_service.get_task_trace_delta_snapshot_from_task(task)`，单次读取就返回
`delta steps + next_cursor + has_more + latest_seq`，同时 route 侧 dead helper `_latest_seq_from_task()` 也已退场。focused tests
先补红灯，锁定 route 不该再分开重算 delta 与 latest seq，再做最小实现收口。外部 `GET /api/tasks/{task_id}/trace/delta`
契约保持不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`464/464`），
`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（reconnect terminal step-id 也退回 shared delta snapshot helper）：后端
`backend/app/api/routes/tasks.py:stream_running_task_reconnect()` 现已不再先调一次
`get_task_trace_delta_steps_from_task(task)`，然后在任务已结束且当前轮询没有新 step 时，再额外走
`get_task_trace_steps_from_task(task)` 重读整份 trace 来补终态 `step_id`；当前改为统一复用扩展后的 shared
`chat_persistence_service.get_task_trace_delta_snapshot_from_task(task)`，单次读取就直接返回
`delta steps + next_cursor + has_more + latest_seq + latest_step_id`，同时 `tasks.py` 里那条只剩直引意义的
`get_task_trace_delta_steps_from_task` 也已退场。focused tests 先补红灯，锁定 reconnect 不该再双读 trace，再做最小实现收口。
外部 SSE / trace / e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`465/465`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（route 层 usage 解析退回 shared task-row helper）：后端
`backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 里剩下的三处
`_parse_usage_json_blob(...)` 直连已经一起收回 shared service。当前 `chat_persistence_service.get_task_usage_from_task(task)`
统一承接 `task export`、`session export` 和 `stream_running_task_reconnect()` 终态 `done` 事件里的 usage 读取，route 不再直接依赖 persistence 私有 usage parser。
focused tests 先补红灯，锁定 service 要提供 shared usage helper，且三条 route 路径都必须复用它，再做最小实现收口。外部
export / SSE / trace / e2e 契约保持不变。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`
通过（`467/467`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（dead `get_task_trace_delta_steps_from_task()` helper 退场）：后端
`backend/app/services/chat_persistence_service.py` 里只剩历史兼容意义、生产调用已经归零的
`get_task_trace_delta_steps_from_task()` 现已正式删除。当前 route 侧 trace delta / reconnect 已统一停在
`get_task_trace_delta_snapshot_from_task(task)` 主干，focused tests 也同步补红灯，锁定 service 不该再继续暴露这条 dead helper，
再做最小实现收口。外部 SSE / trace / e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`467/467`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（session export trace preview / RAG 统计退回 shared service）：后端
`backend/app/api/routes/sessions.py:_build_session_export_payload()` 现已不再在 route 层自己读取 `TraceStep[]` 后手工循环计算
`rag_hit_count` 与 `trace_preview`；当前改为统一复用新的
`chat_persistence_service.get_task_trace_preview_summary_from_task(task)`，由 shared service 统一返回
`trace_step_count + rag_hit_count + trace_preview` 摘要。focused tests 先补红灯，锁定 service 要提供这条 helper，且 session export route
必须直接走它而不是继续自己拆 `TraceStep`，再做最小实现收口。外部 session export / SSE / trace / e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`469/469`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

2026-06-17 主线续推（task export RAG 拆解退回 shared service）：后端
`backend/app/api/routes/tasks.py:_build_task_export_payload()` 现已不再在 route 层自己循环 `TraceStep[]` 手工提取
`rag_hit_count / rag_knowledge_base_ids / rag_chunks`；当前改为统一复用新的
`chat_persistence_service.get_trace_rag_export_summary(trace_steps)`，由 shared service 统一返回 task export 需要的 RAG 摘要。focused tests
先补红灯，锁定 service 要提供这条 helper，且 task export route 必须直接走它而不是继续调用本地 `_collect_rag_export(...)`，
再做最小实现收口。外部 task export / SSE / trace / e2e 契约保持不变。最新校验：
`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`471/471`），`cd frontend && npm run lint`
通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。

## 当前能力摘要

- backend：
  - 后端数据库已收敛为 PostgreSQL（`INSIGHT_AGENT_DATABASE_URL`）
  - 2026-04-14 修复：`POST /api/tasks` 在 PostgreSQL 下的会话标题自动命名 SQL 类型兼容问题（`CASE WHEN` 布尔参数），消除“发会话消息无响应”根因（后端 500）
  - 保留 `backend/scripts/migrate_sqlite_to_postgres.py` 用于历史 SQLite 数据一次性平迁（幂等 upsert）
  - `POST /api/tasks`、`GET /api/tasks/{task_id}/stream`、`GET /api/tasks/{task_id}/trace`、`GET /api/tasks/{task_id}/trace/delta`
  - RAG：`GET /api/rag/status`、`POST /api/rag/ingest`、`POST /api/rag/query`
  - RAG 治理最小版：`GET /api/rag/knowledge-bases`、`POST /api/rag/knowledge-bases/{knowledge_base_id}/clear`、`DELETE /api/rag/knowledge-bases/{knowledge_base_id}`
  - `rag-rbac-lite` 增量（2026-05-09）：知识库支持 `shared-*` 前缀共享命名空间；`admin` 可写共享库（ingest/clear/delete），普通用户对共享库只读（status/query/list），个人库维持用户隔离可读写
  - `trace/delta` 支持 `limit` 参数控制单次增量返回量（默认 200，最大 500）
  - 流式阶段会按批次持久化最终 `observation`（默认每 8 个 token chunk 一次，并在结束时兜底），`trace/delta?after_seq=` 可持续拉取增量
  - 前端在 `trace/delta` 返回 `has_more=true` 时会快速连续拉取，优先清空积压增量
  - `GET /api/tasks` 支持 `limit/offset` 与可选 `session_id` 过滤，响应含 `total/has_more`
  - 任务相关响应补充状态派生字段：`status_normalized`、`status_label`、`status_rank`（向后兼容）
  - `GET /api/tasks/usage/summary` 支持全局与按 `session_id` 的 usage 聚合
  - `GET /api/tasks/usage/dashboard`：返回最近窗口趋势、会话榜、任务榜（支持按 `session_id` 聚合）
  - `GET /api/tasks/usage/dashboard` 支持 `source_kind` 筛选（`all/provider/estimated/mixed/legacy`）
  - `GET /api/tasks/usage/dashboard` 的 `trend` 已补来源趋势字段：`source_tasks_provider/source_tasks_estimated/source_tasks_mixed/source_tasks_legacy`
  - usage 从占位升级为可计算：`prompt_tokens/completion_tokens/cost_estimate`（并支持单价配置）
  - `provider-usage-alignment` 首版已落地：执行链路优先写入 provider 官方 usage（`prompt_tokens/completion_tokens`），缺失字段自动回退估算，并在 `done.usage` 标注 `prompt_tokens_source/completion_tokens_source/usage_source`
  - `e2e-main-path` 已补 usage 来源断言：校验 `done.usage` 的 token 数值与来源字段（`prompt_tokens_source/completion_tokens_source/usage_source`）
  - usage 汇总接口已补来源维度统计：`source_tasks_provider/source_tasks_estimated/source_tasks_mixed/source_tasks_legacy`
  - 会话接口支持创建/列表/详情/消息/重命名/删除
  - 设置接口支持读取/保存/校验：`GET /api/settings`、`PUT /api/settings`、`POST /api/settings/validate`
  - `remote` 模式已接入 OpenAI-compatible Provider：基于 `base_url + api_key + model` 调用 `/chat/completions`（支持多家兼容厂商按配置接入）
  - `remote` 模式严格校验：缺少 `api_key` 或 `base_url` 时任务流返回明确错误码（不再静默回落 `mock`）
  - `remote-provider-hardening` 已完成首轮收口：Provider 运行时错误统一为结构化错误码（401/403、429、5xx、网络错误、无效 JSON、空响应、SSE 中断），并通过 SSE `error` 事件稳定透传 `code/fatal/retryable/detail/status_code`
  - `task-cancel-timeout` 首版已落地：新增 `POST /api/tasks/{task_id}/cancel`，任务执行链路支持用户取消与超时中断（`TASK_TIMEOUT_SEC`），并发出 `cancelled/timeout` SSE 事件
  - `task-cancel-timeout` e2e 覆盖已补齐：新增 `backend/scripts/e2e_task_cancel_timeout.py`（取消链路默认可跑，超时链路建议低 `TASK_TIMEOUT_SEC` 环境）
  - 后端 e2e CI 已扩展：GitHub Actions 工作流 [backend-e2e.yml] 现覆盖 `baseline/main-path/export-consistency/cancel/timeout`；工作流已升级 `actions/checkout@v6`、`actions/setup-python@v6` 以适配 GitHub Actions Node 24 运行时（消除 Node 20 action 弃用提示）；Python 运行时统一为 **3.14**（与 `compose.full.yml`、根目录 `.python-version` 一致）；前端 **Node.js 24.x** 已对齐（`compose.full.yml` 前端镜像、`frontend/package.json` engines、根目录 `.nvmrc`）；并新增后端 e2e 失败快照归档（e2e 日志 + health + 诊断 + backend 日志 artifact 上传）
  - e2e-main-path 增量（2026-05-09）：后端主链路脚本新增 `shared-*` 知识库权限断言（普通用户写共享库 `403`、共享库读能力稳定；若当前账号具备 admin 权限则追加 admin 写共享库成功断言）
  - 后端 e2e 可观测性增强：`backend-e2e` 现会在 CI Summary 输出 `export-consistency` 关键检查点快照（含步骤与 OK 项），并额外输出断言计数统计（steps/ok/pass/task-export/session-export/shared-rag/cross-user/not-found）与归档 `/tmp/e2e-export-consistency-summary.txt`；失败诊断新增导出一致性日志尾部片段，便于快速定位权限或导出结构回归
  - 后端 e2e 规则对齐（2026-05-09）：`backend-e2e` 的 export consistency 摘要阈值已对齐脚本 7 步输出（`steps/ok` 期望由 6 调整为 7），并新增 `shared_rag_semantics_ok` 计数，避免共享知识库权限回归被摘要漏报
  - 后端 e2e 规则增强（2026-05-09 补充）：`backend-e2e` 的 export consistency 步数阈值改为自动从日志首条 `[x/N]` 解析 `N`，不再硬编码固定步数，降低后续脚本步骤变更带来的 CI 维护成本
  - 后端 e2e 维护性补充（2026-05-09）：`backend-e2e` export consistency 摘要新增 `add_warning` 统一告警函数，收敛 `P0/P1` 计数与消息拼装逻辑，减少阈值规则扩展时的重复改动
  - 后端 e2e 告警增强（2026-05-08）：`backend-e2e` 的 export summary 新增阈值告警行（`Threshold alerts`），当计数偏离预期时会直接列出异常项（expected vs actual），便于在 PR Summary 快速分流回归类型
  - CI 告警分级（2026-05-08）：`backend-e2e` 与 `frontend-e2e` 的导出告警已统一增加严重级别标签（`[P0]/[P1]`）与级别计数（`severity: P0=..., P1=...`），便于快速判断处理优先级
  - CI 告警模板统一（2026-05-08）：`backend-e2e` 与 `frontend-e2e` 导出摘要已统一为同字段顺序（`total_alerts` → `severity` → 分级明细）与同类作用域标签格式（如 `[P1][backend-export-consistency]` / `[P1][workbench-main-path]`），提升跨工作流对比效率
  - CI 规则收口（2026-05-08）：`backend-e2e` 与 `frontend-e2e` 的导出诊断规则已提炼为 workflow 内变量块（关键计数 regex / key-lines regex），并统一 `total_alerts=0` 时输出 `severity: P0=0, P1=0`，降低后续规则扩展时的维护分叉
  - CI 告警分级补强（2026-05-08）：`frontend-e2e` 新增 `P0` 诊断失真判定（存在 `error-context` 但 `export_api_path_hints=0`，或 UI/响应头双计数同时为 0；edge-cases 另含 `export_404_semantic_hints=0`），用于快速识别“诊断信息缺失/异常分类失真”风险
  - 导出链路回归补齐：新增 `backend/scripts/e2e_export_consistency.py`，校验任务/会话导出 JSON 与 Markdown 字段一致性、`download=true` 附件头、跨用户导出隔离 404 与不存在资源 404 语义
  - 导出链路回归加固：`e2e_export_consistency` 新增导出 `Content-Type` 断言（JSON=`application/json`、Markdown=`text/markdown`，含 download 场景），防止 MIME 类型回归漏检
  - 导出链路回归补充（2026-05-09）：`e2e_export_consistency` 新增 `shared-*` 跨角色语义断言（普通用户写共享库 `403`；当当前账号为 admin 时补充“admin 写入后普通用户可读”断言），确保共享库权限演进不破坏导出主链路
  - 前端可视化回归 CI 首版已接入：新增 Playwright 用例（用量统计弹窗主路径 + 来源趋势区可见性）与 `.github/workflows/frontend-e2e.yml`（拉起 postgres/chroma/backend 后执行前端 e2e）
  - CI Node 24 对齐补充：`frontend-e2e` 工作流已将 `actions/setup-node` 升级到 `v5`、`actions/upload-artifact` 升级到 `v7`，并显式启用 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`，消除 Node 20 action 弃用告警
  - CI 修复补充：修正 `.github/workflows/frontend-e2e.yml` 的 YAML 缩进错误（`push` 触发器误嵌套到 `env`），恢复工作流可解析性
  - 前端回归稳定性修复：Playwright 登录态注入改为显式透传 storage key（避免浏览器上下文常量不可见导致的未登录误失败）
  - 前端回归稳定性修复（第二次）：`usage-dashboard` 用例新增 UI 登录兜底（若未命中 Workbench 则自动登录），避免 CI 冷启动/状态差异下找不到设置入口
  - 前端回归覆盖扩展（一次性补齐）：Playwright 新增“设置菜单治理入口”场景（审计日志/知识库治理弹窗可见性），并为设置菜单项补充稳定 `data-testid`（`settings-menu-audit/settings-menu-knowledge-base/settings-menu-model`）
  - 前端回归补齐（2026-05-09）：Playwright 新增“非 admin 用户在知识库治理中对 `shared-*` 行的清空/删除按钮禁用”断言，确保 UI 权限提示与后端 `403` 语义一致
  - 前端回归补齐（2026-05-09 补充）：上述 `shared-*` 权限断言已补入 `workbench-main-path` 主链路用例，确保主路径回归也覆盖“非 admin 共享库按钮禁用、私有库按钮可用”语义
  - 前端回归覆盖扩展（二次补齐）：Playwright 新增 `workbench-main-path` 场景，覆盖对话发送、Trace 可见、RAG ingest/query、任务/会话 JSON+Markdown 导出、运行中任务刷新恢复与取消后再次发送；并为 Composer/Inspector 导出与 RAG 控件补充稳定 `data-testid`
  - 前端回归覆盖扩展（三次补齐）：新增 `workbench-edge-cases` 场景，覆盖 RAG 空命中可见性与导出接口缺失资源 `404` 语义断言
  - 前端回归工程化补充：e2e 登录/鉴权预置与 Workbench 就绪逻辑已抽取到 `frontend/e2e/helpers/workbench.ts`，降低重复维护
  - 前端回归矩阵扩展：Playwright 已增加 `firefox/webkit` 项目；`frontend-e2e` 工作流改为“smoke 三浏览器 + chromium 全量”，并新增 remote 网络错误映射回归；当前本地回归已验证 chromium 全量 `25/25` 与 smoke 矩阵 `15/15`
  - 前端回归深化：usage/知识库治理/回底按钮已补稳定 `data-testid`，并新增细粒度断言（来源筛选请求参数、表头左对齐、治理动作无边框文本按钮、上滑阅读时回底按钮显隐交互）
  - 前端异常态覆盖深化：Playwright `workbench-remote-errors` 新增 `503` 映射与“remote 取消后发送冷却恢复”用例（含请求次数断言、冷却期阻断重发、冷却后恢复发送），避免“取消后立即重发触发上游限流”回归
  - 前端设置校验异常态覆盖：Playwright 新增模型设置弹窗 `settings/validate` 回归（`remote_api_key_unauthorized` 与 `remote_preflight_network_error`），并给模型设置关键控件补充稳定 `data-testid`，降低多语言与提示时序引发的误报
  - 前端流式异常态覆盖：Playwright 新增 `remote_provider_stream_invalid_json` 与 `remote_provider_stream_interrupted` 回归，并完成 Context tab helper 稳态修复（改为“重试点击直到 `#inspector-panel-context` 可见”）
  - 前端导出异常态覆盖补齐：`workbench-edge-cases` 新增“空会话导出”（JSON/Markdown）与“跨用户导出隔离（task/session 均返回 404）”回归，补全 `trace/session export` 的空数据与权限语义断言
  - 前端导出协议断言补齐：`workbench-edge-cases` 新增“download 响应 `Content-Type` 与附件扩展名一致性”回归，覆盖 task/session 的 JSON/Markdown 导出
  - 前端主链路导出校验增强：`workbench-main-path` 已升级为“UI 下载事件 + 同路径 API 响应头”双重断言，覆盖 task/session 的 JSON/Markdown 导出 `Content-Type` 与附件扩展名一致性
  - 前端导出错误提示一致性收口：Inspector 导出请求统一走 `ApiError` 映射（不再直接透出后端原始 detail），并新增 UI 回归“切换为其他用户 token 后触发导出 404，提示包含 404 且按钮可恢复”
  - 回归稳定性补丁：`mock` provider 新增仅测试触发的慢流标记（`[mock-slow]` / `[mock-slow-ms=30]`），用于稳定复现“刷新恢复 + 取消”路径，默认请求行为不变
  - 回归稳态补丁（恢复取消链路）：`workbench-main-path` 新增“先确认后端进入 running/pending 再刷新”的断言，并在等待取消按钮期间持续维持 Context 面板激活，规避自动恢复切回 Trace 的时序抖动
  - 回归覆盖一次性补齐（会话与取消交互）：`workbench-edge-cases` 新增 3 条回归（取消后同文案立即重发不丢消息、跨会话切换时流式状态严格会话隔离且切回可取消、mock 取消后不出现重试按钮且可快速恢复发送）；本地全量 e2e 最新为 chromium `25/25`
  - 回归覆盖六项一次性补齐：新增“恢复提示三态（恢复中/成功/失败）”“trace delta 重试 + 后台暂停/前台恢复”“auth refresh 自动续期 + logout-all 失效重登”“设置弹窗与治理子弹窗重开状态重置”回归；并补充稳定测试锚点（`chat-recovery-notice`、`inspector-trace-sync-*`、`audit-*`、`settings-section-trigger-*`）。
  - CI 稳定性增强：`frontend-e2e` 新增并发互斥（同分支取消旧运行）、失败后 `--last-failed` 诊断重跑、`error-context.md + trace.zip` 失败索引文件并随 artifact 上传（artifact 名含 `run_id/run_attempt`），便于快速定位失败快照。
  - CI 诊断增强（2026-05-08）：`frontend-e2e` 导出断言摘要已扩展覆盖 `workbench-main-path` + `workbench-edge-cases`（从 `error-context` 提取 UI 下载层/响应头层/API 路径/404 语义提示计数，并输出关键行），结果同步写入 `GITHUB_STEP_SUMMARY` 与 artifact（`/tmp/frontend-e2e-export-summary.md`）
  - CI 诊断增强（2026-05-09）：`frontend-e2e` 导出摘要新增 `workbench-main-path-shared-kb` 分区，仅在 shared 权限主链路用例失败上下文存在时统计 `shared_permission_semantic_ok` 并告警，降低主链路权限回归的定位成本
  - CI 告警汇总补齐（2026-05-09）：`frontend-e2e` 的 `threshold alerts` 新增 shared 分区汇总行（`shared_scope: workbench-main-path-shared-kb contexts=..., shared_permission_semantic_ok=...`），便于在无告警场景也快速确认 shared 诊断覆盖状态
  - CI 摘要语义收口（2026-05-09）：`frontend-e2e` 的 shared 分区补充 `expected` 文案（有 shared 失败上下文时期望 `>=1`，无上下文时期望 `0`），并在无上下文场景仍输出 shared 计数，减少诊断歧义
  - CI 稳态补充（2026-05-09）：`frontend-e2e` shared 分区的 error-context 匹配已从固定子串改为正则模式（`workbench-main-path.*shared.*kb`），降低测试标题微调导致诊断分区漏命中的风险
  - CI 稳态补充（2026-05-09 再补充）：shared 分区匹配已进一步收紧为变量化路径规则 `SHARED_CONTEXT_PATH_REGEX=workbench-main-path.*(shared-kb-actions-disabled|shared.*kb.*disabled)`，兼顾命名弹性与误匹配控制
  - CI 维护性优化（2026-05-09）：`frontend-e2e` 导出摘要对 `error-context.md` 的扫描已收敛为单次 `find` 后分组（main/shared/edge），减少重复 IO 并降低分区统计来源漂移风险
  - CI 维护性优化（2026-05-09 再补充）：`frontend-e2e` 导出摘要新增 `add_warning` 统一告警函数，收敛 `P0/P1` 计数与消息拼装逻辑，减少重复分支并降低阈值扩展时的改动面
  - CI 准确性补充（2026-05-09）：`frontend-e2e` 的 `workbench-main-path` 分区已排除 shared 专项上下文（通过 `SHARED_CONTEXT_PATH_REGEX` 反向过滤），避免 shared 用例失败时误触主链路导出提示告警
  - CI 规则收口补充（2026-05-09）：`frontend-e2e` 新增 `MAIN_CONTEXT_PATH_REGEX` 与 `EDGE_CONTEXT_PATH_REGEX`，main/edge/shared 三个分区匹配均改为变量化规则入口，降低手写字符串分叉风险
  - CI 维护性优化（2026-05-09 三次）：`frontend-e2e` 导出摘要新增 `print_matched_files` / `print_key_lines` 统一输出函数，收敛分区内重复循环，降低 main/shared/edge 三分区展示逻辑的维护成本
  - CI 诊断可见性补充（2026-05-09）：`frontend-e2e` 的 main/shared/edge 三个导出分区断言计数已补 `context_files_detected` 字段，便于直接核对当前分区统计是否来源于预期失败上下文样本
  - CI 工程化重构（2026-05-09）：`frontend-e2e` 的导出诊断逻辑已从 workflow 内联 Bash 抽离到 `frontend/scripts/ci_export_diagnostics.sh`（workflow 改为脚本调用），并补齐本地 Bash 3 兼容（移除 `mapfile` 依赖），实现“本地可复跑 + CI 可复用 + 规则单点维护”
  - CI 工程化重构（2026-05-09 补充）：`backend-e2e` 的 export consistency 摘要逻辑已抽离为 `backend/scripts/ci_export_consistency_summary.sh`，workflow 改为脚本调用并保持 Summary/artifact 输出路径不变，后续阈值规则维护可单点收敛
  - CI 回归护栏补齐（2026-05-09）：新增 `frontend/scripts/test_ci_export_diagnostics.sh` fixture 自测脚本并接入 `frontend-e2e` workflow（`Validate export diagnostics fixture tests`），用于在发布前校验导出诊断脚本的 counters/alerts 语义稳定性
  - CI 工程化扩展（2026-05-09）：前后端导出诊断脚本均已支持可选 JSON 摘要输出（`frontend: /tmp/frontend-e2e-export-summary.json`、`backend: /tmp/e2e-export-consistency-summary.json`）并纳入 workflow artifact，便于后续做趋势对比与机器消费
  - CI 回归护栏扩展（2026-05-09）：新增 `backend/scripts/test_ci_export_consistency_summary.sh` fixture 自测并接入 `backend-e2e`（`Validate export consistency summary fixture tests`），覆盖成功/缺陷/日志缺失三类诊断语义
  - CI 统一门禁（2026-05-09）：新增 `scripts/ci_diag_guard.sh`（配套 `scripts/test_ci_diag_guard.sh`）统一消费前后端 JSON 诊断摘要，支持 `strict-level=none|p0|any`；`frontend-e2e` 与 `backend-e2e` 均已接入 `Evaluate export diagnostics guard` 步骤并提供环境变量开关（默认 `none`）
  - CI 统一门禁（2026-05-09 补充）：前后端 workflow 的门禁默认级别已从 `none` 提升到 `p0`（仅拦截高优先级诊断），并将门禁判定结果（scope/strict/warnings/gate_result）写入 `GITHUB_STEP_SUMMARY` 与 guard artifact，降低排查上下文切换成本
  - CI 统一门禁（2026-05-09 策略化补充）：前后端 workflow 现按事件自动选择门禁级别：`push@main => any`、其余场景 `=> p0`；并在 summary 显式输出 `policy` 与 `selected_strict_level`，便于快速确认当前门禁生效策略
  - CI 统一门禁（2026-05-09 JSON 补充）：`ci_diag_guard` 新增 `--json-summary-file`，前后端 workflow 已产出 guard JSON（`/tmp/frontend-e2e-export-guard-summary.json`、`/tmp/backend-e2e-export-guard-summary.json`）并纳入 artifact，支持后续跨运行自动汇总
  - CI 统一门禁（2026-05-09 触发覆盖补充）：`frontend-e2e` 与 `backend-e2e` 的 `workflow_dispatch` 已新增 `export_diag_strict_level=auto/none/p0/any`，手动触发时可覆盖自动策略；summary 同步输出 `dispatch_override` 与 `policy_source`
  - CI 总览聚合（2026-05-09）：新增 `scripts/ci_export_diagnostics_overview.sh`（配套 `scripts/test_ci_export_diagnostics_overview.sh`），前后端 workflow 均新增 `Build export diagnostics overview`，产出 overview Markdown/JSON 并追加至 `GITHUB_STEP_SUMMARY` 与 artifact
  - CI 策略解析收口（2026-05-09）：新增 `scripts/ci_resolve_diag_strict_level.sh`（配套 `scripts/test_ci_resolve_diag_strict_level.sh`），前后端 workflow 的 strict-level 选择逻辑统一改为脚本解析（`event/ref/default/main_push/dispatch_override`），减少重复脚本分叉
  - CI fixture 入口收口（2026-05-09）：新增聚合脚本 `scripts/test_ci_e2e_tooling.sh`，前后端 workflow 分别以 `backend/frontend` scope 调用，统一执行 resolver/guard/overview 与端侧诊断脚本测试，减少 workflow 内重复测试步骤
  - CI 流水线收口（2026-05-09）：新增统一脚本 `scripts/ci_export_diag_pipeline.sh`（配套 `scripts/test_ci_export_diag_pipeline.sh`），将 strict-level 解析、guard 判定、overview 产物与 summary 追加合并为单入口；`frontend-e2e` 与 `backend-e2e` 已统一改为 `Run export diagnostics pipeline` 步骤
  - CI 输出降噪（2026-05-09）：`ci_diag_guard` 新增 `--quiet`，并在 `test_ci_diag_guard.sh` / `test_ci_resolve_diag_strict_level.sh` 中收敛预期失败分支输出，降低 fixture 步骤日志噪音
  - CI 启动与验活收口（2026-05-09）：新增 `scripts/ci_start_bg_process.sh` 与 `scripts/ci_wait_http_status.sh`（配套 `scripts/test_ci_service_bootstrap.sh`），统一后台进程启动与 HTTP 状态等待逻辑；`backend-e2e` / `frontend-e2e` 的后端启动与健康检查已切换到脚本调用
  - CI 诊断流程再收口（2026-05-09）：新增 `scripts/ci_export_diag_flow.sh`（配套 `scripts/test_ci_export_diag_flow.sh`），将“诊断摘要生成 + guard/overview pipeline”合并为单入口；前后端 workflow 对应步骤统一为 `Run export diagnostics flow`
  - CI artifact 收口（2026-05-09）：新增 artifact 清单文件 `scripts/ci_artifacts_backend.txt`、`scripts/ci_artifacts_frontend.txt` 与统一归集脚本 `scripts/ci_stage_artifacts.sh`（配套 `scripts/test_ci_stage_artifacts.sh`）；前后端 workflow 上传前新增 `Stage ... artifacts` 步骤，改为上传 staging 目录，降低 YAML 长列表维护成本
  - CI 失败诊断脚本化（2026-05-09）：新增 `scripts/ci_collect_backend_failure_diagnostics.sh` 与 `scripts/ci_build_frontend_failure_index.sh`（配套 `scripts/test_ci_collect_backend_failure_diagnostics.sh`、`scripts/test_ci_build_frontend_failure_index.sh`）；前后端 workflow 已改为脚本生成失败快照与 Playwright 失败索引，减少内联 shell 维护成本
  - CI 执行入口收口（2026-05-09）：新增 `scripts/ci_run_backend_e2e.sh` 与 `scripts/ci_run_frontend_e2e.sh`（配套 `scripts/test_ci_run_backend_e2e.sh`、`scripts/test_ci_run_frontend_e2e.sh`）；前后端 workflow 的 e2e 执行命令块已改为脚本调用，支持 `--dry-run` 语义校验与本地复跑
  - CI backend 启动收口（2026-05-09）：新增 `scripts/ci_boot_backend_instance.sh`（配套 `scripts/test_ci_boot_backend_instance.sh`），统一 backend 实例“启动 + 健康等待”流程；`backend-e2e` 与 `frontend-e2e` 的 backend 拉起步骤已改为单入口脚本调用
  - CI finalize 编排收口（2026-05-09）：新增 `scripts/ci_finalize_e2e_scope.sh`（配套 `scripts/test_ci_finalize_e2e_scope.sh`），统一执行“export diagnostics flow + artifact stage”两段收尾动作；前后端 workflow 已将两步合并为单步 finalize 调用
  - CI 失败日志展示收口（2026-05-09）：新增 `scripts/ci_print_log_files.sh`（配套 `scripts/test_ci_print_log_files.sh`），前后端 workflow 的失败日志展示步骤已统一为脚本调用；并将 `/tmp/frontend-e2e-backend.log` 纳入 frontend artifact 清单，避免仅控制台可见
  - CI upload 路径解耦（2026-05-09）：`ci_finalize_e2e_scope.sh` 新增 `--github-output-file` 输出能力，前后端 workflow upload 步骤已改为读取 finalize step output 的 `artifacts_stage_dir`，避免在 YAML 中硬编码 stage 路径
  - CI upload 命名解耦（2026-05-09）：`ci_finalize_e2e_scope.sh` 新增 `artifact_name` 输出，前后端 workflow upload 步骤的 `name` 字段已改为读取 finalize step output，命名策略集中在 finalize 调用参数
  - CI finalize workflow 入口收口（2026-05-11）：新增 `scripts/ci_finalize_e2e_for_workflow.sh`（配套 `scripts/test_ci_finalize_e2e_for_workflow.sh`），统一封装 workflow 侧 strict-level 默认值、事件上下文解析与 artifact 命名策略；前后端 workflow 的 finalize 步骤已改为调用该脚本，减少 YAML 参数拼装重复
  - CI artifact stage 指标补齐（2026-05-11）：`ci_finalize_e2e_scope.sh` 现会回传 `artifact_included_count/artifact_missing_count/artifact_manifest`，并将 artifact stage 统计追加到 step summary；`ci_finalize_e2e_scope` fixture 已补对应断言，方便快速定位“上传物为空/缺失”的根因
  - CI artifact stage 门禁补齐（2026-05-11）：新增 `scripts/ci_assert_artifact_stage_health.sh`（配套 `scripts/test_ci_assert_artifact_stage_health.sh`），前后端 workflow 新增 artifact stage guard 步骤并写入 summary/json；支持 `strict-level=none|warn|fail-on-empty|fail-on-missing`（当前默认 `warn`）
  - CI artifact stage 策略解析收口（2026-05-11）：新增 `scripts/ci_resolve_artifact_stage_strict_level.sh`（配套 `scripts/test_ci_resolve_artifact_stage_strict_level.sh`），前后端 workflow 的 artifact guard strict-level 改为脚本按 `event/ref/default/main_push/dispatch_override` 自动决策；并新增 `workflow_dispatch.artifact_stage_strict_level=auto|none|warn|fail-on-empty|fail-on-missing` 覆盖入口（当前策略：`push@main=warn`、其余 `warn`）
  - CI artifact stage 防误伤补丁（2026-05-11）：artifact guard 步骤改为“仅在 finalize 成功时执行”；若 finalize 失败则在 summary 记录 `skipped` 原因并不追加二次门禁失败，避免掩盖首个真实失败点
  - CI artifact stage PR 门禁扩展（2026-05-12）：`fail-on-empty` 现已扩展到 PR 场景，并支持通过 `min_included_count` 设定最小产物数阈值；当前前后端 workflow 已为 PR/关键分支场景启用 `fail-on-empty + min_included_count=2`，默认 `push@main` 仍维持 `warn` 以避免误伤现有单产物路径
  - CI artifact stage workflow 透传补丁（2026-05-12）：修复 `ci_finalize_e2e_for_workflow.sh` 未透传 `min_included_count` 到 `ci_finalize_e2e_scope.sh` 的回归，避免 PR 门禁配置在 finalize 包装层丢失
  - CI artifact upload 防二次报错补丁（2026-05-12）：前后端 workflow 的 artifact upload 步骤改为仅在 `finalize_*` 成功时执行，避免 finalize 失败后继续触发 `actions/upload-artifact` 的空 path 报错；新增 `scripts/test_ci_workflow_guards.sh` 静态校验 upload guard 条件与 finalize outputs 路径引用
  - CI artifact PR 路径感知扩展（2026-05-12）：新增 `scripts/ci_resolve_artifact_stage_path_level.sh`、`scripts/ci_collect_changed_files.sh`、`scripts/ci_resolve_artifact_stage_scope_config.sh`、`scripts/ci_load_artifact_stage_scope_config.sh`、`scripts/ci_run_artifact_stage_guard.sh`、`scripts/ci_write_skipped_artifact_guard_summary.sh`（配套 `scripts/test_ci_collect_changed_files.sh`、`scripts/test_ci_resolve_artifact_stage_scope_config.sh`、`scripts/test_ci_artifact_stage_scope_integration.sh`、`scripts/test_ci_load_artifact_stage_scope_config.sh`、`scripts/test_ci_run_artifact_stage_guard.sh`、`scripts/test_ci_write_skipped_artifact_guard_summary.sh`），将 artifact guard 的 PR 升级条件从“仅看 PR”收紧为“PR 且命中关键目录/文件”；backend/frontend workflow 现已把 artifact guard 主逻辑与 finalize 失败时的 skipped summary 都下沉到脚本：先由 scope 配置脚本生成规则，再通过 load helper 落地为 shell 变量，供 changed-files、path-regex、`pr_ref_regex` 与 guard 摘要元信息复用，`actions/checkout` 同步切到 `fetch-depth: 0`。同时修正 path-regex 语义，使其既能命中真实目录前缀改动（如 `backend/...`、`frontend/...`），也能命中 `compose.full.yml` / workflow 文件本身，避免浅克隆、缺失 base SHA 或旧正则过窄带来的 guard 误判
  - CI workflow guard 收口（2026-05-13）：`backend-e2e` 与 `frontend-e2e` 已移除对 `--guard-markdown-out/--guard-json-out` 的显式传参，guard 输出路径统一由 finalize/scope 脚本解析；`scripts/test_ci_workflow_guards.sh` 也已切换为验证“workflow 继续调用统一 guard 脚本，但不再硬编码 guard 输出路径”
  - `tool-runtime-productionization` 后端切片持续推进（2026-05-13）：已将 mock tool plan 构建与单 tool 执行逻辑从 `backend/app/services/chat_execution_service.py` 抽离到新的 `backend/app/services/tool_runtime.py`，并补出统一入口 `execute_tool_spec`；当前进一步收口为最小 registry 结构（`mock_plan` / `mock_retrieve` / `calc_eval`）、显式 `ToolInvocation` 归一化边界，以及带最小 runtime 元信息的 `ToolRegistration(name, kind, label, retryable_by_default, default_timeout_ms, requires_user_context, supports_result_preview, runner)` 注册项结构；同时已新增并接线多个内部 helper：`build_tool_result_preview()`、`tool_requires_user_context()`、`is_tool_retryable_by_default()`、`get_tool_default_timeout_ms()`、`ensure_tool_registration()`、`maybe_raise_mock_tool_execution_error()`、`ToolRuntimeContext / build_tool_runtime_context()`、`compute_tool_retry_decision()`、`build_tool_end_payload()`、`build_tool_success_meta()`、`build_tool_error_meta()`、`build_tool_start_payload()`、`build_tool_error_payload()`、`build_tool_phase()`、`build_tool_execution_policy()`、`build_action_step_initial_meta()`、`build_action_step_initial_step()`。本轮又继续补齐更高层的 attempt 生命周期与编排收尾 helper：`build_tool_attempt_start_events()`、`build_tool_attempt_bundle()`、`build_tool_attempt_execution()`、`build_tool_attempt_loop_result()`、`build_tool_attempt_loop_terminal_result()`、`build_tool_plan_item_retry_loop_result()`、`build_tool_attempt_success_events()`、`build_tool_attempt_error_events()`、`build_tool_step_success_update()`、`build_tool_step_error_update()`、`build_tool_attempt_success_transition()`、`build_tool_attempt_error_transition()`、`build_tool_attempt_result()`、`build_tool_attempt_outcome()`、`build_tool_iteration_context()`、`build_tool_iteration_execution()`、`build_tool_iteration_success_artifacts()`、`build_tool_plan_item_success_bundle()`、`build_tool_plan_item_result()`、`build_tool_plan_item_execution_result()`、`build_tool_plan_item_execution()`、`build_tool_plan_item_postprocess()`、`build_tool_plan_item_success_effects()`、`build_tool_plan_item_terminal_effects()`、`build_tool_rag_followup()`、`build_tool_step_output()`、`build_tool_observation_entry()`、`build_tool_trace_event()`、`build_tool_terminal_failure_transition()`、`build_tool_rag_step()`、`build_tool_prompt_with_observations()`；`chat_execution_service.py` 现又继续减少手工编排，改为直接消费 attempt bundle、attempt execution、attempt loop result、attempt loop terminal result、retry loop 最终结果、plan-item 顶层 `tool_end/error/retryable/postprocess` 结果，以及 success/fatal 两条路径的 effects 结果。期间发现一个 CI 回归：`build_tool_plan_item_execution()` 在新抽象后未透出 `iteration_execution`，导致成功流在编排层读取 `plan_item_execution["iteration_execution"]` 时抛 `KeyError`，任务流只发 `error` 不发 `done`，从而同时击穿后端 baseline 与前端 smoke；现已补回该字段并新增 focused 回归覆盖，并继续补了 attempt bundle、attempt execution、attempt loop result、attempt loop terminal result、retry loop 最终结果、plan-item 顶层事件访问、success/fatal effects、以及顶层 effects 透出契约测试，当前 SSE/trace/e2e 契约恢复稳定。focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 61 条兼容测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-13，retry loop helper）：本轮继续坚持“先补 failing test 再改实现”，新增 `execute_tool_plan_item_retry_loop()`，将单个 tool plan item 的完整 retry loop 控制从 `chat_execution_service.py` 下沉到 `backend/app/services/tool_runtime.py`，并保持 `tool_start/state` 仍先于实际执行发出、`tool_end/error` 顺序不变、success path 的 `trace/observation/rag_followup` 与 terminal failure path 的 `state/error` 收尾语义不变。`chat_execution_service.py` 现改为消费该 helper 的事件流与终态对象，外部 SSE/trace/e2e 契约保持稳定。focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 64 条兼容测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，loop result flattening）：在不改外部 SSE/trace/e2e 契约的前提下，继续补 focused failing tests，并新增 `build_tool_plan_item_retry_loop_execution_result()`，让 runtime 直接为单个 tool loop 暴露更扁平的终态字段 `trace_event/success_effects/terminal_effects/should_return`，同时保留旧的嵌套结果对象；`chat_execution_service.py` 因此进一步减少对 `loop_result/retry_loop_result/loop_terminal_result` 的穿透访问，成功流与 terminal failure 流都只消费更高层结果。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，stream effects helper）：继续坚持先补 failing tests，再新增 `build_tool_plan_item_stream_effects()`，把单个 tool loop 结束后 service 真正要消费的后处理效果进一步收口为 `trace_steps/trace_events/observation/terminal_effects/should_return`；`chat_execution_service.py` 已切到逐个消费这组 effects，保持 trace append 与 trace SSE/persist 的原有节奏不变，同时减少 success path 的 rag follow-up 分支胶水。focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 66 条兼容测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，terminal return helper）：继续先补 failing tests，再新增 `build_tool_plan_item_terminal_return_effects()`，把 terminal failure 分支里 service 还在手工拼的任务状态、失败审计事件和 `state(error)` payload 收口到 runtime helper；`chat_execution_service.py` 已切到消费该 helper，terminal 收尾逻辑继续缩短，外部错误语义和 SSE 契约保持不变。focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 67 条兼容测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，success seq increment）：继续在不改外部契约的前提下压薄 success path，将 `build_tool_plan_item_stream_effects()` 继续扩展为直接暴露 `seq_increment`，让 `chat_execution_service.py` 不再回头依赖 `loop_execution_result["success_effects"]["rag_followup"]` 来维护 `seq_cursor`；当前 service 成功分支已进一步收敛为“消费 stream effects 并执行副作用”。focused 回归脚本保持 67 条兼容测试，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，observation delta）：继续压薄 success path，将 `build_tool_plan_item_stream_effects()` 扩展为直接暴露 `tool_observations` 增量列表，统一表达本次 tool 成功后需要追加到 provider prompt 的 observation 文本；`chat_execution_service.py` 已切到通过 `extend(...)` 消费该 delta，不再手工 `append(str(stream_effects["observation"]))`。focused 回归脚本保持 67 条兼容测试，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，service-effects helper）：继续把 `chat_execution_service.py` 的单个 tool 分支压缩为“消费更高层 effects 并执行副作用”，新增 `build_tool_plan_item_service_effects()`，统一归并 `trace_steps/trace_events/tool_observations/seq_increment/should_return/terminal_return_effects`；`chat_execution_service.py` 已切到优先消费这层更高层结果。focused 回归脚本已扩展到 69 条兼容测试；同时已更新 [tool-runtime-productionization handoff](</Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md>) 以反映最新真实状态。最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，service-effects follow-up）：继续先补 failing tests，再把 `build_tool_plan_item_service_effects()` 进一步抬高到更接近 service 最终消费形态，新增 `trace_writes` 与 `continue_update`，分别统一表达 trace append/SSE/persist 指令，以及 success path 的 `tool_observations + seq_increment` 增量；`chat_execution_service.py` 已切到消费这两组更高层结果，同时保留旧的顶层兼容字段，外部 SSE/trace/e2e 契约保持不变。focused 回归脚本仍为 69 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，next-action helper）：继续先补 failing tests，再把单个 tool 的“继续流转 / 终止返回”分支选择也上提到 runtime，新增 `build_tool_plan_item_next_action()`，让 `build_tool_plan_item_service_effects()` 直接暴露 `next_action(kind=continue|return)`；`chat_execution_service.py` 已切到通过该对象统一消费 `continue_update` 与 `terminal_return_effects`，同时保留既有兼容字段，外部 SSE/trace/e2e 契约保持不变。focused 回归脚本仍为 69 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，return-action helper）：继续先补 failing tests，再把 terminal path 里 `complete_task / record_failure_event / state(error)` 这组三连调用的参数拼装收口到 `build_tool_plan_item_return_action()`；`chat_execution_service.py` 已切到通过该 helper 执行 terminal return 副作用，不再直接拆 `terminal_return_effects` 字段，同时保留外部 SSE/trace/e2e 契约不变。focused 回归脚本已扩展到 70 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，trace-write-action helper）：继续先补 failing tests，再把 `trace_writes` 的执行输入抬高到更明确的 runtime helper，新增 `build_tool_plan_item_trace_write_action()`，并让 `build_tool_plan_item_service_effects()` 直接暴露 `trace_write_actions`；`chat_execution_service.py` 已切到通过这层更高层输入执行 trace append/SSE/persist，同时保留旧的 `trace_writes` 兼容字段，外部 SSE/trace/e2e 契约保持不变。focused 回归脚本已扩展到 71 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，next-action-execution helper）：继续先补 failing tests，再把 `next_action` 的 continue/return 执行输入收口到 `build_tool_plan_item_next_action_execution()`；`chat_execution_service.py` 已切到通过该 helper 统一消费 `continue_update` 与 `return_action`，不再直接同时依赖 `next_action` 和 `build_tool_plan_item_return_action()`，外部 SSE/trace/e2e 契约保持不变。focused 回归脚本已扩展到 73 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，service-effects-execution helper）：继续先补 failing tests，再把 service 最终消费的执行输入再聚合一层，新增 `build_tool_plan_item_service_effects_execution()`，统一承接 `trace_write_actions + next_action_execution`；`chat_execution_service.py` 已切到通过该 helper 获取单个 tool 的最终执行结果，进一步减少对多层 helper 的串联拼装。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 75 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，service-execution helper）：继续先补 failing tests，再把 `loop_execution_result` 到 service 最终消费输入的整条链收口到 `build_tool_plan_item_service_execution()`；`chat_execution_service.py` 已切到通过这个单入口 helper 直接获取单个 tool 的最终执行结果，不再显式串联 `build_tool_plan_item_service_effects()` 与 `build_tool_plan_item_service_effects_execution()`。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 77 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-14，continue-action helper）：继续先补 failing tests，再把 success path 的 `continue_update` 对称化为显式 `build_tool_plan_item_continue_action()`；`build_tool_plan_item_next_action_execution()` 现在在 continue/return 两侧都直接暴露动作对象，`chat_execution_service.py` 也已切到通过 `continue_action` 更新 `tool_observations` 与 `seq_cursor`，同时保留 `continue_update` 兼容字段。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 78 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，service-actions helper）：继续先补 failing tests，再把单个 tool 的 service 副作用执行顺序显式化为 `build_tool_plan_item_service_actions()`；`build_tool_plan_item_service_effects_execution()` 现会直接暴露 `service_actions`，`chat_execution_service.py` 已切到按统一 action 序列顺序消费 `trace_write / continue / complete_task / record_failure_event / emit_state / return`，同时保留既有 `trace_write_actions` 与 `next_action_execution` 兼容字段。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 80 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，service-action builders）：继续先补 failing tests，再把 `service_actions` 内部几类动作也对称化为显式 helper：`build_tool_plan_item_trace_write_service_action()`、`build_tool_plan_item_continue_service_action()`、`build_tool_plan_item_return_service_actions()`；`build_tool_plan_item_service_actions()` 现在不再手拼匿名 dict，而是消费这些 builder，同时保持 `chat_execution_service.py` 和外部 SSE/trace/e2e 契约不变。focused 回归脚本已扩展到 83 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，service-execution generator）：继续先补 failing tests，再新增 `execute_tool_plan_item_service_execution()`，把“retry loop 结束后再组装 `service_execution`”这一步也前推到 runtime；`chat_execution_service.py` 现已直接消费这个更高层 generator 返回的 `service_actions`，不再显式再调一次 `build_tool_plan_item_service_execution()`。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 85 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，service-actions executor）：继续先补 failing tests，再新增 `execute_tool_plan_item_service_actions()`，把 `service_actions` 的实际执行壳子也前推到 runtime；`chat_execution_service.py` 现已只负责把 runtime 产出的 `event` 包装成 SSE 字符串，并消费最终 `seq_cursor/should_return` 结果，不再手写 action kind 分发。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 87 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，registry injection seam）：继续先补 failing tests，再让 `run_tool()`、`execute_tool_spec()`、`build_tool_runtime_context()` 以及底层 registration helpers 支持可选 `registry` 注入；默认 mock registry 行为保持不变，但 runtime seam 已具备接入真实 registry 的前置扩展位。focused 回归脚本已扩展到 90 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，registry builder seam）：继续先补 failing tests，再新增 `get_default_tool_registry()` 与 `build_tool_registry()`，并让 `get_registered_tool_names()` 也支持可选 `registry`；默认 mock registry 行为保持不变，但现在已经有显式的默认快照、merge builder 和自定义枚举入口，便于后续真实 registry 分阶段接线。focused 回归脚本已扩展到 92 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，high-level registry threading）：继续先补 failing tests，再让 `execute_tool_plan_item_retry_loop()` 与 `execute_tool_plan_item_service_execution()` 也支持可选 `registry` 透传；这样 custom registry 不再只停留在 `run_tool()` 这一层，而是已经能够贯穿到高层 runtime 入口。focused 回归脚本已扩展到 94 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，registry loader seam）：继续先补 failing tests，再新增 `load_tool_registry()`，并让默认的 registry 枚举/解析路径实际经过这个 loader；这样 runtime 不只是“可注入 registry”，默认路径本身也已经收敛到显式 loader 边界，后面接真实 registry loader 时更容易渐进替换。focused 回归脚本已扩展到 96 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，pluggable registry loader）：继续先补 failing tests，再让 `load_tool_registry()` 接受可插拔 `loader`，并把 `registry_loader` 参数线程化到 `run_tool()`、`execute_tool_spec()` 与高层 runtime 入口；这样 runtime 现在已经不只是“可注入 registry dict”，而是可以接真正的 loader/provider 空壳。focused 回归脚本已扩展到 99 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，registry provider object）：继续先补 failing tests，再新增 `ToolRegistryProvider` / `StaticToolRegistryProvider`，并让 `load_tool_registry()` 接受可选 `provider`；同时把 `registry_provider` 参数线程化到 `get_registered_tool_names()`、`resolve/ensure registration`、`build_tool_runtime_context()`、`run_tool()`、`execute_tool_spec()` 以及高层 runtime 入口。默认 mock 行为与外部 SSE/trace/e2e 契约保持不变，但 runtime 现已能直接接 provider object，而不必先手拼 registry dict。focused 回归脚本已扩展到 102 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，default provider path）：继续先补 failing tests，再新增 `get_default_tool_registry_provider()`，并让默认 `load_tool_registry()` 路径也显式经过 provider object；这样 `StaticToolRegistryProvider` 不再只停留在测试 seam，默认生产路径本身也开始消费 provider object，同时保持外部 SSE/trace/e2e 契约不变。focused 回归脚本已扩展到 104 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端切片续推（2026-05-15，named default provider）：继续先补 failing tests，再新增具名 `DefaultToolRegistryProvider`，并让 `get_default_tool_registry_provider()` 返回该实现；默认 provider 不再只是匿名静态包装，而是具备清晰的生产语义，同时继续保持默认路径走 provider object、外部 SSE/trace/e2e 契约不变。focused 回归脚本已扩展到 106 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，configured provider stack）：继续先补 failing tests，再新增 `ConfiguredToolRegistryProvider` 与 `build_tool_registry_provider()`，把 `provider / loader / overrides` 组合逻辑收口成可复用 provider 组合层；同时 `load_tool_registry()` 改为统一委托给 builder，`chat_execution_service.py` 也开始在 tool loop 外显式预构建并复用 `tool_registry_provider`，不再每次隐式零散取默认 provider。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 110 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，configured provider resolution stack）：继续先补 failing tests，再新增 `get_configured_tool_registry_provider()` 与 `resolve_tool_registry_provider()`，把 `registry / registry_provider / registry_loader` 的优先级解析收口成统一 provider resolution stack；`get_registered_tool_names()` 与 `resolve_tool_registration()` 已切到复用这层入口，`chat_execution_service.py` 也改为显式通过 `get_configured_tool_registry_provider()` 预构建默认 provider。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 113 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，settings-backed registry overrides）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_OVERRIDES_JSON`、`build_tool_registry_overrides_from_settings()`，并让 `get_configured_tool_registry_provider()` 开始真实消费 settings 中的 tool metadata 覆盖；当前默认 configured provider 已不再只是 seam，而是具备正式的配置来源链。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 116 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，settings-backed tool disable path）：继续先补 failing tests，再新增 `build_tool_registry_settings_config()` 与 `get_disabled_tool_names_from_settings()`，让 `INSIGHT_AGENT_TOOL_REGISTRY_OVERRIDES_JSON` 除 metadata override 外还能通过 `enabled=false` 禁用已注册 tool；`ConfiguredToolRegistryProvider` 与 `build_tool_registry()` 现已原生支持 disabled tool 过滤，默认 configured provider 的配置来源链又前进了一层。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 119 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，settings-backed registry profiles）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_PROFILE`、`get_tool_registry_profile_name_from_settings()` 与 `build_tool_registry_profile_settings_config()`，让默认 configured provider 支持按内建 profile（如 `planning_only`）切换一组 tool 启停，并允许 JSON overrides 局部 re-enable。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 123 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-15，settings-backed extra tool aliases）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_EXTRA_TOOLS_JSON` 与 `build_tool_registry_extra_tools_from_settings()`，让默认 configured provider 能基于已有 template tool 生成额外 alias registrations，并参与 profile / disable / override 组合链。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 126 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，settings-backed provider sources）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_PROVIDER_SOURCE`、`INSIGHT_AGENT_TOOL_REGISTRY_PROVIDER_SOURCES_JSON`、`get_tool_registry_provider_source_name_from_settings()` 与 `build_tool_registry_provider_sources_from_settings()`，让默认 configured provider 能从 settings 选择命名基础 registry source，再叠加 profile / disable / override / extra alias 组合链。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 130 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，provider source adapters）：继续先补 failing tests，再把 `build_tool_registry_provider_sources_from_settings()` 从“source -> extra tools”升级成真正的 adapter 形态：命名 source 现在可声明 `provider/profile/disabled_tool_names/overrides/extra_tools`，并作为默认 configured provider 的基础 registry source。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 133 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，named providers + loader adapters）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_PROVIDERS_JSON`、`build_tool_registry_providers_from_settings()`、`resolve_named_tool_registry_loader()` 与 `build_tool_registry_provider_adapter()`，把 provider/source 组合链从“只能引用 default 或前置 source”推进成“可复用命名 provider + loader-backed adapter”形态。命名 source 现在既能引用命名 provider，也能直接声明 `loader/profile/disabled_tool_names/overrides/extra_tools`。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 137 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，provider factories）：继续先补 failing tests，再新增 `resolve_named_tool_registry_provider_factory()` 与 `build_profile_tool_registry_provider()`，把 provider/source adapter 再往前推进成代码级 `provider_factory` 入口。命名 provider 与命名 source 现在都能直接声明 `provider_factory`，复用内建 profile provider，并允许在其基础上继续 `enabled=true` 重开 tool、叠加 overrides 与 extra tools。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 141 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，named loaders）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_LOADERS_JSON`、`build_tool_registry_loaders_from_settings()` 与 `build_tool_registry_loader_adapter()`，让 settings-backed named loader 也成为一等入口。命名 provider 与命名 source 现在都能引用配置里的 loader 名，而不再只认内建 `default` loader。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 145 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，loader factories）：继续先补 failing tests，再新增 `resolve_named_tool_registry_loader_factory()` 与 `build_profile_tool_registry_loader()`，把 loader 这条链也补成代码级 `loader_factory` 入口。现在 named loader 可以直接声明 `loader_factory`，而且通过该 loader 构建的 named provider / source 也会保留内建 profile 的禁用集合，并允许继续 `enabled=true` 重开 tool、叠加 overrides 与 extra tools。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 149 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，settings-backed factory aliases）：继续先补 failing tests，再新增 `INSIGHT_AGENT_TOOL_REGISTRY_LOADER_FACTORIES_JSON`、`INSIGHT_AGENT_TOOL_REGISTRY_PROVIDER_FACTORIES_JSON`、`build_tool_registry_loader_factories_from_settings()` 与 `build_tool_registry_provider_factories_from_settings()`，把 `loader_factory` / `provider_factory` 也推进成 settings-backed 的命名别名入口。命名 loader / provider 现在既能直接引用内建 factory，也能引用配置里的 factory alias，并保留 profile 语义下传。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 155 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，cycle-safe file manifests）：继续先补 failing tests，再为 `build_tool_registry_from_file()` 增加 `_visited_files / _visited_dirs / _visited_sources` 防护，让 `registry_sources / registry_files / registry_dirs` 三条装配链在遇到自循环、重复文件、重复目录和重复 source 时统一“安全忽略已访问项”，不再递归炸栈。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 176 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，file-registry diagnostics artifacts）：继续先补 failing tests，再新增 `build_tool_registry_from_file_artifacts()`，把 file-backed registry 装配过程统一收口为 `registry + diagnostics`。当前已覆盖 `skipped/missing registry_sources`、`registry_files`、`registry_dirs` 三类观测结果，便于后续 trace / audit 接线复用；`build_tool_registry_from_file()` 继续只返回 registry，因此外部 SSE/trace/e2e 契约保持不变。focused 回归脚本已扩展到 178 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，diagnostics artifact chain）：继续先补 failing tests，再把 diagnostics seam 从底层 file helper 上提到高层 loader/provider/settings source 路径：新增 `build_tool_registry_loader_from_file_artifacts()`、`build_tool_registry_provider_from_file_artifacts()`、`build_tool_registry_loaders_from_settings_artifacts()`、`build_tool_registry_providers_from_settings_artifacts()`、`build_tool_registry_provider_sources_from_settings_artifacts()` 与 `get_configured_tool_registry_provider_artifacts()`。现有无 artifacts 入口继续保留原返回形状，但内部已委托新的 artifacts builders，因此外部 SSE/trace/e2e 契约保持不变。focused 回归脚本已扩展到 183 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，diagnostics runtime artifacts chain）：继续先补 failing tests，再新增 `build_tool_registry_diagnostics_summary()`、`build_tool_registry_diagnostics_runtime_artifacts()` 与 `build_configured_tool_registry_provider_runtime_artifacts()`，把 diagnostics 从“高层 artifacts 可取到”推进到“runtime 可直接消费的 summary / trace-candidate / audit-candidate”；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:271) 也已切到通过这个更高层入口拿 `provider`，但当前仍不额外对外发 SSE/trace 事件，因此外部契约保持不变。focused 回归脚本已扩展到 187 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，diagnostics audit wiring）：继续先补 failing tests，再新增 `build_tool_registry_diagnostics_audit_event()`，把 `diagnostics_runtime.audit_detail` 收口成内部 audit event 候选物；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:129) 同时抽出通用 `record_audit_event` 路径，并在 provider 预构建阶段把 `tool_registry_diagnostics` 写入内部 audit，但不额外对外发 SSE/trace 事件，因此外部契约保持不变。focused 回归脚本已扩展到 189 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，diagnostics audit action chain）：继续先补 failing tests，再新增 `build_tool_registry_diagnostics_audit_service_action()`、`build_configured_tool_registry_provider_runtime_service_actions()` 与 `execute_configured_tool_registry_provider_runtime_service_actions()`，把 `audit_event -> service_action -> apply` 这条内部 side-effect 链也收口到 runtime；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:274) 现在只负责调用这条链，不再手工拆 `audit_event` 字段。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 192 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-18，diagnostics internal trace action chain）：继续先补 failing tests，再新增 `build_tool_registry_diagnostics_trace_service_action()`，并扩展 `build_configured_tool_registry_provider_runtime_service_actions()` 与 `execute_configured_tool_registry_provider_runtime_service_actions()`，把 provider 预构建阶段的 diagnostics `trace_step/trace_event` 也收口成内部 `internal_trace_write -> persist` 动作链；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:288) 现在通过 runtime 动作链同时处理 internal trace 与 audit，但仍不额外对外发 SSE 事件，因此外部 SSE/trace/e2e 契约保持不变。focused 回归脚本已扩展到 193 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，provider preflight service execution chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_service_execution()` 与 `execute_configured_tool_registry_provider_service_execution()`，把 provider 预构建阶段的 `runtime_artifacts -> service_actions -> apply` 三段式收口成更高层的 `service_execution` 入口；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:288) 现在直接消费这层入口拿 `provider`，进一步减少了 service 侧手工胶水。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 195 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，provider preflight single-entry chain）：继续先补 failing tests，再新增 `execute_configured_tool_registry_provider_preflight()`，把 provider 预构建阶段进一步压成“单入口执行并返回 provider/result”的 helper；[chat_execution_service.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/chat_execution_service.py:288) 现在直接消费这一层单入口，不再手工串联 build/execute 两步。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 196 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，provider preflight structured result chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_preflight_result()` 与 `build_configured_tool_registry_provider_preflight_summary()`，把 provider preflight 的返回结果进一步结构化成统一 `result + summary` 形状；`execute_configured_tool_registry_provider_preflight()` 现已通过这组 helper 产出结构化结果，便于后续复用或观测扩展。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 198 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，provider preflight enriched summary chain）：继续先补 failing tests，再扩展 `build_configured_tool_registry_provider_preflight_summary()`，让 provider preflight 的 summary 不只包含 source 和 side-effect 计数，还统一携带 `tool_count`、`tool_names`、`service_action_count`、`service_action_kinds`、`diagnostics_total`、`skipped_total`、`missing_total` 等结构化指标；`execute_configured_tool_registry_provider_preflight()` 现已自动产出这组更实用的内部摘要，便于后续非 chat 复用和观测扩展。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已保持 198 条兼容测试全绿，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，provider preflight typed model chain）：继续先补 failing tests，再新增 `ConfiguredToolRegistryProviderPreflightSummaryModel`、`ConfiguredToolRegistryProviderPreflightResultModel` 以及对应的 `build_configured_tool_registry_provider_preflight_summary_model()` / `build_configured_tool_registry_provider_preflight_result_model()`，把 preflight 这层内部承载真正 typed 化，同时保留现有 dict outward 形状不变。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 200 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，diagnostics typed model chain）：继续先补 failing tests，再新增 `ToolRegistryDiagnosticsSummaryModel`、`ToolRegistryDiagnosticsRuntimeArtifactsModel` 以及对应的 `build_tool_registry_diagnostics_summary_model()` / `build_tool_registry_diagnostics_runtime_artifacts_model()`，把 diagnostics summary/runtime 这侧也补成 typed internal + dict outward 的一致分层。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 202 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，configured runtime artifacts typed model chain）：继续先补 failing tests，再新增 `ConfiguredToolRegistryProviderRuntimeArtifactsModel` 与 `build_configured_tool_registry_provider_runtime_artifacts_model()`，把 configured provider runtime artifacts 这层也补成 typed internal + dict outward 的一致分层，并让它直接持有 typed diagnostics runtime model。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 203 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，service execution typed model chain）：继续先补 failing tests，再新增 `ConfiguredToolRegistryProviderServiceExecutionModel`、`ConfiguredToolRegistryProviderServiceExecutionResultModel` 以及对应的 `build_configured_tool_registry_provider_service_execution_model()` / `build_configured_tool_registry_provider_service_execution_result_model()`，把 configured provider 的 service execution 这层也补成 typed internal + dict outward 的一致分层，并为旧 shape 的 `runtime_artifacts` 缺省值保留兼容兜底。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 205 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，runtime service actions typed model chain）：继续先补 failing tests，再新增 `ConfiguredToolRegistryProviderRuntimeServiceActionModel`、`ConfiguredToolRegistryProviderRuntimeServiceActionsModel`、`ConfiguredToolRegistryProviderRuntimeServiceActionsResultModel`，并补齐 `build_tool_registry_diagnostics_trace_service_action_model()`、`build_tool_registry_diagnostics_audit_service_action_model()`、`build_configured_tool_registry_provider_runtime_service_actions_model()`、`build_configured_tool_registry_provider_runtime_service_actions_result_model()`，把 provider preflight 的 trace/audit service actions 这层也补成 typed internal + dict outward 的一致分层，同时清理了 README 中重复的 service execution 记录。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 209 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，typed preflight result chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_preflight_summary_model_from_result_model()`、`build_configured_tool_registry_provider_preflight_result_model_from_models()` 与 `execute_configured_tool_registry_provider_preflight_model()`，把 `service_execution_result -> preflight_result` 这段内部链路也改成 typed model 直连，同时为旧的最小 `runtime_artifacts` shape 保留兼容合并。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 211 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，typed service_execution execution chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_service_execution_result_model_from_models()` 与 `execute_configured_tool_registry_provider_service_execution_model()`，把 `runtime_service_actions_result -> service_execution_result` 这段内部链路也改成 typed model 直连，并让 dict outward 的 `execute_configured_tool_registry_provider_service_execution()` 复用这条新链。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 213 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，typed runtime_service_actions execution chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_runtime_service_action_model_from_dict()`、`build_configured_tool_registry_provider_runtime_service_actions_model_from_dicts()` 与 `execute_configured_tool_registry_provider_runtime_service_actions_model()`，把最底层 `runtime_service_actions` 执行器输入也改成 typed model 直连，并让 dict outward 的 `execute_configured_tool_registry_provider_runtime_service_actions()` / `execute_configured_tool_registry_provider_runtime_service_actions_result_model()` 退化为薄包装。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 215 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，shared hydration helper chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_runtime_artifacts_model_from_dict()` 与 `build_configured_tool_registry_provider_service_execution_model_from_dict()`，把 `runtime_artifacts/service_execution` 的共享 hydration 收口成统一 helper，并替换 `service_execution_result`、`preflight_result` 与 dict outward `execute` 路径中的重复手工组装。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 217 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，preflight_result shared bridge chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_preflight_result_model_from_dict()`，并让 `build_configured_tool_registry_provider_preflight_summary_model()` 与 `build_configured_tool_registry_provider_preflight_result_model()` 统一复用这层共享 bridge；同时补上缺省 `provider` 的兼容回退，保持旧最小 `preflight_result` shape 可用。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 218 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-19，typed preflight summary builder chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_preflight_summary_model_from_models()`，并让 `build_configured_tool_registry_provider_preflight_result_model_from_models()` 直接走真实 typed summary builder，不再先构造 placeholder summary 再覆盖。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本维持 218 条兼容测试全绿，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 后端整组续推（2026-05-20，shared preflight summary builder chain）：继续先补 failing tests，再新增 `build_configured_tool_registry_provider_preflight_summary_model_from_parts()`，并让 `build_configured_tool_registry_provider_preflight_summary_model_from_result_model()` 与 `build_configured_tool_registry_provider_preflight_summary_model_from_models()` 统一复用这层共享 builder，进一步收掉 preflight summary 的重复逻辑。外部 SSE/trace/e2e 契约保持不变，focused 回归脚本已扩展到 219 条兼容测试，最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - `tool-runtime-productionization` 文档终稿同步（2026-05-14，design checkpoint）：已把 [design 文档](</Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-design.md>) 更新为当前真实架构说明，并将 [handoff 文档](</Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md>) 的建议改为“当前 helper 分层已到达阶段性合理停止点，除非新需求触发再继续抽象”。当前最新基线已推进到 focused 回归 `192` 条、`compileall` 通过、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - 新会话交接文档（2026-05-13）：已补充 [docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md)，汇总当前 `tool-runtime-productionization` 的真实状态、已验证基线、CI 风险教训、关键文件入口，以及下一步建议继续收口“单个 tool plan item retry loop 整体 helper”的开发方向，供后续会话直接接手
  - `tool-runtime-productionization` 收尾归档（2026-06-04）：runtime 基础设施已完成收口，残余风险审计问题已修复，focused 基线稳定在 `305/305`；[design 文档](</Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-design.md>) 与 [handoff 文档](</Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/docs/superpowers/specs/2026-05-13-tool-runtime-productionization-handoff.md>) 现作为 archived 历史记录保留，不再高频维护，后续默认进入“默认工具去 mock 化 / 真实工具接入”阶段
  - 默认 planner 去 mock 暴露（2026-06-04）：在不打碎现有 registry/配置兼容层的前提下，后端新增 `build_tool_plan_summary()` 与 `get_tool_display_name()` 展示链，`chat_execution_service.py` 的 planning trace 改为输出 `Task Planner`，`mock_plan` 的 tool 输出也升级为结构化 steps；前端流式 trace store 与 Inspector subtitle 现优先展示 `meta.tool.label` / `display_name`。本轮 focused 回归 `308/308`、`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 planner 内部名去 mock 化（2026-06-04）：后端本轮继续把默认 plan 入口从 `mock_plan` 切到 `task_plan`，并新增 alias 解析，让 `task_plan` 在运行时复用现有 `mock_plan` 注册项与配置生态；因此默认主链与页面展示都不再依赖 mock 内部名，而旧的 `planning_only` profile、`disabled_tool_names=["mock_plan"]`、`template="mock_plan"` 等设置仍然保持兼容。当前 focused 回归已提升到 `309/309`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 retrieval 内部名去 mock 化（2026-06-04）：后端本轮继续把默认检索入口从 `mock_retrieve` 切到 `task_retrieve`，并新增 alias 解析，让 `task_retrieve` 在运行时复用现有 `mock_retrieve` 注册项与 query 行为；同时默认 planning summary 与默认 tool 展示链开始使用 `Knowledge Retrieval` 作为展示名。这样默认主链进一步摆脱 `mock_*` 内部名，而旧的 `disabled_tool_names=["mock_retrieve"]`、`template="mock_retrieve"` 等配置保持兼容。当前 focused 回归已提升到 `311/311`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 registry canonical 收口（2026-06-04）：后端本轮继续把默认 registry / profile / provider / loader / file-manifest 主链统一切到 `task_plan / task_retrieve`，并新增统一的 tool-name normalize helper，让旧的 `mock_plan / mock_retrieve` 只作为兼容别名参与 `disabled_tool_names`、`template`、`overrides` 与旧 trace 路径解析。这样“默认工具去 mock 化”不再只停留在展示层或 plan 入口，而是推进到了默认注册表与配置主链。当前 focused 回归保持 `311/311`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 runtime outward canonical 收口（2026-06-04）：后端本轮继续把兼容 alias 的 outward 输出统一到 canonical tool name。现在即使旧配置仍传 `mock_plan / mock_retrieve`，`build_tool_runtime_context()`、tool start payload、action step meta、success/error meta 与 observation 主链都会优先落成 `task_plan / task_retrieve`；兼容 alias 只负责 lookup/执行，不再继续把 legacy 名字泄漏到默认 trace/meta。当前 focused 回归保持 `311/311`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 internal mock semantics 收口（2026-06-04）：后端本轮继续把 planner / retrieval 的内部语义命名也从 mock 收口到真实任务语义：`ToolRegistration.kind` 已改成 `task_planner / knowledge_retrieval`，runner helper 同步改成 task 命名，`Unknown mock tool` 错误文案也统一改成 `Unknown tool`。这样兼容 alias 仍继续工作，但默认运行时内部语义已不再把这两条主链标注成 mock。当前 focused 回归保持 `311/311`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - 默认 generic runtime markers 收口（2026-06-04）：后端本轮继续把开发期 marker 从 mock 语义切到 generic 语义，新增 `maybe_raise_tool_execution_error()` 并让 `run_tool()` 默认走这条主链；`[tool-error] / [tool-fatal] / [multi-tool]` 现已成为默认测试/调试标记，旧 `[mock-tool-error] / [mock-tool-fatal] / [mock-multi-tool]` 仍通过兼容桥继续可用。focused 回归基线已提升到 `313/313`，`compileall`、`bash scripts/test_ci_e2e_tooling.sh common` 与前端 `npm run lint` 通过
  - provider-assisted planner 首版（2026-06-04）：后端本轮开始真正接入非 mock planner 能力，在 `chat_execution_service.py` 中让 `build_tool_plan(prompt, provider=provider)` 优先尝试远端 provider 生成受限 JSON tool plan，再把结果约束回当前 `task_plan / task_retrieve / calc_eval` schema；若返回无效 JSON、未知工具、非法计算表达式或直接抛错，则静默回退到既有规则 planner，因此页面、SSE、trace 与工具执行主链形状保持不变。focused 回归基线已提升到 `315/315`，`compileall` 与 `bash scripts/test_ci_e2e_tooling.sh common` 通过
  - planning usage / overall usage 对齐（2026-06-04）：后端本轮继续把 provider-assisted planner 的调用纳入 usage/cost 闭环：`build_tool_plan_artifacts()` 现会带出 planning provider usage 与 attempt/used 状态，`chat_execution_service.py` 则把 planning 阶段 token/cost 写入 planning trace meta，并在 `done.usage` 中追加 `planning_prompt_tokens/planning_completion_tokens/planning_total_tokens/planning_cost_estimate/planning_usage_source` 与 `overall_prompt_tokens/overall_completion_tokens/overall_total_tokens/overall_cost_estimate`。原有 `done.usage.prompt_tokens/completion_tokens/cost_estimate` 仍保持最终回答语义不变。focused 回归基线已提升到 `318/318`，`compileall` 与 `bash scripts/test_ci_e2e_tooling.sh common` 通过
  - planning usage 前端接线收尾（2026-06-04）：Workbench 现已把流式 `sseTaskUsage` 与当前会话 `recentTasksScoped` 真正接入 Inspector，上下文面板会同时展示“当前任务用量”“规划阶段用量”“整体总计”与会话聚合用量，不再停留在仅后端产出 `planning_* / overall_*` 字段但前端未消费的半完成状态。校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` `318/318`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common`、`cd frontend && npm run lint` 通过
  - retrieval 可见文案收口（2026-06-04）：后端本轮继续把默认真实检索链里最后的 `mock` 可见语义清掉：`build_tool_rag_step()` 生成的 follow-up thought 现已改成 `Knowledge Retrieval returned snippets from the selected knowledge base.`，并新增 focused tests 锁定 `mock_retrieve` 兼容入口在 success step、observation 与 rag follow-up 三条 outward 主链上都统一显示 `Knowledge Retrieval`。focused 回归基线已提升到 `319/319`，`compileall` 与 `bash scripts/test_ci_e2e_tooling.sh common` 通过
  - planning trace 可观测性前端收口（2026-06-04）：前端本轮继续把后端已落盘的 planning trace meta 真正展示出来：`formatTraceStepMetaSubtitle()` 现会额外显示 `Prompt / Completion Token`、`Usage 来源`，并在 planning 步骤上明确标出“Planner provider used / Planner fallback”。这一增强同时覆盖 Workbench Inspector 与任务详情页 trace 时间线，因此现在可以直接从 UI 判断本次规划是否真的走了 provider 以及其 token 来源。校验：`cd frontend && npm run lint`、`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` `319/319`、`bash scripts/test_ci_e2e_tooling.sh common` 通过
  - planner registry-aware 治理首步（2026-06-04）：后端本轮继续把规划主链真正收口到 enabled registry。现在 `build_tool_plan()` 与 `build_tool_plan_artifacts()` 不再硬编码总是允许 `task_plan / task_retrieve / calc_eval`，而是会根据传入的 `registry_provider` 动态裁剪 rule-based planner 与 provider-assisted planner 的允许工具集合；`chat_execution_service.py` 也会在 planning 前先读取 configured provider，再把这套真实 enabled 工具集传给 planner。这样当 registry/profile 禁用 retrieval、calc 或只保留 planning-only 时，planner 不会再计划被关闭的工具。focused 回归基线已提升到 `322/322`，`compileall` 与 `bash scripts/test_ci_e2e_tooling.sh common` 通过
  - CI fixture 稳定性修复（2026-05-11）：`scripts/test_ci_finalize_e2e_for_workflow.sh` 的“缺失事件上下文应失败”断言改为显式 `unset GITHUB_EVENT_NAME/GITHUB_REF` 后执行，避免在 GitHub Actions 默认环境变量存在时出现假阳性（expected fail but passed）
  - CI 告警增强（2026-05-08）：`frontend-e2e` 导出摘要新增阈值告警（`threshold alerts`），当 `main-path` 或 `edge-cases` 的关键计数低于预期时会直接输出 `expected vs actual`，便于第一时间判断是下载层、响应头层还是 404 语义层回归
  - 回归稳态收口（2026-04-22）：修复 `workbench-edge-cases` 在并发回归下的偶发抖动（Context tab 定位收敛到 Inspector 顶部导航，取消后同文案重发场景改为“API cancel + UI 去重可见性断言”）；最新本地全量回归再次验证为 chromium `30/30`。
  - 回归稳态补充（2026-04-22）：修复 `workbench-edge-cases` “切换 token 后导出 404 提示”用例竞态（切 token 前先等待任务详情导出按钮可用）；本地回归复测：chromium 串行（`--workers=1`，与 CI 对齐）`30/30` 通过。
  - 鉴权接口：`POST /api/auth/register`、`POST /api/auth/login`、`GET /api/auth/me`
  - `full-data-auth` 增量（2026-05-09）：用户模型新增 `role`（`admin/user`），首个注册用户自动授予 `admin`，并新增 admin-only 用户列表接口 `GET /api/auth/users`
  - 会话鉴权治理（最小版）：`POST /api/auth/refresh`、`POST /api/auth/logout`、`POST /api/auth/logout-all`、`GET /api/auth/sessions`、`DELETE /api/auth/sessions/{session_id}`
  - 审计扩展已落地：记录 `login/logout/refresh/settings_update/settings_validate/task_create/task_cancel/task_timeout/task_failed/rag_ingest/rag_kb_clear/rag_kb_delete` 到 `audit_logs`
  - 审计查询接口：`GET /api/audit/logs`（支持 `event_type/session_id/task_id/start_at/end_at` 与分页）
  - 除 `GET /health` 与 `/api/auth/*` 外，其余业务 API 均要求 `Authorization: Bearer <token>`
  - `settings/validate` 远端预检支持 `HEAD` 失败自动回退 `GET`，降低网关不支持 HEAD 导致的误判
  - `settings/validate` 预检请求会携带 `Authorization`；当远端返回 `401/403` 时，明确返回 `remote_api_key_unauthorized`，避免误报为网络错误
  - `GET /api/sessions/{session_id}/usage/summary` 兼容保留（会话聚合）
  - Memory：`GET .../memory/status`、`POST .../memory/add`、`POST .../memory/query`
  - 删除会话时会 best-effort 清理 `memory_{session_id}` collection，降低孤儿向量残留
  - `/health` 含 Chroma 可达性
- frontend：
  - `rag-rbac-lite` 前端收口（2026-05-09）：知识库治理弹窗已接入 `currentUser.role`，普通用户对 `shared-*` 知识库的清空/删除按钮禁用，并提示权限受限；与后端共享库写权限规则一致
  - Ant Design 6 兼容性修复（2026-05-08）：`Workbench` 任务中心抽屉已将 `Drawer width` 替换为 `size`，消除控制台废弃警告（`[antd: Drawer] width is deprecated`）
  - `full-trace-session` 收口补充（2026-05-08）：任务详情页与会话导出入口的下载逻辑已抽取为共享工具（`frontend/lib/export-download.ts`），统一 `content-disposition` 文件名解析、鉴权下载与 `ApiError` 语义，降低后续导出链路维护分叉风险
  - 新增 Auth Gate：登录/注册入口页、token 本地持久化、401 过期自动回登录
  - Auth 页面可读性收口：移除面试导向文案、白底区域表单与 Tab 对比度提升、退出按钮垂直居中优化
  - Auth 交互细化：登录区标题文案改为“账号登录”；提交按钮禁用态提升对比度；退出入口迁移至左下角设置模块并避免与折叠控件冲突
  - Auth 交互细化（继续）：移除“当前用户：未登录”提示；注册邮箱格式前置校验并给出友好错误；登录表单 autofill/已填充态样式优化，浅色背景下可读性更稳定
  - 登录页品牌文案收口：左侧标题与说明融合主页主叙事（可观测智能体、对话到任务闭环），并补回 SSE/Trace、Memory/RAG、Token/Cost 关键信息点
  - 登录交互修复：密码框显隐图标恢复可见并支持显示/隐藏切换
  - 登录页国际化：Auth Gate 新增 `auth` 文案分组并接入中英文词条，覆盖登录页标题/说明/表单标签/占位符/按钮与鉴权反馈提示
  - 登录页设置增强：新增轻量设置模块（语言/主题/主题色）并接入 `Preferences`；登录页视觉样式改为跟随深浅主题与主色实时切换
  - 账号切换收口：退出/401/重新登录时清空 Query 缓存与流式轨迹状态，避免跨账号残留
  - 登录态增强：本地持久化 `refresh_token`，401 时自动调用 `/api/auth/refresh` 轮换 token 并重试请求；刷新失败再回登录
  - 侧栏账户展示收口：当前登录用户已融合到左下角“设置”弹窗顶部，并采用与“主题/主题色/语言”一致的行样式（图标 + 标题 + 值）
  - 首次引导页已移除：注册/登录后直接进入工作台
  - 新增运行态提示条：显示当前 `mock/remote` 状态，并可一键打开模型设置
  - 发送前校验补强：`remote` 且未配置 `api_key` 时前端阻断发送并引导去设置
  - 审计入口迁移到左下角设置菜单（方案 A）：独立审计子页支持事件类型/时间范围/`session_id`/`task_id` 过滤，详情展开，以及 JSON/CSV 导出（可选“当前页/全部筛选结果”）
  - 设置菜单新增“知识库治理”入口：在设置弹窗内查看当前账号知识库列表、来源采样与文档条数，并支持清空/删除
  - 设置菜单新增“用量统计”入口：在设置弹窗内查看用量仪表盘，支持全局/当前会话切换、趋势查看、会话榜与任务榜
  - 知识库治理交互优化：列表表头统一左对齐（末列明确“操作”）、刷新改为图标按钮、清空/删除按钮风格统一；来源标签支持悬浮查看完整值并补充“采样统计”说明
  - 知识库治理信息增强：新增“样本片段”预览列（悬浮查看完整文本），避免只看到来源标签而无法判断真实内容
  - 审计页可读性优化：主视图改为分页表格展示，筛选区升级为双行下拉+输入框检索并统一控件尺寸，事件类型标签化；总条数下置表格左下角，操作区右对齐，并恢复行展开明细（展开内不再二次展开，主表保留会话/任务 ID，分页默认每页 10 条）
  - 弹窗状态收口：审计日志弹窗与模型设置弹窗在每次打开时都会重置筛选/分页/提示状态，避免沿用上次操作痕迹
  - 模型设置弹窗交互调整：打开时按后端配置回显当前 `mode/provider/model/base_url`，并显式关闭浏览器自动填充（避免凭证误填）
  - 修复模型设置回显异常：解决弹窗打开时被默认值覆盖导致显示 `mock` 的问题
  - 模型设置弹窗优化：底部元信息新增 `base_url` 展示；`remote` 模式新增“仅支持 OpenAI-compatible 接口”提示
  - 模型设置弹窗模式收口：切换 `mock/remote` 时表单区与下方元信息区实时联动；`mock` 固定回显 `provider=mock`、`model=mock-gpt` 并隐藏 `base_url/api_key`
  - 模型设置表单校验收口：`remote` 模式下 `provider/model/base_url` 必填；`api_key` 在“首次 remote 配置”时必填，后续留空将沿用已存密钥
  - 模型设置提示兼容性修复：弹窗结果提示改为 `App.useApp().message` 上下文调用，避免 Ant Design 主题上下文警告
  - 模型设置交互补充：`基础地址/数据库` 在元信息区改为普通文本展示；切换到 `mock` 并保存时会清空远端配置（含 `api_key/base_url`）
  - 模型设置回显修复：当已保存为 `remote` 配置时，切换回 `remote` 会正确回显后端已存的 `provider/model/base_url`
  - 聊天体验修复：发送后立即显示用户临时消息；assistant 流式消息仅在生成中/失败态显示，避免与已落库历史回复重复
  - `remote-provider-hardening` 前端收口：流式错误与设置校验结果已按 `error_code` 做本地化映射，并在提示中保留错误码，降低真实厂商联调排障成本
  - `task-cancel-timeout` 前端首版：Inspector「当前任务」支持取消运行中任务，流式状态支持 `cancelled/timeout` 事件与提示
  - `running-task-recovery` 前端首版：持久化当前会话，在刷新页面或切回会话时自动探测并接管该会话下 `pending/running` 任务流（复用后端 running reconnect + delta）
  - `running-task-recovery` 可视化补充：聊天区新增自动恢复提示条，展示恢复中/恢复成功/恢复失败状态（自动消退，可手动关闭）
  - 会话切换串台修复：流式状态新增 `session_id` 绑定并按当前会话隔离展示；切换会话时不再短暂显示其他会话任务
  - 恢复误报修复：任务实际完成后若任务列表短暂滞后为 `running`，自动恢复不会再误报“任务流恢复失败”（同任务去重恢复 + 409 视为无害收敛）
  - 聊天滚动修复：流式输出时若用户主动上滑阅读历史消息，不再被强制拉回底部；仅在贴底状态自动跟随新输出
  - 取消态提示修复：任务取消/超时后即使后端追加 `error(task_cancelled/task_timeout)`，前端也不再误判为失败态，不再出现“重试上次发送”
  - 结束补拉提示修复：流结束后的 `trace/delta` 自动补拉改为静默，不再把底部状态覆盖成“暂无新的轨迹增量”
  - 同文案重发修复：待发送用户消息去重改为按 `task_id` 判定，取消后再次发送相同内容也会立即显示，不再被上一条同文案误去重
  - 取消按钮即时可用修复：发送后会立即刷新 `tasks/messages`，并在任务列表尚未回刷时用流式任务兜底展示“当前任务”与取消按钮
  - 取消后卡住修复：取消任务成功后前端会本地中断活动 SSE 流并立即退出“生成中”，发送按钮无需长时间等待即可恢复可用
  - 远端取消防抖：`remote` 模式取消后增加短暂发送冷却，避免“刚取消立刻重发”触发上游限流
  - 滚动交互升级：消息区“回到底部”按钮锚定在聊天区右下角（非滚动内容内）；上滑阅读时若有新输出会显示动态徽标与轻脉冲提示，点击可快速回到底部
  - 回底按钮避让微调：按钮位置进一步右移并上移，减少对底部消息与输入区域的遮挡
  - 会话命名收口：空会话在首条消息发送时自动以消息前缀命名（仅占位标题且无历史消息时触发）
  - 三栏工作台（会话 / 消息 / 轨迹与上下文）
  - 右侧 Inspector（Context）完成运行态信息架构收口：概览 KPI + 同步诊断 + 当前任务 + 最近任务分区，弱化堆叠卡片并保留扩展位
  - `full-trace-session` 首步收口：新增任务详情独立页 `GET /tasks/{task_id}`（前端路由 `/tasks/[taskId]`），集中展示任务快照、Trace 时间线/流程图回放与任务导出
  - `full-trace-session` 重排收口：主工作台恢复为中栏聊天主视图；任务中心改为右侧抽屉承载任务索引筛选/排序/搜索，避免替换聊天上下文；任务级分析统一下沉到 `/tasks/[taskId]`
  - `full-trace-session` 清理收口：右侧 Inspector 旧任务块已从代码中物理删除（不再以样式隐藏保留），主入口以“任务中心 + 任务详情页”作为唯一任务分析路径
  - `full-trace-session` 交互细化：运行调试弹窗视觉样式与主站统一；任务中心“任务详情”按钮提升可见性；右侧“当前任务”卡片仅保留取消动作
  - `full-trace-session` 交互细化补充：任务中心头部已移除模式/模型信息，保留单一“关闭”操作并固定到右侧，降低头部视觉噪音
  - `full-trace-session` 样式微调：运行调试弹窗改为上下单列并移除分区高亮底色，保持与主界面统一底色体系
  - `full-trace-session` 回归对齐：前端 Playwright 用例已切换到“聊天头部任务中心入口 -> 任务中心抽屉 -> 新标签任务详情导出”主链路，不再依赖已移除的 Inspector 任务导出选择器
  - `trace-export-json-md` 首版已落地：支持单任务导出 JSON 与 Markdown（含任务元信息、task-linked 消息、TraceStep、RAG chunks 与 usage），并在任务快照区提供一键导出入口
  - `session-export-lite` 首版已落地：支持当前会话导出 JSON 与 Markdown（会话消息、任务摘要、Trace 预览、RAG 命中统计、会话级 usage 汇总），入口已统一迁移到左侧会话行“...”菜单（方案 1，按会话行触发）
  - 任务索引支持前端本地筛选与排序（状态筛选、时间顺序、失败任务优先），便于运行态排障
  - 任务中心样式收口：任务列表改为审计日志同款表格风格（筛选/搜索工具栏 + 表格 + 分页）；顶部筛选区采用审计日志同款双行布局与控件尺寸体系，并保留任务详情快捷入口
  - 任务中心细化修复：全局任务范围改为自动拉取完整分页后再本地筛选，避免出现“全局条数少于当前会话”的首屏截断错觉；同时移除“用量来源”列并下调“任务详情”按钮为轻量操作样式
  - 任务索引支持关键词快速定位（标题/ID）与失败摘要提示，便于快速定位异常任务
  - Trace 面板支持步骤类型筛选、关键词检索与类型计数（时间线/流程图一致生效），提升长轨迹排障效率
  - 右侧面板完成一体化收口优化：Trace 密度切换、Context 快速跳转、状态徽标与分区说明统一，兼顾当前能力与后续扩展
  - 左侧与中栏完成统一风格优化：会话导航强化激活层级、聊天头部运行态信息条收敛、消息与输入区交互节奏统一
  - 根据最新交互收敛要求，已移除会话状态胶囊与输入计数提示，头部恢复紧凑模式标签展示
  - 轨迹支持「时间线 / 流程图」双视图（thought/action/observation/tool/rag 区分）
  - 会话支持分页加载、重命名、删除
  - 设置弹窗新增“运行调试”子页：承接 Memory 状态/add/query 与 RAG 状态/ingest/query
  - RAG 知识库 ID 改为“输入后应用”模式，避免输入过程频繁触发状态请求
  - 设置弹窗新增知识库治理子页：按账号列出知识库、显示来源采样，并支持行级清空/删除
  - Trace 元信息支持展示每步 cost（`meta.cost_estimate`）
  - usage 展示增强：支持全局/会话自动切换汇总，含加载/错误/空状态与覆盖率（with_usage/total）
  - usage 来源可视化：当前任务与任务中心/任务详情已展示 usage 来源（provider/estimated）
  - 用量统计弹窗补充来源分布：展示 provider/estimated/mixed/legacy 四类任务数，并对历史无来源字段任务标记为“旧数据”
  - 用量统计弹窗支持来源筛选（全部/官方/估算/混合/旧数据），便于按来源排查成本与 token 分布
  - `trace/delta` 支持流式期间自动轮询同步（静默）+ 失败退避重试，并在流结束后自动补拉一次
  - 任务 SSE 支持运行中重连：`GET /api/tasks/{task_id}/stream` 在 `running` 状态下可重连并回补增量
  - Inspector 上下文摘要新增 delta 同步健康度（状态/重试次数/最近成功时间/下次重试/最近错误/恢复提示），恢复提示为短时展示并自动消退；重试中展示秒级倒计时
  - 页面处于后台（不可见）时暂停 delta 自动同步，回到前台自动恢复
  - Inspector 在 delta 连续失败时提供轻量告警提示（不阻塞主链路）
  - 前端 ESLint 基线已落地，`npm run lint` 可直接执行且当前告警已清零
  - W3 增量：mock 工具链路支持可复现错误语义（`[mock-tool-error]` 可恢复重试、`[mock-tool-fatal]` 致命失败），`tool_end` / `error` / `trace.meta.tool` 已携带 `retry_count/error`
  - W3 优化：新增本地计算器工具（`[calc:1+2*3]` 或“计算 1+2*3”），统一走 `tool_start/tool_end/trace` 生命周期
  - W1 优化：设置弹窗支持“校验配置”按钮（调用 `POST /api/settings/validate`，校验通过后再保存）
  - W1 设置语义收口：`remote` 模式下 `api_key` 留空将沿用已存密钥（首次配置仍必填）；保存/校验反馈统一使用 message 浮层提示（不再使用顶部可关闭提示条）
  - W2 稳定性优化：前端合并 SSE/`trace/delta` 步骤后按 `seq` 稳定排序，降低时间线/流程图在高频增量场景下的乱序概率
  - W2 稳定性优化：`trace/delta` 增加任务隔离保护，旧任务延迟返回不再污染当前任务轨迹
  - W3 稳定性优化：前端仅在 `error.fatal=true` 时将全局 phase 置为 `error`；可重试工具错误不再误伤主任务状态
  - W1/W2 稳定性优化：SSE 流式期间改为周期 heartbeat，且后端 trace 持久化增加节流，降低连接误判与数据库写放大
  - W2 重连流优化：`running` 任务重连时 `done/error` 事件补齐 `session_id/step_id` 并标记 `resumed=true`，契约更一致
  - W2 重连轮询优化：重连流改为“有增量快轮询、无增量退避慢轮询”，降低 DB 压力与事件噪声
  - W2 后端性能优化：重连流复用当前 task 快照做 delta 计算，减少循环内重复 DB 查询
  - W1/W2 可调优优化：trace 节流与 running 重连流轮询/heartbeat 参数均支持环境变量配置
  - W1 usage 语义优化：`completion_tokens` 改为基于最终输出文本估算（覆盖流式与 fallback），不再按 chunk 数近似
  - W2 前端稳态优化：SSE 事件按 `task_id` 做活动任务隔离，避免并发/延迟事件串台
  - W2 前端收敛优化：SSE phase 统一归一（`completed→done`、`failed→error`）并集中化流式文案，减少分支判断
  - W2 前端稳态优化：当存在活动任务时，缺失 `task_id` 的非 `start` 事件将被忽略，进一步降低串台风险
  - W2 前端性能优化：`trace` 事件合并步骤时移除重复 `upsert` 计算，降低高频流式阶段状态更新开销
  - W2 前端展示优化：phase 映射补齐 `thinking/tool_running/tool_retry`，统一显示为运行态标签
  - W1/W2 前端国际化优化：流式状态与错误提示（start/delta/tool/error/close 等）改为 i18n 文案驱动，不再依赖硬编码英文
  - W2 收口优化：`trace/delta` 的 `traceCursor` 改为单调递增（避免并发请求导致游标回退），token 事件合并为单次 store 更新；空闲提示纳入 i18n 并随语言切换
  - W1/W2 国际化维护优化：store 默认流式文案改为复用 `en.stream` 单一来源，避免与 i18n 文案表双份维护导致漂移
  - W2 后端语义优化：`GET /api/tasks/{task_id}/trace/delta` 的 `lag_seq` 改为基于任务快照真实尾游标计算，避免无信息量的恒零值

## SSE 与 TraceStep 契约（当前实现）

`GET /api/tasks/{task_id}/stream` 的 `event: trace` 中 `data.step` 与 REST `TraceStep` 同构（`id/type/content/meta/seq?`）。

当前 SSE 事件类型：
- `start`
- `state`
- `trace`
- `tool_start`
- `tool_end`
- `heartbeat`
- `token`
- `done`
- `error`

对齐规则：
- SSE 按时间增量发步骤；REST `trace` 返回落库后的完整步骤数组。
- `tool_start/tool_end` 作为工具生命周期事件，与 `trace` 中的 action 步骤通过同一 `step_id` 对齐。
- 最终 `observation` 在 SSE 中先空内容，再由 `token.delta` 拼接；REST 中是完整内容。
- REST 返回步骤由后端 `TraceStep`（Pydantic）校验，前端按 `lib/types/trace.ts` 对齐消费。

## Memory / Chroma / Embedding 约定（当前实现）

- 会话级 collection：`memory_{session_id}`
- 知识库级 collection：`kb_{user_hash}_{knowledge_base_id}`（用户隔离）
- 后端通过 `chromadb.HttpClient(host=CHROMA_HOST, port=CHROMA_PORT)` 连接 Chroma Server
- 默认环境变量：
  - `CHROMA_HOST=127.0.0.1`
  - `CHROMA_PORT=8001`
  - `CHROMA_PROBE=true`
- 嵌入策略：当前未在应用层传自定义 embedding function，文本由 Chroma Server 默认策略处理
- 错误语义：
  - Chroma 不可达时，`memory/add` 与 `memory/query` 返回 503
  - Chroma 不可达时，`rag/ingest` 与 `rag/query` 返回 503
  - 任务结束后的 memory 摘要写入是 best-effort，不阻塞主任务

### 通俗理解：为什么有 RAG 还需要 Memory

- `PostgreSQL`：系统“完整账本”。保存会话、消息、任务、trace、usage 的原始记录。
- `Chroma Memory`：当前会话“便签本”（`memory_{session_id}`）。保存可语义召回的会话记忆片段（含任务完成后的自动摘要）。
- `Chroma RAG`：项目“知识库”（`kb_{user_hash}_{knowledge_base_id}`）。保存导入文档的分块内容，并按用户隔离用于知识检索增强。

三者是互补，不是重复：

- 只用 `RAG` 不够：RAG 主要是外部知识，不包含你在本次对话里刚确认的偏好和约束。
- 只用 `PostgreSQL` 不够：结构化存储适合完整留档，但直接每轮全量喂给模型成本高；Memory 用向量检索可只取最相关片段。
- 所以实际分工是：`PostgreSQL` 管“真实历史”，`Memory` 管“会话记忆”，`RAG` 管“外部知识”。

一个直观例子：

- 你说“后续都用中文、回答不超过 5 行” -> 这类临时约束更适合进 Memory。
- 你导入产品手册/API 文档 -> 这类长期资料进 RAG。
- 你完整的对话与任务执行记录 -> 始终落 PostgreSQL。

## 目录

```text
InsightAgent/
├── backend/
├── frontend/
└── data/
```

## Docker（可选，Chroma）

在仓库根目录启动：

```bash
docker compose up -d chroma
```

默认后端连接 `http://127.0.0.1:8001`。可通过 `GET /health` 检查 `chroma.reachable`。

完整本地栈（backend + frontend + chroma + postgres）可使用：

```bash
docker compose -f compose.full.yml up -d
```

如需一键启动（会自动拉起 `postgres/chroma`，再启动 backend/frontend），可直接执行仓库根目录脚本：

```bash
./start_insightagent.command
```

macOS 下也可在 Finder 中双击 `start_insightagent.command` 直接启动。
该脚本已修复 PostgreSQL 就绪检查（不再依赖 `docker compose exec`，避免 `backend/.env` 缺失导致误判未就绪），并改用 Chroma `v2` heartbeat 检测，兼容新版本镜像；启动前会额外清理旧 `uvicorn/next dev` 进程，并采用轮询方式等待服务健康。

## 后续阶段最终决策（定位 / 面试 / 单 Mac / 部署）

### 后续开发总路线（建议按优先级）

1. P0：历史任务详情与 Trace 回放、Trace/任务 JSON + Markdown 导出、会话级导出、remote provider 错误体验收口、最小 e2e 主链路。
2. P1：任务取消与超时、运行中任务恢复、RAG 知识库最小治理、usage 统计增强、审计事件补充。
3. P2：轻量 RBAC、实验性 Trace step 重跑、生产部署 Runbook、Playwright e2e（provider 官方 usage 对齐首版已完成，后续补 e2e 与前端来源展示）。

### 当前明确暂缓

1. Redis/Kafka/Celery 分布式队列（当前规模用进程内取消/超时/并发上限即可）。
2. Kubernetes、微服务拆分、服务网格（复杂度明显高于收益）。
3. 企业级 SSO/RBAC 全套（保留扩展位，先做轻量 RBAC）。
4. 复杂多模型编排面板、复杂多跳 RAG、大规模重排体系（先完成稳定性与治理闭环）。
5. 大规模 UI 重构、PDF/视频/GIF 自动导出（先用当前 UI + JSON/Markdown 导出满足演示与复盘）。

### 部署策略（结合单 Mac 开发 + 后续正式部署）

1. 本机开发：继续使用 `docker-compose.yml`（Chroma）/ `compose.full.yml`（backend + frontend + Chroma + PostgreSQL）。
2. 前端：优先部署到 Vercel 免费版（静态资源 + Next.js 站点）。
3. 后端与状态服务：独立部署 FastAPI + PostgreSQL + Chroma（不要与前端同进程耦合）。
4. 面试演示：准备“一键演示脚本”（导入知识库 -> 发起任务 -> 查看 Trace/成本）。

## 下一步（W4+）

- e2e 收口已完成：主链路、导出稳定性、取消/超时、shared 知识库权限、artifact/export guard、frontend/backend scope 对称性与 common tooling 聚合回归均已跑通；当前可把重心切回开发主线。
- 下一阶段优先转向真实工具运行时、单机队列与并发治理、知识库治理深化；CI/e2e 保持“小步补洞、随改随回归”的维护节奏即可。

### 最新同步（2026-04-23）

- 任务中心表格头部计数文案（如 `17 / 17`）已移除，分页不再显示该类汇总计数。
- 任务中心“失败置顶”开关已移除，任务顺序仅由时间排序控制（最新/最早）。
- 任务中心筛选行与表格样式完成统一化优化：更轻量的筛选区、清晰表头层级、统一操作按钮视觉。
- 任务中心表格视觉继续收口：去除偏蓝底色，恢复中性底色；“重置筛选”按钮已对齐审计日志默认样式。

### 最新同步（2026-05-20）

- `tool-runtime-productionization` 本轮继续沿 typed internal 收口：`build_configured_tool_registry_provider_preflight_result_model()` 不再依赖顶层 dict `provider_source_name/provider/runtime_artifacts`，而是先 hydration `service_execution` 与计数结果再走 `from_models()`。
- `execute_configured_tool_registry_provider_preflight_model()` 已改为直接调用 typed `execute_configured_tool_registry_provider_service_execution_model()`，移除一层 `model -> dict -> model` 往返，同时保持外部 SSE / trace / e2e 契约不变。
- focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 `220` 条测试，并新增“`preflight_result` 从 `service_execution` 继承默认 provider/provider_source_name”兼容覆盖。

### 最新同步（2026-05-21）

- `tool-runtime-productionization` 本轮继续坚持“先补 failing test 再改实现”，新增 `build_configured_tool_registry_provider_preflight_result_model_from_dict()` 的最小 dict bridge 回归，锁定当顶层只保留计数时，`provider/provider_source_name/runtime_artifacts` 仍可从 `service_execution` 继承。
- `build_configured_tool_registry_provider_preflight_result_model_from_dict()` 已改为只负责兼容归一化，并直接复用 `build_configured_tool_registry_provider_preflight_result_model()`；`ConfiguredToolRegistryProviderPreflightResult` 的 dict outward bridge 因而进一步收薄，同时保持外部 SSE / trace / e2e 契约不变。
- focused 回归脚本 `backend/scripts/test_tool_runtime_slice.py` 已扩展到 `221` 条测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续沿相邻 typed internal seam 收口：新增共享 helper `build_configured_tool_registry_provider_runtime_service_actions_result_model_from_dict()`，并让 `build_configured_tool_registry_provider_service_execution_result_model()` 与 `build_configured_tool_registry_provider_preflight_result_model()` 统一复用这层计数 hydration；同时补上 `service_execution_result` 在最小 `execution_result={}` 场景下的 `trace_write_count/audit_event_count -> 0` 兼容回退。focused 回归脚本已扩展到 `222` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续沿相邻 summary seam 收口：新增 `build_configured_tool_registry_provider_preflight_summary_model_from_dict()`，并让 `build_configured_tool_registry_provider_preflight_summary_model()` 直接复用这层 dict bridge；同时补上最小顶层 `preflight_result` 仍可从 `service_execution` 继承 `provider/provider_source_name/runtime_artifacts` 的 focused 兼容覆盖。focused 回归脚本已扩展到 `223` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_summary` / `preflight_result` 之间共享的 `service_execution` 归一化真正收成单点 helper：新增 `build_configured_tool_registry_provider_preflight_service_execution_model_from_dict()`，并让两条 dict bridge 统一复用这层 provider/provider_source_name/runtime_artifacts merge 逻辑。focused 回归脚本已扩展到 `224` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续沿相邻 typed internal seam 收口：新增 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_dict()`，把 `preflight_result` 顶层计数 + 共享 `service_execution` 归一化进一步收成单点 helper，并让 `preflight_summary` / `preflight_result` 两条链统一复用。focused 回归脚本已扩展到 `225` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_summary` / `preflight_result` 共同依赖的 typed pair 收成单点 helper：新增 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()`，统一返回 `service_execution_model + execution_result_model`，并让两条 dict bridge 直接复用；`build_configured_tool_registry_provider_preflight_result_model_from_dict()` 也因此去掉了一层 `model -> dict -> model` 往返。focused 回归脚本已扩展到 `226` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续沿相邻 typed helper 细化复用：新增 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_service_execution_model()`，让 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()` 不再重复计算 `service_execution_model`，而是基于已归一化的 typed model 直接补齐 execution-result。focused 回归脚本已扩展到 `227` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把相邻 `service_execution_result` 的 typed 入口统一下来：新增 `build_configured_tool_registry_provider_service_execution_result_model_from_service_execution_model()`，并让通用 `service_execution_result` 与 preflight 那条 `...from_service_execution_model()` 统一复用这层 helper。focused 回归脚本已扩展到 `228` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_result` 的 typed 入口也统一下来：新增 `build_configured_tool_registry_provider_preflight_result_model_from_service_execution_model()`，让 `build_configured_tool_registry_provider_preflight_result_model()` 不再自己手工拼装 typed execution-result，而是直接复用统一 helper。focused 回归脚本已扩展到 `229` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_summary` 的 typed 入口也补齐成对称 helper：新增 `build_configured_tool_registry_provider_preflight_summary_model_from_service_execution_model()`，并让 `build_configured_tool_registry_provider_preflight_summary_model_from_dict()` 直接复用这层 typed 入口，不再先展开共享 typed pair。focused 回归脚本已扩展到 `230` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_result` 的 dict outward bridge 也压到对称 typed 入口：`build_configured_tool_registry_provider_preflight_result_model_from_dict()` 现已直接复用 `build_configured_tool_registry_provider_preflight_result_model_from_service_execution_model()`，不再经过 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()` 这层共享 pair helper。focused 回归脚本已扩展到 `231` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_execution_models_from_dict()` 自身也压成更薄的组合壳：它现在会直接复用 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_dict()`，不再自己显式串联 `...preflight_service_execution_result_model_from_service_execution_model()`。focused 回归脚本已扩展到 `232` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_execution_models` 这层补齐对称 typed 入口：新增 `build_configured_tool_registry_provider_preflight_execution_models_from_service_execution_model()`，并让 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()` 直接复用它，去掉 `service_execution_model` 的重复 hydration。focused 回归脚本已扩展到 `233` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_summary/result` 两个 typed 入口也统一到 execution-model pair helper：`build_configured_tool_registry_provider_preflight_summary_model_from_service_execution_model()` 与 `build_configured_tool_registry_provider_preflight_result_model_from_service_execution_model()` 现已直接复用 `build_configured_tool_registry_provider_preflight_execution_models_from_service_execution_model()`，不再各自重复补 execution-result。focused 回归脚本已扩展到 `234` 条测试；最新校验同上通过。

### 最新同步（2026-05-25）

- `tool-runtime-productionization` 本轮继续把 `preflight_service_execution_result_model_from_dict()` 这层 outward bridge 也压到 shared pair helper：它现在会直接复用 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()`，不再自己先 hydration `service_execution_model` 再补 execution-result。focused 回归脚本已扩展到 `235` 条测试；最新校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_service_execution_result_model_from_service_execution_model()` 这层 typed wrapper 也统一到 shared pair helper：它现在会直接复用 `build_configured_tool_registry_provider_preflight_execution_models_from_service_execution_model()`，不再自己单独走通用 `service_execution_result` helper。focused 回归脚本已扩展到 `236` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_summary_model_from_service_execution_model()` 这层 typed 入口也统一到对称 result helper：它现在会直接复用 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_service_execution_model()`，不再自己展开 shared pair helper。focused 回归脚本已扩展到 `237` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_result_model_from_service_execution_model()` 这层 typed 入口也统一到对称 result helper：它现在会直接复用 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_service_execution_model()`，不再自己展开 shared pair helper。focused 回归脚本维持 `237` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_execution_models_from_service_execution_model()` 这层 shared pair helper 收回为更薄的组合壳：它现在会直接复用 `build_configured_tool_registry_provider_preflight_service_execution_result_model_from_service_execution_model()`，同时 `preflight_service_execution_result_model_from_service_execution_model()` 改为直接走通用 `service_execution_result` helper。focused 回归脚本已扩展到 `238` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict 侧也收成同样的组合壳：`preflight_service_execution_result_model_from_dict()` 现在会直接走通用 `service_execution_result` helper，而 `preflight_execution_models_from_dict()` 则直接复用 `preflight_service_execution_result_model_from_dict()`。focused 回归脚本维持 `238` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict outward 的 `preflight_summary/result` 两个入口也统一回 `preflight_execution_models_from_dict()`：它们现在都会直接消费这层组合壳返回的 typed pair，不再各自重复做 `service_execution` hydration。focused 回归脚本已扩展到 `239` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 的 payload normalization 单独收成 helper：新增 `build_configured_tool_registry_provider_preflight_service_execution_payload_from_dict()`，并让 `preflight_service_execution_model_from_dict()`、`preflight_service_execution_result_model_from_dict()`、`preflight_execution_models_from_dict()` 统一复用它，去掉 dict 侧重复的 `service_execution` hydration。focused 回归脚本已扩展到 `240` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 payload-normalization 之上的 typed pair seam 也收成单点：新增 `build_configured_tool_registry_provider_preflight_execution_models_from_service_execution_payload()`，并让 `preflight_service_execution_model_from_dict()`、`preflight_service_execution_result_model_from_dict()`、`preflight_execution_models_from_dict()` 三条 dict 入口统一复用它。focused 回归脚本维持 `240` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_service_execution_model_from_dict()` 和 `preflight_service_execution_result_model_from_dict()` 这两个壳层再往外退一步：它们现在都直接从 `build_configured_tool_registry_provider_preflight_execution_models_from_dict()` 取第一/第二个 model，不再自己单独触碰 payload-to-pair helper。focused 回归脚本维持 `240` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 的 dict/typed outward 入口一起收成 total-model 单点：新增 `build_configured_tool_registry_provider_preflight_models_from_dict()` 与 `build_configured_tool_registry_provider_preflight_models_from_service_execution_model()`，统一产出 `service_execution_model + execution_result_model + summary_model + result_model`。
- `preflight_service_execution_model_from_dict()`、`preflight_service_execution_result_model_from_dict()`、`preflight_summary_model_from_dict()`、`preflight_result_model_from_dict()` 以及 typed 侧的 `preflight_summary/result_model_from_service_execution_model()` 现在都直接从这两层 total-model helper 取各自结果，不再平行展开 pair/result 链路。focused 回归脚本已扩展到 `244` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 的 pair-to-total 总装也收成单点：新增 `build_configured_tool_registry_provider_preflight_models_from_models()`，统一负责 `service_execution_model + execution_result_model -> summary_model + result_model`。
- `preflight_execution_models_from_dict()` 现已退回为 total-model 兼容壳，直接从 `build_configured_tool_registry_provider_preflight_models_from_dict()` 取前两个 model；`preflight_result_model_from_models()` 也改为直接复用 `preflight_models_from_models()`。focused 回归脚本已扩展到 `246` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict 侧的 `payload -> total models` 也收成单点：新增 `build_configured_tool_registry_provider_preflight_models_from_service_execution_payload()`，统一负责 normalized payload 直接派生 `service_execution_model + execution_result_model + summary_model + result_model`。
- `preflight_models_from_dict()` 现已直接复用这层 payload-total helper；typed 侧 `preflight_execution_models_from_service_execution_model()` 也退回为 total-model 兼容壳，直接从 `preflight_models_from_service_execution_model()` 取前两个 model。focused 回归脚本已扩展到 `247` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 payload 侧 pair helper 也彻底退成 total-model 兼容壳：`preflight_execution_models_from_service_execution_payload()` 现在会直接从 `build_configured_tool_registry_provider_preflight_models_from_service_execution_payload()` 取前两个 model。
- 同时 `preflight_models_from_service_execution_payload()` 自己改为直接完成 `service_execution_model` hydration、`execution_result_model` 派生和 total-model 组装，不再经过 payload pair helper。focused 回归脚本已扩展到 `249` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 typed 侧的 `preflight_service_execution_result_model_from_service_execution_model()` 也退成 total-model 兼容壳：它现在会直接从 `build_configured_tool_registry_provider_preflight_models_from_service_execution_model()` 取第二个 model。
- 同时 `preflight_models_from_service_execution_model()` 自己改为直接走通用 `build_configured_tool_registry_provider_service_execution_result_model_from_service_execution_model()` 完成 execution-result 派生，避免再绕回 typed preflight result helper。focused 回归脚本维持 `249` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `summary` 这条基础 seam 也退回 total-model 兼容壳：`preflight_summary_model_from_models()` 现在会直接从 `build_configured_tool_registry_provider_preflight_models_from_models()` 取第三个 model，`preflight_summary_model_from_result_model()` 也直接返回已有 `result.summary`。
- 同时 `preflight_models_from_models()` 自己改为直接走 `preflight_summary_model_from_parts()` 完成 summary 组装，再组合 result model，不再反向依赖 `summary_model_from_models()`。focused 回归脚本已扩展到 `251` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把高层 raw-input / execute 入口也退回 total-model helper：新增 `build_configured_tool_registry_provider_preflight_models()` 与 `execute_configured_tool_registry_provider_preflight_models()`。
- `build_preflight_result_model()` 和 `execute_preflight_model()` 现在都会直接从这两层 helper 取第四个 `result_model`，不再各自重复做 `service_execution_model` hydration 或 `execution_result_model` 执行后组装。focused 回归脚本已扩展到 `253` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把最外层 outward dict seam 也收成单点：新增 `build_configured_tool_registry_provider_preflight_dicts_from_models()`、`build_configured_tool_registry_provider_preflight_dicts()` 与 `execute_configured_tool_registry_provider_preflight_dicts()`。
- `build_preflight_summary()`、`build_preflight_result()` 与 `execute_preflight()` 现在都退回为只取 summary/result dict 的兼容壳，不再各自重复做 typed hydration、result 组装或执行后 dict 转换。focused 回归脚本已扩展到 `256` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把高层 outward `models + dicts` 也统一到 total-output seam：新增 `build_configured_tool_registry_provider_preflight_outputs_from_models()`、`build_configured_tool_registry_provider_preflight_outputs()` 与 `execute_configured_tool_registry_provider_preflight_outputs()`。
- `build_preflight_models()`、`build_preflight_result_model()`、`build_preflight_result()`、`execute_preflight_models()`、`execute_preflight_model()` 与 `execute_preflight()` 现在都退回为从这层 `outputs` helper 取对应 model/dict 的兼容壳；focused 回归脚本已扩展到 `257` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把单参 `preflight_result` 的 dict 兼容链也统一到 total-output seam：新增 `build_configured_tool_registry_provider_preflight_outputs_from_dict()`。
- `preflight_service_execution_model_from_dict()`、`preflight_service_execution_result_model_from_dict()`、`preflight_models_from_dict()`、`preflight_summary_model_from_dict()`、`preflight_result_model_from_dict()`、`preflight_dicts()` 与 `preflight_summary()` 现在都退回为从这层 `outputs_from_dict()` 取对应结果的兼容壳；focused 回归脚本维持 `257` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution_payload + preflight_result` 这条 payload typed 链也统一到 total-output seam。
- `preflight_execution_models_from_service_execution_payload()` 与 `preflight_models_from_service_execution_payload()` 现在都退回为从 `build_configured_tool_registry_provider_preflight_outputs()` 取对应 typed 结果的兼容壳，不再各自重复做 payload -> typed hydration / summary/result 组装；focused 回归脚本维持 `257` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution_payload + execution_result` 这条 payload total-output seam 本身也明确收成单点：新增 `build_configured_tool_registry_provider_preflight_outputs_from_service_execution_payload()`。
- `build_preflight_outputs()` 现在退回为从这层 payload helper 取结果的兼容壳，而 `preflight_execution_models_from_service_execution_payload()` 与 `preflight_models_from_service_execution_payload()` 也一起锁到这层新 helper；focused 回归脚本已扩展到 `258` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 typed `service_execution_model + preflight_result` 这条链也统一到 total-output seam。
- `preflight_service_execution_result_model_from_service_execution_model()`、`preflight_execution_models_from_service_execution_model()` 与 `preflight_models_from_service_execution_model()` 现在都退回为从 `build_configured_tool_registry_provider_preflight_outputs_from_service_execution_model()` 取对应 typed 结果的兼容壳；focused 回归脚本已扩展到 `259` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把单参 dict total-output 总出口也并回更高层 `build_configured_tool_registry_provider_preflight_outputs()`。
- `build_configured_tool_registry_provider_preflight_outputs_from_dict()` 现在退回为从 `build_configured_tool_registry_provider_preflight_outputs(service_execution, execution_result)` 取总装结果的兼容壳；focused 回归脚本已扩展到 `261` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution_payload + execution_result` 这条 total-output 入口也并回高层 `build_configured_tool_registry_provider_preflight_outputs()`。
- `build_configured_tool_registry_provider_preflight_outputs()` 现在自己承担 `service_execution` hydration，再直接复用 `build_configured_tool_registry_provider_preflight_outputs_from_service_execution_model()`；`build_configured_tool_registry_provider_preflight_outputs_from_service_execution_payload()` 则退回为从高层 `outputs()` 取总装结果的兼容壳；focused 回归脚本已扩展到 `263` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute 侧也补成对称的 typed total-output seam：新增 `execute_configured_tool_registry_provider_preflight_outputs_from_service_execution_model()`。
- `execute_configured_tool_registry_provider_preflight_outputs()` 现在退回为只负责构造 `service_execution_model`，再直接复用这层 typed execute helper；focused 回归脚本已扩展到 `265` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把最外层 summary/result wrapper 这批 outward 入口退回到最近邻 helper。
- `build_preflight_summary()` 现在直接复用 `build_preflight_dicts()`；`build_preflight_summary_model_from_dict()` / `...from_service_execution_model()` 与 `build_preflight_result_model()` / `...from_dict()` / `...from_service_execution_model()` 现在都退回为从对应 `models()` helper 取结果；`build_preflight_result()`、`execute_preflight_model()`、`execute_preflight()` 也分别退回为复用 `result_model()`、`execute_preflight_models()`、`execute_preflight_dicts()` 的兼容壳；focused 回归脚本维持 `265` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution/execution_models` 这组 wrapper 也退回到最近邻 helper。
- `build_preflight_service_execution_model_from_dict()` 与 `build_preflight_service_execution_result_model_from_dict()` 现在直接复用 `build_preflight_execution_models_from_dict()`；typed `...service_execution_result_model_from_service_execution_model()` 退回为复用 `...execution_models_from_service_execution_model()`；而 `execution_models_from_service_execution_payload()` / `...from_service_execution_model()` 也都退回为从对应 `models()` helper 取前两个结果。进一步地，`build_preflight_models()`、`...models_from_service_execution_payload()`、`...models_from_dict()` 与 typed `...models_from_service_execution_model()` 现在也都直接走 `models_from_models()` 主链，不再先经过 `outputs()`；focused 回归脚本已扩展到 `267` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `dicts` 这一层 outward wrapper 也并回 `models` 主链。
- `build_preflight_dicts_from_models()` 现在直接复用 `build_preflight_models_from_models()` 再做 `to_dict()`；`build_preflight_dicts()` 现在直接复用 `build_preflight_models_from_dict()`；`execute_preflight_dicts()` 现在直接复用 `execute_preflight_models()` 后再统一走 `dicts_from_models()`，不再平行依赖更深的 `outputs()` seam；focused 回归脚本已扩展到 `270` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `outputs` 这一组 build/execute wrapper 也退回到 `models + dict projection` 主链。
- build 侧 `build_preflight_outputs()` / `...from_service_execution_payload()` / `...from_service_execution_model()` / `...from_dict()` 现在都先走各自最近邻的 `models` helper，再统一通过共享的 resolved-model output assembler 产出 dict；execute 侧补齐了对称的 `execute_preflight_models_from_service_execution_model()`，并让 `execute_preflight_outputs()` / `...outputs_from_service_execution_model()` 也都回到 `execute_models` 主链；focused 回归脚本已扩展到 `272` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution` 内核层的两处 model/dict 往返拿掉。
- `build_service_execution_model()` 现在直接复用新的 typed `runtime_artifacts_model -> runtime_service_actions_model` helper，不再先 `runtime_artifacts.to_dict()`；`execute_service_execution_model()` 现在直接把 `service_execution.service_actions` 组装成 typed actions model 再执行，不再先 `[action.to_dict()]` 再绕一层 dict result helper；focused 回归脚本已扩展到 `274` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮开始把相邻的 `service_execution result + dict` 这一层也补成 `outputs` 单点。
- 新增 `build_service_execution_outputs()` / `...outputs_from_service_execution_model()` / `...outputs_from_models()` 与 `execute_service_execution_outputs()` / `...outputs_from_service_execution_model()`；`build_service_execution_result_model()`、`...result_model_from_service_execution_model()`、`execute_service_execution()` 现在都退回为从这条 `outputs` seam 取 typed model 或 dict；focused 回归脚本已扩展到 `277` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把更内层的 `runtime_service_actions` build/execute wrapper 也补成 `outputs` 单点。
- 新增 `build_runtime_service_actions_outputs()` / `...outputs_from_runtime_artifacts_model()` / `...outputs_from_models()` 与 `execute_runtime_service_actions_outputs()` / `...outputs_from_models()`；`build_runtime_service_actions()`、`build_runtime_service_actions_model()`、`execute_runtime_service_actions()`、`execute_runtime_service_actions_result_model()` 现在都退回为从这条 `outputs` seam 取 typed model 或 dict，`build_service_execution_model()` 与 `execute_service_execution_model()` 也同步退回到这层最近邻 helper。focused 回归脚本已扩展到 `284` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `runtime_service_actions` 的 typed-from-artifacts 和 dict-from-dicts 两条入口也并回到最近邻 `outputs` seam。
- 新增 `build_runtime_service_actions_outputs_from_dicts()`；`build_runtime_service_actions_model_from_runtime_artifacts_model()`、`build_runtime_service_actions_model_from_dicts()`、`build_service_execution_model_from_dict()`、`execute_runtime_service_actions_outputs()` 现在都退回为从 `...outputs_from_runtime_artifacts_model()` / `...outputs_from_dicts()` 取 typed model 或 dict，不再平行保留额外的组装路径。focused 回归脚本已扩展到 `288` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `runtime_service_actions result` 也补成了对称 `outputs` seam。
- 新增 `build_runtime_service_actions_result_outputs_from_models()` 与 `...outputs_from_dict()`；`build_runtime_service_actions_result_model_from_dict()` 现在退回为从这层 `result outputs` seam 取 typed result model，而 `build_service_execution_outputs_from_service_execution_model()` 也同步退回为先走这层 seam，再统一组装 `service_execution` result。focused 回归脚本已扩展到 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 这边两条 build-side `service_execution_result` wrapper 也并回了 `service_execution outputs` 主链。
- `build_preflight_service_execution_result_model_from_dict()` 现在退回为先走 `build_preflight_service_execution_payload_from_dict()`，再直接复用 `build_service_execution_outputs()`；`build_preflight_service_execution_result_model_from_service_execution_model()` 则退回为直接复用 `build_service_execution_outputs_from_service_execution_model()`。focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `preflight models / execution_models` 三条入口一起并回 `service_execution outputs` 主链。
- `build_preflight_execution_models_from_dict()` / `...from_service_execution_payload()` / `...from_service_execution_model()` 现在都会先复用 `service_execution outputs` 产出 typed `execution_result_model`，再只保留一层最薄的 `service_execution_model` hydration 或透传；`build_preflight_models_from_dict()` / `...from_service_execution_payload()` / `...from_service_execution_model()` 也统一退回为“单次 `service_execution_model` hydration + `service_execution outputs` + `preflight_models_from_models()`”的总装路径。focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把最外层 raw `service_execution + execution_result` build wrapper 也退回到了 payload seam。
- `build_preflight_models()`、`build_preflight_outputs()` 和 `build_preflight_result_model()` 现在都直接复用 `...from_service_execution_payload()` 这一层，不再自己在 raw 输入层重复选路或做平行总装；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build/execute 的 dict outward wrapper 也统一退回 `outputs` seam。
- `build_preflight_dicts()`、`build_preflight_result()`、`execute_preflight_dicts()`、`execute_preflight()` 现在都直接从对应 `outputs` helper 取 summary/result dict，不再先走 `models` 再做二次 dict 投影；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把剩余的 preflight model outward wrapper 也统一退回 `outputs` seam。
- `build_preflight_summary_model_from_dict()`、`build_preflight_result_model_from_dict()`、`build_preflight_summary_model_from_service_execution_model()`、`build_preflight_result_model_from_service_execution_model()` 与 `execute_preflight_model()` 现在都直接从对应 `outputs` helper 取 typed model，不再平行复用旧的 `models` / `execute_models` seam；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight outputs` 这组 wrapper 本身也往最近邻 seam 收了一层。
- `build_preflight_outputs()` 现在直接构造 typed `service_execution_model` 后复用 `build_preflight_outputs_from_service_execution_model()`；`build_preflight_outputs_from_service_execution_payload()` 与 `build_preflight_outputs_from_dict()` 都退回为只负责参数兼容再复用高层 `outputs()`；`build_preflight_outputs_from_service_execution_model()` 与 `execute_preflight_outputs_from_service_execution_model()` 则统一退回为从 `build_preflight_outputs_from_models()` 取总装结果，`execute_preflight_outputs()` 也直接复用 typed execute helper；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight ...from_models()` 这组 outward wrapper 也并回 `outputs_from_models` 主链。
- `build_preflight_dicts_from_models()`、`build_preflight_summary_model_from_models()`、`build_preflight_result_model_from_models()` 现在都直接从 `build_preflight_outputs_from_models()` 取 dict / typed model；`build_preflight_result_model()` 也直接复用 `build_preflight_outputs_from_service_execution_payload()` 取 result model，不再平行依赖 `models_from_models` / `models_from_service_execution_payload`；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight models_from_dict / ...payload / ...service_execution_model` 三条 typed 总装入口也统一并回 `preflight_execution_models_*` 主链。
- `build_preflight_models_from_dict()`、`build_preflight_models_from_service_execution_payload()`、`build_preflight_models_from_service_execution_model()` 现在都先复用对应的 `preflight_execution_models_*` helper，再统一进入 `build_preflight_models_from_models()`，不再各自重复打一遍 `service_execution outputs`；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict/typed `service_execution_model/result_model` wrapper 也统一并回 `preflight_execution_models_*` 主链。
- `build_preflight_service_execution_model_from_dict()`、`build_preflight_service_execution_result_model_from_dict()`、`build_preflight_service_execution_result_model_from_service_execution_model()`，以及 `build_preflight_execution_models_from_dict()` 现在都直接复用 `preflight_execution_models_from_service_execution_payload()` / `...from_service_execution_model()`，dict path 的 typed pair 装配边界进一步收敛到单点 payload seam；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight_execution_models_from_service_execution_payload()` 也退回到 typed helper。
- `build_preflight_execution_models_from_service_execution_payload()` 现在先构造 typed `service_execution_model`，再直接复用 `build_preflight_execution_models_from_service_execution_model()`，这样 dict/payload 两层都会统一先落到 typed `service_execution_model` 再派生 `execution_result_model`；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 typed `preflight_execution_models_from_service_execution_model()` 也退回到通用 `service_execution_result_model` helper。
- `build_preflight_execution_models_from_service_execution_model()` 和 `build_preflight_service_execution_result_model_from_service_execution_model()` 现在都直接复用 `build_service_execution_result_model_from_service_execution_model()`，typed path 不再平行保留一条单独的 `service_execution_outputs_from_service_execution_model()` 委托链；focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 剩余的 dict/payload/typed wrapper 成片并回 `outputs_from_*` seam。
- `build_preflight_service_execution_model_from_dict()`、`build_preflight_service_execution_result_model_from_dict()`、`build_preflight_execution_models_from_dict()`、`build_preflight_models_from_dict()` 现在都直接从 `build_preflight_outputs_from_dict()` 取对应 typed 结果；`...from_service_execution_payload()` 与 `...from_service_execution_model()` 两组 `execution_models/models/result_model` wrapper 也同步退回为从最近邻 `outputs_from_service_execution_payload()` / `...outputs_from_service_execution_model()` 取值，不再分散依赖旧的 `execution_models` / generic typed seam。focused 回归脚本维持 `292` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 的 `preflight models` 平行总装链也并回 `execute_preflight_outputs*` seam。
- `execute_preflight_models_from_service_execution_model()` 现在直接从 `execute_preflight_outputs_from_service_execution_model()` 取前四个 typed 结果，`execute_preflight_models()` 也同步退回为直接复用 `execute_preflight_outputs()`；execute 侧不再重复做一遍 `execute_service_execution_model() + preflight_models_from_models()` 的平行总装。focused 回归脚本现已扩展到 `294` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution` execute 侧也补成了和 build 侧对称的共享总装 seam。
- `execute_service_execution_outputs_from_service_execution_model()` 现在先执行 typed `runtime_service_actions_outputs_from_models()`，再统一复用 `build_service_execution_outputs_from_models()` 组装 typed result+dict；`execute_service_execution_model()` 则同步退回为直接从这层 `outputs_from_service_execution_model()` 取 result model。focused 回归脚本现已扩展到 `296` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 的 `preflight outputs_from_service_execution_model()` 也并回 `service_execution outputs` seam。
- `execute_preflight_outputs_from_service_execution_model()` 现在直接复用 `execute_service_execution_outputs_from_service_execution_model()` 取 typed `execution_result_model`，不再平行直连 `execute_service_execution_model()`；这条 execute-side preflight 总装链现在和 build-side `preflight_outputs_from_service_execution_model()` 的最近邻 seam 也更对称了。focused 回归脚本维持 `296` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把三条还直接切 `outputs()[0]` 的 `runtime_service_actions / service_execution` wrapper 一起退回到最近邻 `*_model_*` helper。
- `build_service_execution_model_from_dict()` 现在直接复用 `build_runtime_service_actions_model_from_dicts()`，`build_service_execution_model()` 直接复用 `build_runtime_service_actions_model_from_runtime_artifacts_model()`，`execute_runtime_service_actions_outputs()` 也直接复用 `build_runtime_service_actions_model_from_dicts()`；这三条链不再自己切 `outputs_*()[0]` 拿 model。focused 回归脚本现已扩展到 `297` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把一条 raw build 链和两条 `service_execution` result 链一起退回到了最近邻 typed model helper。
- `build_runtime_service_actions_outputs()` 现在先把 raw `runtime_artifacts` hydrate 成 typed `runtime_artifacts_model`，再统一复用 `build_runtime_service_actions_outputs_from_runtime_artifacts_model()`；`build_service_execution_outputs_from_service_execution_model()` 改为直接复用 `build_runtime_service_actions_result_model_from_dict()`，`execute_service_execution_outputs_from_service_execution_model()` 改为直接复用 `execute_runtime_service_actions_model()`。这样三条链都不再为“只需要 typed model”绕经更外层 `outputs` helper。focused 回归脚本现已扩展到 `299` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `*_result_model` 这组 wrapper 成片退回到 typed result seam。
- `build_runtime_service_actions_result_model_from_dict()` 现在直接复用 `build_runtime_service_actions_result_model()`；`build_service_execution_result_model()` 改为先 hydrate typed `service_execution_model` 再复用 `build_service_execution_result_model_from_service_execution_model()`，后者直接组合 `build_runtime_service_actions_result_model_from_dict()` + `build_service_execution_result_model_from_models()`；`execute_service_execution_model()` 也直接组合 `execute_runtime_service_actions_model()` + `build_service_execution_result_model_from_models()`。这样这组 wrapper 不再为了只拿 model 先绕 `outputs` 再拆回来。focused 回归脚本维持 `299` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 raw dict `outputs` wrapper 这一批也并回了最近邻 `result_model` seam。
- `build_runtime_service_actions_result_outputs_from_dict()` 现在直接复用 `build_runtime_service_actions_result_model_from_dict()`，`execute_runtime_service_actions()` 直接复用 `execute_runtime_service_actions_result_model()`；`build_service_execution_outputs()` 直接复用 `build_service_execution_result_model()`，`execute_service_execution()` 与 `execute_service_execution_outputs()` 则都直接复用 `execute_service_execution_model()`。这样这批 raw wrapper 现在只负责 typed hydration 和 `to_dict()`，不再平行保留额外总装链。focused 回归脚本现已扩展到 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 最外层 raw wrapper 也并回了 `summary/result model` seam。
- `build_preflight_summary()` 现在直接复用 `build_preflight_summary_model_from_dict()`，`build_preflight_result()` 直接复用 `build_preflight_result_model()`；`build_preflight_dicts()` 改为直接从 `build_preflight_result_model_from_dict()` 取 `summary/result` dict，`execute_preflight()` 与 `execute_preflight_dicts()` 则都直接复用 `execute_preflight_model()`。这样这批最外层入口现在只负责 `to_dict()` 投影，不再平行保留额外 `outputs` 总装链。focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `preflight` 剩下的 typed `summary/result model` wrapper 成片并回了 `result_model/models` 主链。
- `build_preflight_summary_model_from_dict()`、`...from_service_execution_model()`、`...from_models()` 现在都直接复用对应 `result_model` helper 再取 `summary`；`build_preflight_result_model_from_service_execution_model()` 改为先复用通用 `build_service_execution_result_model_from_service_execution_model()`，再统一进入 `build_preflight_result_model_from_models()`；`build_preflight_result_model_from_models()` 与 `...from_dict()` 也分别退回到 `build_preflight_models_from_models()` 和 `build_preflight_result_model_from_service_execution_model()`。这样这批 typed wrapper 不再为了只拿 `summary/result model` 绕经 `outputs` seam，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 的 `preflight models/model` wrapper 成片并回了 `service_execution outputs + models_from_models` 主链。
- `execute_preflight_models_from_service_execution_model()` 现在先复用 `execute_service_execution_outputs_from_service_execution_model()` 取 typed `execution_result_model`，再统一进入 `build_preflight_models_from_models()`；`execute_preflight_outputs_from_service_execution_model()` 改为从这条 `models` helper 取 resolved models 再做 dict 投影；`execute_preflight_models()` 与 `execute_preflight_model()` 也分别退回到 `...models_from_service_execution_model()` 和 `...models()`。这样 execute 侧不再继续从 `outputs` seam 反向拆 typed 结果，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `preflight service_execution_model/result_model/execution_models/models` wrapper 成片收回到了更直接的 typed seam。
- `build_preflight_service_execution_model_from_dict()` 现在先复用 `build_preflight_service_execution_payload_from_dict()`；`build_preflight_service_execution_result_model_from_dict()` 与 typed `...from_service_execution_model()` 统一退回到通用 `build_service_execution_result_model_from_service_execution_model()`；`build_preflight_execution_models_from_dict()/...from_service_execution_payload()/...from_service_execution_model()` 与三条 `build_preflight_models_from_*()` 也都统一改成“先拿 typed execution-model pair，再进入 `build_preflight_models_from_models()`”。这样 build-side `preflight` 这段不再继续从 `outputs` seam 反向拆 typed 结果，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `preflight summary/result/dicts` wrapper 成片收回到了 `preflight_models_*` 主链。
- `build_preflight_summary_model_from_dict()/...from_service_execution_model()/...from_models()` 现在都直接从对应 `preflight_models_*` helper 取第三个 `summary_model`；`build_preflight_result_model()`、`...result_model_from_service_execution_model()`、`...result_model_from_dict()` 与 `build_preflight_dicts()` 也都统一改成从 `preflight_models_*` helper 取第四个 `result_model` 或最后两个 dict。这样 build-side `preflight` 这段的 outward wrapper 不再平行绕 `outputs` 或 `service_execution_result` seam，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `preflight outputs*` wrapper 成片收回到了 `preflight_models_* + outputs_from_resolved_models()` 主链。
- `build_preflight_outputs()`、`...outputs_from_service_execution_payload()`、`...outputs_from_service_execution_model()` 与 `...outputs_from_dict()` 现在都先复用对应 `preflight_models_*` helper 拿到 typed `service_execution/execution_result/summary/result`，再统一进入 `build_preflight_outputs_from_resolved_models()` 做 dict 投影。这样 build-side `outputs*` 真正只负责 outward dict 组装，不再平行绕 `service_execution_outputs` 或更高层 `outputs` seam，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 的 `preflight outputs/dicts` wrapper 成片收回到了 `execute_preflight_models*` 主链。
- `execute_preflight_outputs()` 现在先复用 `execute_preflight_models()` 拿到 typed `service_execution/execution_result/summary/result`，再统一进入 `build_preflight_outputs_from_resolved_models()`；`execute_preflight_dicts()` 也改成直接从 `execute_preflight_models()` 取 `summary_model/result_model` 再做 `to_dict()`。这样 execute-side 的 outward wrapper 不再平行绕 `outputs_from_service_execution_model()` 或 `execute_preflight_model()`，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 的 `service_execution/preflight` typed wrapper 再拉直一层。
- `execute_service_execution_outputs_from_service_execution_model()` 现在直接复用 `execute_service_execution_model()`；`execute_preflight_models_from_service_execution_model()` 也直接复用同一条 typed `service_execution_result_model` seam，再统一进入 `build_preflight_models_from_models()`。这样 execute 侧不再为拿 typed `execution_result_model` 中间绕一层 `service_execution outputs`，focused 回归脚本维持 `302` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `runtime_service_actions` 的 raw build/execute wrapper 退回到更直接的 `model/result_model` seam。
- `build_runtime_service_actions()` 现在直接复用 `build_runtime_service_actions_model()`；`build_runtime_service_actions_model()` 统一先 hydrate typed `runtime_artifacts_model` 再进入 `...model_from_runtime_artifacts_model()`；`build_runtime_service_actions_outputs_from_runtime_artifacts_model()` 与 `...outputs_from_dicts()` 也同步退回成“model helper + to_dict()”的 outward 壳；`execute_runtime_service_actions_outputs()` 则直接复用 `execute_runtime_service_actions_result_model()`，而 `execute_runtime_service_actions_model()` 直接复用 `...result_model_from_models()`。这样这组 wrapper 不再为拿 typed model 或 result 额外绕 `outputs`，focused 回归脚本现在是 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 的 `service_execution/preflight` 最近邻 helper 再拉直一层。
- `build_service_execution_outputs_from_service_execution_model()` 现在直接复用 `build_service_execution_result_model_from_service_execution_model()`；`build_preflight_result_model_from_service_execution_model()` 也退回为“先拿 typed `service_execution_result_model`，再进入 `build_preflight_result_model_from_models()`”；`build_preflight_summary_model_from_service_execution_model()` 则直接复用 `build_preflight_result_model_from_service_execution_model()` 再取 `summary`。这样 build 侧不再为拿 typed result 中间绕 `models` 或更低一层 runtime-result seam，focused 回归脚本维持 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side 最外层 raw `models / result_model` 也统一回到了单参 `preflight_result` dict seam。
- 这里记录的是当时的阶段性方案：`build_preflight_models()` 和 `build_preflight_result_model()` 曾短暂统一经过 `build_preflight_result_payload()`，再分别直接复用 `...models_from_dict()` 与 `...result_model_from_dict()`。该 helper 后续已删除，当前实现不再经过这条单参 payload 主链。
- `tool-runtime-productionization` 本轮继续把 build-side `service_execution + execution_result` 这组 payload 入口统一回到了单参 `preflight_result payload` seam。
- 这里记录的是当时的阶段性方案：`build_preflight_execution_models_from_service_execution_payload()`、`build_preflight_models_from_service_execution_payload()` 和 `build_preflight_outputs_from_service_execution_payload()` 曾短暂统一经过 `build_preflight_result_payload()`，再分别直接复用 `...execution_models_from_dict()`、`...models_from_dict()` 和 `...outputs_from_dict()`。该 helper 后续已删除，当前实现已改为“先 merge metadata，再单次 typed hydrate”。
- `tool-runtime-productionization` 本轮继续把 build raw `preflight summary_model` 和 execute-side 对称 `summary` helper 家族一起收口了。
- `build_preflight_summary_model()` 现在直接复用 `result_model_from_dict()` 再取 summary；同时新增 `execute_preflight_summary_model_from_service_execution_model()`、`execute_preflight_summary_model()` 和 `execute_preflight_summary()`，让 execute 侧也有和 build 侧对称的 `summary_model / summary` 主链。这样 summary 侧入口不再只依赖 top-level `result/model` 之外的平行壳，focused 回归脚本现在是 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `preflight summary` 这一族 wrapper 成片收回到了 `result_model` 主链。
- `build_preflight_summary_model_from_dict()`、`...from_service_execution_model()` 与 `...from_models()` 现在都统一先复用对应最近邻的 `preflight_result_model` helper，再通过 `summary_model_from_result_model()` 取 summary；这样 summary 侧不再平行绕 `outputs` / `models` 取值，focused 回归脚本维持 `304` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 顶层 `preflight` 的 raw `models/model/dicts` wrapper 成片收回到了更近的 typed seam。
- 新增 `execute_configured_tool_registry_provider_preflight_model_from_service_execution_model()` 之后，`execute_preflight_models()` 现在直接复用 typed `...models_from_service_execution_model()`，`execute_preflight_model()` 退回成单次 typed `service_execution_model` hydrate 后直连新的 typed `...model_from_service_execution_model()`，`execute_preflight_dicts()` 也同步改成直接复用 `execute_preflight_model()` 再投影 `summary/result` dict。这样 execute 顶层 raw outward wrapper 又少掉一层 `outputs` 中转，focused 回归脚本现在是 `304` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `preflight` 的 raw `models/result/result` 与 typed `from_models` wrapper 成片收回到了最近邻 typed seam。
- `build_preflight_models()` 现在直接复用 typed `build_preflight_models_from_service_execution_model()`；`build_preflight_result_model()` 退回为单次 typed `service_execution_model` hydrate 后直接复用 `...result_model_from_service_execution_model()`；`build_preflight_result()` 则统一直接复用 `build_preflight_result_model()` 并做 `to_dict()`。同时 `build_preflight_summary_model_from_models()`、`build_preflight_result_model_from_models()` 与 `build_preflight_dicts_from_models()` 也都退回成直接复用 `models_from_models()` / `result_model_from_models()`，不再平行绕 `outputs_from_models()`。这样 build-side `preflight` 这段又少掉一层 total-output 中转，focused 回归脚本维持 `303` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side 两参 `preflight` raw wrapper 成片收回到了单参 `preflight_result dict` seam。
- 这里记录的是当时的阶段性方案：曾新增 `build_configured_tool_registry_provider_preflight_result_payload()`，并让 `build_preflight_models()`、`build_preflight_outputs()`、`build_preflight_outputs_from_service_execution_payload()`、`build_preflight_result_model()` 与 `build_preflight_result()` 统一先合成 outward `preflight_result` payload。该 helper 后续已删除，当前实现不再走这条旧链路。
- `tool-runtime-productionization` 本轮继续把 build/execute 最外层 dict outward wrapper 收回到了 `dicts` / `outputs_from_dict` seam。
- `build_preflight_summary()` 现在直接复用 `build_preflight_dicts()` 取 summary dict，`build_preflight_result()` 也直接复用同一层取 result dict；同时 `build_preflight_dicts()` 本身改成直接从 `build_preflight_outputs_from_dict()` 取最后两个 dict。execute 侧的 `execute_preflight_dicts()` 现在直接复用 `execute_preflight_outputs()`，而 `execute_preflight()` 直接复用 `execute_preflight_dicts()`。这样最外层 dict outward wrapper 不再各自平行绕 `model/models` seam，focused 回归脚本维持 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution` 最外层 raw wrapper 收回到了 `outputs_from_service_execution_model()` / `outputs()` seam。
- `build_service_execution_outputs()` 现在直接复用 `build_service_execution_outputs_from_service_execution_model()`；`execute_service_execution_outputs()` 也直接复用 `execute_service_execution_outputs_from_service_execution_model()`；而 `execute_service_execution()` 则直接复用 `execute_service_execution_outputs()` 取最终 dict。这样 `service_execution` build/execute 两侧的 raw outward wrapper 不再各自平行做一次 typed hydration 或 result `to_dict()`，focused 回归脚本维持 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution_result` 与 `preflight_service_execution_result` 这批 wrapper 收回到了最近邻 `service_execution outputs` seam。
- `build_service_execution_result_model()`、`...result_model_from_service_execution_model()` 与 `execute_service_execution_model()` 现在都统一退回为从各自最近邻 `service_execution outputs` helper 取 typed result；同时 `build_preflight_service_execution_result_model_from_dict()` 与 `...from_service_execution_model()` 也统一复用同一条 `service_execution outputs` 主链。这样这批 helper 不再平行保留“result_model 自己组装一遍、outputs 再组装一遍”的重复入口，focused 回归脚本维持 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build/execute 最外层 `preflight` dict outward wrapper 收回到了 `summary_model/result_model` seam。
- `build_preflight_summary()` 现在直接复用 `build_preflight_summary_model_from_dict()` 并做 `to_dict()`；`build_preflight_dicts()` 改成直接从 `build_preflight_result_model_from_dict()` 取 `summary/result` dict；`build_preflight_result()` 直接复用 `build_preflight_result_model()`。execute 侧的 `execute_preflight_dicts()` 与 `execute_preflight()` 也统一直接复用 `execute_preflight_model()`。这样这批最外层 dict wrapper 不再平行绕 `dicts/outputs` seam，focused 回归脚本维持 `303` 条测试；最新校验同上通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution / preflight` 剩余的 wrapper 套 wrapper 入口再拉直一层（2026-06-01）。
- 先把 6 条 focused seam tests 抬成红灯后，`build_service_execution_result_model()`、`build_service_execution_outputs()` 与 `...outputs_from_service_execution_model()` 现已统一退回到最近邻 typed `service_execution_result_model` seam；`execute_service_execution_model()` 直接复用 `execute_configured_tool_registry_provider_runtime_service_actions_model()`，`execute_service_execution_outputs*()` 也都退回为 `result_model + to_dict()` 的最薄 outward 壳；相邻的 `build_preflight_service_execution_result_model_from_service_execution_model()` 则改为直接挂回通用 `service_execution_result` 主链。外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 dict 侧 `preflight` pair 入口再收薄一层（2026-06-01，续二）。
- `build_preflight_execution_models_from_dict()` 现在直接复用 `build_preflight_service_execution_model_from_dict()` 与 `build_preflight_service_execution_result_model_from_dict()`，不再平行绕 `...execution_models_from_service_execution_payload()` 这条 payload pair 入口。这样单参 `preflight_result` 的 dict inward seam 又少了一层 wrapper；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 payload 侧 `preflight execution-model pair` 入口也收回到 dict seam（2026-06-01，续三）。
- `build_preflight_execution_models_from_service_execution_payload()` 现在先统一合成单参 `preflight_result` payload，再直接复用 `build_preflight_execution_models_from_dict()`，不再先 hydrate typed `service_execution_model` 再平行走 `...from_service_execution_model()`。这样 build-side `preflight` 的 `payload -> dict -> typed pair` inward 边界进一步统一；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 build-side typed `preflight` 的 3 条 `...from_service_execution_model()` wrapper 成片收回到通用 `service_execution_result_model` 主链（2026-06-01，续四）。
- `build_preflight_models_from_service_execution_model()`、`build_preflight_result_model_from_service_execution_model()` 与 `build_preflight_outputs_from_service_execution_model()` 现在都先直接复用 `build_service_execution_result_model_from_service_execution_model()`，再分别进入最近邻的 `preflight_models_from_models()`、`preflight_result_model_from_models()` 与 `preflight_outputs_from_models()`。这样 build-side typed `preflight` inward seam 再少掉一层平行 typed 中转；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 execute-side `preflight` 的相邻 outward wrapper 成片收回到更直接的 typed seam（2026-06-01，续五）。
- `execute_preflight_outputs_from_service_execution_model()` 现在先直接复用 `execute_service_execution_model()` 拿 typed `execution_result_model`，再进入 `build_preflight_outputs_from_models()`；顶层 `execute_preflight_outputs()` 则直接构造 typed `service_execution_model` 后复用这条 helper，不再先绕 `execute_preflight_models()`。这样 execute-side `preflight` 也更贴近“typed result-model 主链 + 最近邻 outward 壳”的边界；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 execute-side 顶层 `preflight` 的 `models/model/dicts` 三层 wrapper 也收回到统一的 `outputs` seam（2026-06-01，续六）。
- `execute_preflight_models()` 现在先构造 typed `service_execution_model`，再直接复用 `execute_preflight_outputs_from_service_execution_model()` 并取前四个 typed 结果；`execute_preflight_model()` 与 `execute_preflight_dicts()` 也都改成直接复用 `execute_preflight_outputs()`，分别取 `result_model` 与最后两个 dict。这样 execute-side 顶层 `preflight` 进一步统一到了单一 total-output seam；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- CI / e2e 修复（2026-05-28）：前端 workflow 的 Playwright 安装阶段已补浏览器缓存，并去掉最重的 `--with-deps` 三浏览器安装；同时把 `frontend-e2e` job 超时从 `35` 分钟放宽到 `45` 分钟，降低冷启动浏览器下载把整条 e2e job 拖死的概率。对应 workflow guard 已补红灯并转绿，`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- CI / e2e 跟进修复（2026-05-28）：在上面的缓存优化之后，前端 smoke matrix 仍暴露出 Playwright 系统依赖缺失问题，因此 workflow 进一步调整为“恢复 `~/.cache/ms-playwright` 缓存 + 单独执行 `npx playwright install-deps chromium firefox webkit` + 再执行 `npx playwright install chromium firefox webkit`”。这样既保留浏览器缓存收益，也补齐 Ubuntu runner 上 `webkit/firefox` 的系统依赖，`bash scripts/test_ci_workflow_guards.sh` 与 `bash scripts/test_ci_e2e_tooling.sh common` 已再次通过。
- `tool-runtime-productionization` 本轮继续把 build-side `preflight` 的 `summary/result/dicts/models` wrapper 成片收回到了统一的 `outputs` seam（2026-06-01，续七）。
- `build_preflight_summary()`、`build_preflight_summary_model_from_dict()/...from_service_execution_model()/...from_models()`、`build_preflight_result_model()`、`...result_model_from_service_execution_model()`、`...result_model_from_dict()`、`...result_model_from_models()` 与 `build_preflight_dicts()` 现在都直接从最近邻 `preflight_outputs*()` helper 取 typed `summary/result` 或最后两个 dict；同时 `build_preflight_models()` 也直接复用 top-level `build_preflight_outputs()` 取前四个 typed 结果。这样 build-side `preflight` 的 outward wrapper 基本都退回成“统一 total-output seam + 最近邻取值壳”；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮还顺手把 payload 侧两条 `preflight` inward wrapper 一起并回了 `outputs_from_service_execution_payload()`（2026-06-01，续八）。
- `build_preflight_execution_models_from_service_execution_payload()` 与 `build_preflight_models_from_service_execution_payload()` 现在都先直接复用 `build_preflight_outputs_from_service_execution_payload()`，再分别取前两个 / 前四个 typed 结果，不再平行保留一条独立的 payload pair / models 总装链。这样 payload inward 入口也开始和 dict / typed 两侧共用同一条 total-output seam；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict 侧 `preflight` 的 `models_from_dict / outputs_from_dict` 总装边界再拉直一层（2026-06-01，续九）。
- `build_preflight_outputs_from_dict()` 现在直接复用 `build_preflight_execution_models_from_dict()` 拿 typed pair，再统一进入 `build_preflight_models_from_models()` 和 `build_preflight_outputs_from_resolved_models()`；`build_preflight_models_from_dict()` 则同步直接复用 `build_preflight_outputs_from_dict()` 并取前四个 typed 结果。这样 dict 侧当前也更接近“`outputs_from_dict` 作为 total-output seam，其余 wrapper 只做最近邻取值”的边界；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build/execute typed `preflight ...from_service_execution_model()` 的相邻平行链也成片收回到了 `outputs_from_service_execution_model()`（2026-06-01，续十）。
- `build_preflight_execution_models_from_service_execution_model()`、`build_preflight_models_from_service_execution_model()` 与 `execute_preflight_models_from_service_execution_model()` 现在都直接复用各自最近邻的 `...preflight_outputs_from_service_execution_model()`，再分别取前两个或前四个 typed 结果，不再各自平行保留一段 `result_model/models_from_models` typed 总装链。这样 `typed service_execution_model -> total outputs` 这层边界也更统一了；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 dict / execute 顶层 `preflight` 的两条相邻 total-output 平行链也一起收回到了统一 seam（2026-06-01，续十一）。
- `build_preflight_execution_models_from_dict()` 现在直接复用 `build_preflight_outputs_from_dict()` 并取前两个 typed 结果，而 `build_preflight_outputs_from_dict()` 自己则直接复用 `build_preflight_service_execution_model_from_dict()` 与 `...service_execution_result_model_from_dict()` 后统一进入 `build_preflight_models_from_models()`；同时顶层 `execute_preflight_models()` 也改成直接复用 `execute_preflight_outputs()` 并取前四个 typed 结果。这样 dict 侧 `outputs_from_dict` 与 execute 顶层 `outputs` 都更明确成了单一 total-output seam；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 typed `service_execution_model -> preflight outputs` 这一组中间层再收薄一层（2026-06-02，续十二）。
- 先把 3 条 focused seam tests 拉成红灯后，`build_preflight_service_execution_result_model_from_service_execution_model()`、`build_preflight_outputs_from_service_execution_model()` 与 `execute_preflight_outputs_from_service_execution_model()` 现已统一退回为直接复用最近邻 `service_execution_outputs_from_service_execution_model()` helper，再分别取 typed `execution_result_model` 或继续进入 `build_preflight_outputs_from_models()`。这样 build / execute 两侧在 `typed service_execution_model -> total outputs` 这一层都更接近单一 seam；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- `tool-runtime-productionization` 本轮继续把 build-side `preflight outputs` 的 raw / payload / dict 三条入口也统一回到了同一条 typed seam（2026-06-02，续十三）。
- 先把 3 条 focused seam tests 拉成红灯后，`build_preflight_outputs()` 现已直接复用 `build_preflight_outputs_from_service_execution_payload()`；`build_preflight_outputs_from_service_execution_payload()` 与 `build_preflight_outputs_from_dict()` 则统一改成先落到 typed `service_execution_model`，再直接复用 `build_preflight_outputs_from_service_execution_model()`。这样 build-side `preflight outputs` 这一层的 raw / payload / dict outward wrapper 都更接近“单次 typed hydration + 单一 total-output seam”的边界；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side `preflight` 的 `dict / payload` wrapper 家族整体退回到了 typed `service_execution_model / result_model` seam（2026-06-02，续十四）。
- 先把 8 条 focused seam tests 拉成红灯后，`build_preflight_summary()` 现已直接复用 `build_preflight_summary_model_from_dict()`；`build_preflight_summary_model_from_dict()`、`build_preflight_execution_models_from_dict()`、`build_preflight_execution_models_from_service_execution_payload()`、`build_preflight_models_from_service_execution_payload()`、`build_preflight_models_from_dict()` 与 `build_preflight_result_model_from_dict()` 都统一改成先做单次 typed `service_execution_model` hydrate，再直接复用对应的 typed helper；`build_preflight_dicts()` 则退回为直接复用 `build_preflight_result_model_from_dict()` 并从 `result_model.summary/result_model` 做 dict 投影。这样 build-side `preflight` 的 dict / payload wrapper 家族又少掉了一整层 `outputs` 中转；外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `303` 条测试；本轮校验同上通过。
- `tool-runtime-productionization` 本轮继续把 build-side `service_execution_model` 侧残留的 typed `preflight` wrapper 成片收回到了更近的 `service_execution_result_model / from_models` seam（2026-06-03，续十五）。
- 先把 4 条 focused seam tests 拉成红灯后，`build_preflight_execution_models_from_service_execution_model()` 现已直接复用 `build_preflight_service_execution_result_model_from_service_execution_model()`；`build_preflight_models_from_service_execution_model()`、`build_preflight_outputs_from_service_execution_model()` 与 `build_preflight_result_model_from_service_execution_model()` 也都统一先拿 typed `service_execution_result_model`，再分别进入最近邻的 `preflight_models_from_models()`、`preflight_outputs_from_models()` 与 `preflight_result_model_from_models()`。这样 typed `service_execution_model -> preflight` 这一段不再平行依赖 total `outputs` wrapper，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `summary_model / result_model / dicts` 这组 wrapper 收回到了更中心的 `preflight_models_*` 主链（2026-06-03，续十六）。
- 先把 6 条 focused seam tests 抬成红灯后，`build_preflight_summary_model_from_dict()`、`build_preflight_summary_model_from_service_execution_model()`、`build_preflight_summary_model_from_models()` 现在都直接从对应的 `preflight_models_*` helper 取第三个 `summary_model`；`build_preflight_result_model_from_dict()` 直接从 `preflight_models_from_dict()` 取第四个 `result_model`；`build_preflight_dicts_from_models()` 与 `build_preflight_dicts()` 也统一改成从 `preflight_models_*` 取 typed `summary/result` 再做 `to_dict()`。这样 build-side `summary/result/dicts` 这族 wrapper 不再平行绕 `result_model` 辅助层，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 execute-side `preflight` 的 `models / model / summary_model / outputs / dicts` 这组相邻 wrapper 收回到了统一的 `models` 主链（2026-06-03，续十七）。
- 先把 5 条 focused seam tests 抬高，其中 4 条先跑成红灯后，`execute_preflight_models_from_service_execution_model()` 现已直接复用 `execute_service_execution_outputs_from_service_execution_model()` 再进入 `preflight_models_from_models()`；`execute_preflight_model_from_service_execution_model()`、`execute_preflight_summary_model_from_service_execution_model()` 与 `execute_preflight_outputs_from_service_execution_model()` 都统一直接从这条 `models` 主链取值；`execute_preflight_dicts()` 也改成直接从 top-level `execute_preflight_models()` 取 typed `summary/result` 再做 `to_dict()`。这样 execute-side 这串 wrapper 不再平行绕 `outputs` 或 `model`，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 top-level execute raw `outputs / summary_model / model` wrapper 也统一收回到了 `execute_preflight_models()` seam（2026-06-03，续十八）。
- 先把 3 条 focused seam tests 抬成红灯后，`execute_preflight_outputs()` 现在直接复用 `execute_preflight_models()` 再进入 `preflight_outputs_from_resolved_models()`；`execute_preflight_summary_model()` 直接从 `execute_preflight_models()` 取第三个 `summary_model`；`execute_preflight_model()` 也直接从 `execute_preflight_models()` 取第四个 `result_model`。这样 top-level execute raw wrapper 也不再平行绕 `...from_service_execution_model()`，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side top-level raw `summary_model / result_model / outputs_from_dict` 也统一收回到了 `preflight_models_from_dict()` 主链（2026-06-03，续十九）。
- 先把 3 条 focused seam tests 抬成红灯后，`build_preflight_summary_model()` 现在直接从 `preflight_models_from_dict()` 取第三个 `summary_model`；`build_preflight_result_model()` 直接从同一条主链取第四个 `result_model`；`build_preflight_outputs_from_dict()` 也改成直接从 `preflight_models_from_dict()` 取 typed `service_execution / execution_result / summary / result` 后进入 `preflight_outputs_from_resolved_models()`。这样 build-side 这组 top-level raw wrapper 也不再平行绕 `result_model_from_dict()` 或 `outputs_from_service_execution_model()`，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `dict -> execution_models/models` 与 typed `service_execution_model -> outputs/result_model` 这一组相邻 wrapper 收回到更中心的 typed 主链（2026-06-03，续二十）。
- 先把 4 条 focused seam tests 抬成红灯后，`build_preflight_execution_models_from_dict()` 现在直接复用 `preflight_service_execution_result_model_from_dict()`；`build_preflight_models_from_dict()` 退回成先走 `preflight_execution_models_from_dict()` 再进入 `preflight_models_from_models()`；同时 `build_preflight_outputs_from_service_execution_model()` 与 `build_preflight_result_model_from_service_execution_model()` 也统一直接复用 `preflight_models_from_service_execution_model()` 取最近邻 typed 结果。这样 build-side 这一段又少掉了一层平行 typed 中转，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build/execute 两侧 raw dict outward family 收回到更少的 `outputs / dicts` seam（2026-06-03，续二十一）。
- 先把 6 条 focused seam tests 抬成红灯后，build 侧 `build_preflight_summary()` 现已直接复用 `build_preflight_dicts()` 取 `summary_dict`，`build_preflight_dicts()` 改成直接复用 `build_preflight_outputs_from_dict()` 取最后两个 dict，`build_preflight_result()` 也直接复用 `build_preflight_outputs()` 取 `result_dict`；execute 侧 `execute_preflight_summary()` 和 `execute_preflight()` 都统一改成直接复用 `execute_preflight_dicts()`，而 `execute_preflight_dicts()` 再统一直接复用 `execute_preflight_outputs()` 取最后两个 dict。这样 raw dict outward family 又少掉了一层 `summary_model / result_model / models` 中转，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build/execute 两侧 typed model outward family 收回到统一的 `outputs*` 主链（2026-06-03，续二十二）。
- 先把 8 条 focused seam tests 抬成红灯后，build 侧 `build_preflight_summary_model()`、`build_preflight_summary_model_from_dict()`、`build_preflight_summary_model_from_service_execution_model()`、`build_preflight_result_model()` 和 `build_preflight_result_model_from_service_execution_model()` 现在都直接从对应的 `preflight_outputs*` 取最近邻 typed `summary_model / result_model`；execute 侧 `execute_preflight_summary_model()`、`execute_preflight_summary_model_from_service_execution_model()`、`execute_preflight_model()` 和 `execute_preflight_model_from_service_execution_model()` 也统一直接改为复用 `execute_preflight_outputs*`。这样 typed model outward family 又少掉了一层 `models*` 中转，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `from_models` outward family 收回到统一的 `outputs_from_models()` 主链（2026-06-03，续二十三）。
- 先把 3 条 focused seam tests 抬成红灯后，`build_preflight_dicts_from_models()` 现在直接复用 `build_preflight_outputs_from_models()` 取最后两个 dict，`build_preflight_summary_model_from_models()` 和 `build_preflight_result_model_from_models()` 也都统一直接从同一条 `outputs_from_models()` 主链取最近邻 typed `summary_model / result_model`。这样 build-side `from_models` outward family 又少掉了一层 `models_from_models()` 中转，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `dict / service_execution_model` 两条 typed seam 翻成以 `outputs` 为中心的主链（2026-06-03，续二十四）。
- 先把 6 条 focused seam tests 抬成红灯后，`build_preflight_outputs_from_dict()` 现在直接以 dict 侧 typed `service_execution_model + execution_result_model` 为起点，再进入 `build_preflight_models_from_models()` 和 `build_preflight_outputs_from_resolved_models()`；`build_preflight_outputs_from_service_execution_model()` 也直接以 typed `service_execution_result_model` 为起点进入 `build_preflight_models_from_models()`。对应地，`build_preflight_execution_models_from_dict()`、`build_preflight_models_from_dict()`、`build_preflight_execution_models_from_service_execution_model()` 和 `build_preflight_models_from_service_execution_model()` 全都统一改成从各自的 `outputs` seam 切片取前两个或前四个 typed 结果。这样 build-side 这两条 typed seam 又少掉了一层平行总装，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side `payload/raw -> outputs` 和 execute-side `outputs/models` 两组相邻 wrapper 一次性收薄（2026-06-03，续二十五）。
- build 侧 `build_preflight_execution_models_from_service_execution_payload()`、`build_preflight_models_from_service_execution_payload()`、`build_preflight_models()` 与 `build_preflight_result_model_from_dict()` 现在都统一直接复用最近邻 `preflight_outputs*` seam 取前两个 / 前四个 typed 结果或第四个 `result_model`，不再平行绕 `from_dict()` / `result_payload()` 链做重复总装。execute 侧 `execute_preflight_outputs_from_service_execution_model()` 现在直接复用 `execute_service_execution_outputs_from_service_execution_model()` 后进入 `build_preflight_outputs_from_models()`；`execute_preflight_models_from_service_execution_model()`、top-level `execute_preflight_outputs()` 与 `execute_preflight_models()` 则统一退回到从各自最近邻 `outputs` seam 切片取值。这样 build/execute 两侧又少掉了一层 wrapper 套 wrapper，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side dict inward `service_execution_result / outputs` 这条链收回到 typed `service_execution_model` 主链（2026-06-03，续二十六）。
- `build_preflight_service_execution_result_model_from_dict()` 现在直接复用 `build_preflight_service_execution_model_from_dict()` 与 `...service_execution_result_model_from_service_execution_model()`，不再平行绕 raw `service_execution_outputs()`；`build_preflight_outputs_from_dict()` 也同步改成直接复用 `build_preflight_service_execution_model_from_dict()` 与 `build_preflight_outputs_from_service_execution_model()`。这样 dict inward 这一段又少掉了一层 payload/result 平行总装，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side payload inward `outputs` 入口也统一收回到 typed `service_execution_model` 主链（2026-06-03，续二十七）。
- `build_preflight_outputs_from_service_execution_payload()` 现在直接复用 `build_configured_tool_registry_provider_service_execution_model_from_dict()` 与 `build_preflight_outputs_from_service_execution_model()`，不再先合成单参 `preflight_result` 再平行绕 `build_preflight_outputs_from_dict()`。这样 payload/dict 两条 inward 入口都已经更早统一落到 typed `service_execution_model -> preflight outputs` 主链，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution` top-level raw `result_model / outputs` 入口也统一收回到 typed `service_execution_model` 主链（2026-06-03，续二十八）。
- `build_service_execution_result_model()`、`build_service_execution_outputs()` 与 `execute_service_execution_outputs()` 现在都统一先做单次 typed `service_execution_model` hydrate，再直接进入最近邻 `...service_execution_outputs_from_service_execution_model()`。这样 `service_execution` 这层 raw 入口也少掉了一层平行总装，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 `service_execution` typed build/execute 主链统一收回到了 `outputs_from_models()` / `outputs_from_service_execution_model()`（2026-06-03，续二十九）。
- `build_service_execution_result_model_from_service_execution_model()` 与 `build_service_execution_outputs_from_service_execution_model()` 现在都先构造 typed `runtime_service_actions_result_model`，再进入 `build_service_execution_outputs_from_models()`；execute 侧则补齐了 `execute_service_execution_outputs_from_models()`，并让 `execute_service_execution_outputs_from_service_execution_model()` 直接复用 `execute_runtime_service_actions_outputs_from_models()` 后进入这条 helper，`execute_service_execution_model()` 也同步退回成只从最近邻 `outputs_from_service_execution_model()` 取 typed `result_model`。这样 build / execute 两侧在 `service_execution` 这层的 typed 总装边界更一致了，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build-side typed `preflight service_execution_model` 家族统一收回到了更近的 `service_execution_result / execution_models / models_from_models` 主链（2026-06-03，续三十）。
- `build_preflight_service_execution_result_model_from_service_execution_model()` 现在直接复用 `build_service_execution_result_model_from_service_execution_model()`；`build_preflight_execution_models_from_service_execution_model()` 则退回成只组合 `service_execution + execution_result_model`。同时 `build_preflight_models_from_service_execution_model()` 与 `build_preflight_outputs_from_service_execution_model()` 都先走 `build_preflight_execution_models_from_service_execution_model()`，再分别进入 `build_preflight_models_from_models()` 与 `build_preflight_outputs_from_models()`；`build_preflight_result_model_from_service_execution_model()` 和 `build_preflight_summary_model_from_service_execution_model()` 也同步退回到更近的 typed seam 取值。这样 build-side typed `preflight` 这一整圈又少掉了一层“先 total outputs 再拆 typed 结果”的中转，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 execute-side typed `preflight service_execution_model` 家族也统一收回到了更近的 `service_execution_outputs / models_from_models / models` 主链（2026-06-03，续三十一）。
- `execute_preflight_models_from_service_execution_model()` 现在直接先复用 `execute_service_execution_outputs_from_service_execution_model()`，再进入 `build_preflight_models_from_models()`；`execute_preflight_outputs_from_service_execution_model()` 与 `execute_preflight_model_from_service_execution_model()` 都同步退回成从 `execute_preflight_models_from_service_execution_model()` 取最近邻 typed 结果，其中前者再进入 `build_preflight_outputs_from_resolved_models()`，后者直接取第四个 `result_model`；`execute_preflight_summary_model_from_service_execution_model()` 则进一步退回成直接复用 `execute_preflight_model_from_service_execution_model()` 再取 `result.summary`。这样 execute-side typed `preflight` 这一整圈也少掉了一层“先 total outputs 再拆 typed 结果”的中转，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续做顶层 outward wrapper 收尾批次，把 build/execute 两侧残留的 `preflight summary_model / result_model / dicts / model / result` 这批 outward helper 一次性压平到了更少的入口（2026-06-03，续三十二）。
- build 侧 `build_preflight_summary_model()` 现在直接复用 `build_preflight_summary_model_from_dict()`；`build_preflight_result_model()` 退回成直接从 `build_preflight_models()` 取第四个 `result_model`；`build_preflight_result()` 则进一步退回成只做 `result_model.to_dict()`；`build_preflight_dicts()` 也同步改成直接从 `build_preflight_result_model_from_dict()` 取 `summary/result` 并做 dict 投影。execute 侧 `execute_preflight_model()` 现在直接从 `execute_preflight_models()` 取第四个 `result_model`；`execute_preflight_summary_model()` 进一步退回成直接复用 `execute_preflight_model()` 再取 `result.summary`；`execute_preflight_dicts()` 则统一从 `execute_preflight_model()` 取 `summary/result` 做最终 dict 投影。这样这批 top-level outward family 也少掉了一层“先 total outputs 再拆 typed 结果”的中转，外部 SSE / trace / e2e 契约保持不变，focused 回归脚本维持 `308` 条测试；`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把 build/execute 顶层剩余的 `preflight summary / outputs / models` outward family 收回到了更近的 typed helper，并保持外部 SSE/trace/e2e 契约不变。
- 当前 build 侧 `build_configured_tool_registry_provider_preflight_summary()` 已退回成直接复用 `...preflight_summary_model()` 再做 `to_dict()`；`...preflight_summary_model_from_dict()` 改成直接复用 `...preflight_result_model_from_dict()`；`...preflight_execution_models_from_dict()` 与 `...preflight_execution_models_from_service_execution_payload()` 都统一先完成单次 typed `service_execution_model` hydration，再进入最近邻 `service_execution_result_model` seam；`...preflight_models_from_dict()` 与 `...preflight_models_from_service_execution_payload()` 则同步退回成直接复用对应的 `execution_models` helper 后进入 `preflight_models_from_models()`。execute 侧 `execute_configured_tool_registry_provider_preflight_models()` 现在直接复用 `...preflight_models_from_service_execution_model()`，`...preflight_outputs()` 则直接从 top-level `models()` 取四个 typed 结果后进入 `preflight_outputs_from_resolved_models()`，`...preflight_summary()` 也退回成直接复用 `...preflight_summary_model()` 再做 `to_dict()`。这样顶层 outward family 基本已经收口到“单次 hydration + 最近邻 typed seam + 单次 dict 投影”的边界，继续收薄的净收益已经明显变小；focused 回归脚本维持 `308` 条测试，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮继续把最后还值得动的 raw outward 入口再收了一层，并保持外部 SSE/trace/e2e 契约不变。
- 当前 build 侧 `build_configured_tool_registry_provider_preflight_models()` 已直接退回成复用 `...preflight_models_from_service_execution_payload()`；`build_configured_tool_registry_provider_preflight_result_model()` 也同步退回成直接从同一条 payload typed seam 取第四个 `result_model`。execute 侧 `execute_configured_tool_registry_provider_preflight()` 则进一步退回成直接复用 `...preflight_model()` 再做 `to_dict()`。这样这块最后几条 raw outward 入口也统一到了更近的 typed seam 上，继续在 `tool_runtime.py` 里搬同族 wrapper 的净收益已经非常有限；focused 回归脚本维持 `308` 条测试，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 继续通过。
- `tool-runtime-productionization` 本轮做的是稳定性复核和小幅可读性收尾，不再继续扩展 wrapper 搬移范围。
- 当前 execute 侧 `execute_configured_tool_registry_provider_preflight_summary_model()` 已整理成“先拿 `result_model`，再进入 `summary_model_from_result_model()`”的直写形式，避免继续堆叠内联调用；与此同时，我也再次复核了剩余 `service_execution / preflight` outward family，结论是继续搬这组同族 wrapper 已经基本只会做职责换位，净收益极低。当前更合理的状态是把这块视为已进入稳定维护边界，而不是继续追求更纯的层级压缩；验证仍维持 `308/308`、`compileall` 通过、`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 这轮又补了一处真实质量清理：`build_configured_tool_registry_provider_preflight_result_payload()` 已经完全退出代码主链、只剩历史文档引用，所以现在已从 [backend/app/services/tool_runtime.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/tool_runtime.py) 删除。当前 build-side raw `service_execution + execution_result` 入口的真实主链是直接做单次 typed `service_execution_model` hydration，再进入 `...preflight_execution_models_from_service_execution_payload()` / `...models_from_service_execution_payload()` / `...outputs_from_service_execution_payload()`；旧文档里提到 `preflight_result_payload` 的历史演进记录应视为已过时实现背景，而不是当前代码状态。
- 这轮把 residual risk audit 里的三类问题一起补掉了：1) build-side 两参 `service_execution + execution_result` payload 入口现在会先 merge `provider / provider_source_name / runtime_artifacts`，再做单次 typed `service_execution_model` hydration，恢复了旧 payload metadata 的兼容语义；2) 旧文档里仍把 `build_configured_tool_registry_provider_preflight_result_payload()` 写成当前主链的地方，现已统一改成明确的历史说明；3) 4 个只被 focused tests 吊着、没有生产调用的 outward helper 已从 [backend/app/services/tool_runtime.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/services/tool_runtime.py) 删除。focused 回归脚本当前为 `305/305`，`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 前端 e2e 回归修复（2026-06-04）：`frontend/e2e/workbench-remote-errors.spec.ts` 中 “remote cancel enters cooldown and recovers send” 已从旧的“每次 remote send 只触发一次 provider 请求”绝对次数断言，调整为“记录首轮 send 的 provider 请求基线，断言 cooldown 期间不再继续增量请求，恢复发送后请求数才会再次增长”的增量语义断言。原因是 provider-assisted planner 上线后，remote 模式一次发送可能先触发 planning provider，再触发主回答 provider；用增量而不是绝对次数校验，才能锁住真正的 cooldown 行为而不误伤新的 planner 能力。校验结果：`cd frontend && npm run lint`、`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py`、`bash scripts/test_ci_e2e_tooling.sh common` 通过；当前因本机未启动 postgres/chroma 依赖，未在本地完整重跑 full Playwright chromium。
- planner/registry 可观测治理（2026-06-04）：本轮把“当前规划到底允许哪些工具、实际走了哪种 planner 路径”从后端内部状态推进成 trace 级信号。`build_tool_plan_artifacts()` 现在会稳定带出 `allowed_tool_names / allowed_tool_labels`，planning trace step meta 也会同时写入 `planning_provider_attempted / planning_provider_used / allowed_tool_names / allowed_tool_labels`；前端 Inspector 与任务详情页 subtitle 则新增 `Rule planner / Planner provider used / Planner fallback` 和 `Allowed tools` 展示。这样当 registry/profile 禁用了 retrieval 或 calc，页面上可以直接看出这次规划的真实边界，而不需要再回头翻后端配置。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`323/323`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings 摘要治理透出（2026-06-04）：本轮把页面级 registry/profile 验收需要的治理信息补进了 `GET /api/settings` / `PUT /api/settings`。后端 summary 现在会稳定返回 `tool_registry_profile`、`tool_registry_provider_source`、`enabled_tool_names` 与 `enabled_tool_labels`，其中 registry 计算基于“用户存储的模型设置 + 全局 runtime tool registry 配置”的合成有效设置；前端模型设置弹窗也已直接展示这三项元数据，后续做 profile/source 切换验收时无需再依赖 trace 或后端日志倒推当前启用工具集。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`324/324`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用户级 registry/profile 设置持久化（2026-06-04）：本轮继续把上面的只读治理信息推进成真正可保存设置。`user_settings` 现已扩展 `tool_registry_profile`、`tool_registry_provider_source` 两列，`StoredSettings`、`/api/settings` 与 sqlite→postgres 迁移脚本也同步支持这两项；模型设置弹窗新增 profile/source 下拉，保存时会和 mode/provider/model/base_url/api_key 一起落库，并继续通过全局 runtime provider sources 计算可选 source 列表。这样默认工具治理不再只是“可观察”，而是已经进入用户级可配置状态；未配置或旧数据仍会自动回退到 runtime 默认。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`324/324`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py backend/scripts/migrate_sqlite_to_postgres.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- registry/profile 校验预览（2026-06-05）：本轮继续把“可配置”推进成“可安全验收”。`POST /api/settings/validate` 现在会把这次待保存设置对应的 `tool_registry_profile`、`tool_registry_provider_source`、`enabled_tool_names`、`enabled_tool_labels` 一起返回；前端模型设置弹窗新增 validate preview 状态，点“校验配置”后会直接在描述区显示本次 profile/source 下真正启用的工具集合，修改表单后旧 preview 会自动清空。同时新增了一条 modal 级前端 e2e，锁定 `planning_only -> validate -> Task Planner` 这条页面验收链。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`325/325`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- registry/profile 运行时验收闭环（2026-06-05）：本轮继续把上面的“可预览”推进成“已真正影响实际执行”的页面级闭环。Workbench Inspector 新增稳定测试锚点 `inspector-trace-feed / trace-card / trace-card-meta`，前端 e2e 也新增“保存 `planning_only` 后直接发送任务”的完整链路，锁定 trace subtitle 中 `Allowed tools` 只能出现 `Task Planner`，且实际 trace 不再泄漏 `Knowledge Retrieval` 或 `calc_eval`。这样模型设置里的 profile 不再只是 summary/preview 可见，而是已经被页面验收覆盖到真实运行时行为。校验结果：`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用户级 registry 设置正式接入 runtime 主链（2026-06-05）：本轮继续把上面的页面验收闭环补成真正的运行时生效链。`chat_execution_service.py` 现在会先读取当前用户保存的 `StoredSettings`，再把其中的 `tool_registry_profile / tool_registry_provider_source` 同时传给 planning 阶段的 `get_configured_tool_registry_provider()` 和工具 preflight；planning trace meta 也同步新增这两个字段，前端 subtitle 现在会直接显示 `Profile / Source / Allowed tools`。与此同时，focused slice 新增了用户级设置进入 planning 与 preflight 主链的回归测试，页面 e2e 也抬高到了校验 trace 中 `Profile planning_only / Source default`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`326/326`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- planner-only / retrieval-only 双侧页面执行验收（2026-06-05）：本轮继续把 registry/profile 的真实执行验收做成成对覆盖。`frontend/e2e/usage-dashboard.spec.ts` 现在新增了 `retrieval_only` 真实执行链，用保存后的 profile 直接发送任务并断言 trace subtitle 中出现 `Profile retrieval_only / Source default / Allowed tools Knowledge Retrieval`，同时实际 trace 中不再出现 `Task Planner` / `calc_eval`。这样 `planning_only` 和 `retrieval_only` 两侧现在都已经被页面级 e2e 锁住，不再只证明“设置已保存”，而是证明“设置会改变真实工具执行集合”。校验结果：`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 三条内建 registry profile 的页面执行验收补齐（2026-06-05）：本轮继续把剩下的 `calculator_only` 也补成同等级真实执行链。`frontend/e2e/usage-dashboard.spec.ts` 现在通过统一的 `saveToolRegistryProfile()` helper 覆盖 `planning_only / retrieval_only / calculator_only` 三种 profile，其中 `calculator_only` 用例会直接保存设置、发送带 `[calc:...]` 的请求，并断言 trace subtitle 中出现 `Profile calculator_only / Source default / Allowed tools calc_eval`，且实际 trace 不再出现 `Task Planner` / `Knowledge Retrieval`。这样三条主要内建治理路径当前都已经有页面级执行回归，不再只靠 summary 或后端 focused test 证明生效。校验结果：`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task detail 回放链补齐 registry/profile 验收（2026-06-05）：本轮继续把上面的 Workbench 运行时验收推进到任务详情页回放链。`frontend/app/tasks/[taskId]/page.tsx` 新增 `task-detail-trace-feed / task-detail-trace-card / task-detail-trace-card-meta` 稳定测试锚点，`frontend/e2e/usage-dashboard.spec.ts` 则补成三条矩阵用例，分别锁定 `planning_only / retrieval_only / calculator_only` 保存后进入 task detail 回放时，trace meta 仍会显示对应的 `Profile / Source / Allowed tools`，且详情页实际 trace 不会泄漏被 profile 禁掉的工具。这样 registry/profile 的真实执行验收已经从当前会话 Inspector 扩展到历史任务详情回放链。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`326/326`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export 摘要层透出 registry/profile 治理边界（2026-06-05）：本轮继续把同一条验收链推进到导出工件。后端 `TaskExportTrace` 现在新增 `governance` 摘要，直接汇总 planning trace 中的 `tool_registry_profile / tool_registry_provider_source / allowed_tool_names / allowed_tool_labels`，对应 Markdown 导出也会在 `Trace Summary` 里显式写出 `Tool Registry Profile / Source / Allowed Tools`，不再要求消费方自己去翻完整 trace step meta。与此同时，focused slice、新增的导出一致性脚本断言，以及任务详情页回放链上的前端矩阵用例都已同步抬高到这一新口径。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`328/328`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py backend/scripts/e2e_export_consistency.py backend/scripts/e2e_main_path.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export 摘要层透出 registry/profile 治理边界（2026-06-05）：本轮继续把同一条治理链补到会话级导出。后端 `SessionExportJsonResponse` 现在新增 `governance` 摘要，汇总该会话内任务 trace 中出现过的 `profiles / provider_sources / allowed_tool_names / allowed_tool_labels`；Markdown 会话导出也新增 `Tool Registry Governance` 小节，直接展示这批任务整体用过的治理边界。与此同时，focused slice 与既有 `e2e_export_consistency.py / e2e_main_path.py` 也都抬高到了新的会话导出口径。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`python3 -m compileall backend/app backend/scripts/test_tool_runtime_slice.py backend/scripts/e2e_export_consistency.py backend/scripts/e2e_main_path.py` 通过，`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 前端 full chromium e2e 稳定性修复（2026-06-05）：针对 `frontend/e2e/usage-dashboard.spec.ts` 在 CI full chromium 中出现的 `calculator_only` 超时与三条 task detail replay 失败，本轮优先修正了同一条脆弱链路：不再在 task center 中盲取 `.first()` 详情按钮，而是发送任务后先捕获 `POST /api/tasks` 返回的 `task_id`，再用 `task-center-keyword-filter` 精确过滤到当前任务后打开详情。同时，三条 profile 执行验收也统一先等待任务创建成功，再进入 trace 断言，避免把“发送成功”和“trace 出现”混成同一个脆弱等待。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过；当前因本地沙箱禁止绑定 localhost 端口，未在本机完整重跑 full Playwright chromium。
- 前端 e2e 解析回归修复（2026-06-05）：上一轮在 `usage-dashboard.spec.ts` 把“发送后先捕获 `task_id`”接入 task detail 回放链时，保留了旧的 `detailPage.url()` 本地变量名，导致块作用域里出现重复 `const taskId` 声明，直接击穿了 Playwright smoke matrix 的 test discovery。本轮已改成 `detailTaskId`，并新增断言要求详情页 URL 中的 id 必须与创建任务返回的 `task_id` 一致。校验结果：`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "calculator-only|task detail replay preserves" --list` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`）。
- 任务详情页治理摘要首屏可见（2026-06-05）：本轮继续把 registry/profile 治理从 trace/export 推进到详情页首屏。`resolveTaskSnapshotSummary()` 现在会直接从 trace steps 中提取 `tool_registry_profile / tool_registry_provider_source / allowed_tool_names / allowed_tool_labels`，任务详情页新增 `task-detail-governance-summary` 区块，直接展示本次任务的 `Profile / Source / Allowed tools`，不再要求用户先翻 trace 卡或导出文件。对应的 `usage-dashboard.spec.ts` task detail replay 验收也同步抬高到必须看到这块治理摘要。校验结果：`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "task detail replay preserves" --list` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 会话级治理摘要进入 Workbench Inspector（2026-06-05）：本轮继续把 registry/profile 治理从 task detail / export 推进到当前主工作台首屏。前端新增 `resolveSessionGovernanceSummary()`，直接从当前会话 `recentTasksScoped` 的 task trace 中汇总 `profiles / provider_sources / allowed_tool_names / allowed_tool_labels`，并在 Inspector Context 面板新增 `inspector-session-governance` 区块，直接展示这场会话实际运行过的 `Profiles / Provider sources / Allowed tools`。与此同时，`usage-dashboard.spec.ts` 的 `planning_only / retrieval_only / calculator_only` 三条页面执行验收也同步抬高到必须看到这块会话级治理摘要，从而把“当前会话 Inspector -> 历史任务详情 -> task export -> session export” 四条展示链进一步对齐。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 命名 provider source 真实页面切换验收（2026-06-05）：本轮继续把 source 治理从 `Source default` 推进到真实 non-default 主链。默认开发环境现在内置 `planning_suite` source：它基于 `planning_only` 治理边界，并把 `task_plan` 的展示标签提升为 `Task Planner Suite`。前端 `usage-dashboard.spec.ts` 新增整链验收，用真实页面保存 `planning_suite` 后，依次校验实时 trace、会话级 Inspector 摘要、任务详情页、task export 和 session export 都同步透出 `Source planning_suite` 与 `Task Planner Suite`，从而把 source 切换也纳入了同一套治理回归主链。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 命名 provider source 小矩阵验收（2026-06-05）：本轮继续把 source 治理从单点 `planning_suite` 扩成小矩阵。默认开发环境现已再内置 `retrieval_suite` source：它基于 `retrieval_only` 治理边界，并把 `task_retrieve` 的展示标签提升为 `Knowledge Retrieval Suite`。前端 `usage-dashboard.spec.ts` 也同步补了两条真实页面回归：一条锁定 `retrieval_suite` 在模型设置校验预览里应直接显示 `retrieval_only / retrieval_suite / Knowledge Retrieval Suite`，另一条锁定保存后从实时 trace、会话级 Inspector、任务详情页、task export 到 session export 的整链治理信息都会统一透出 `Source retrieval_suite` 与 `Knowledge Retrieval Suite`。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 命名 provider source 三向矩阵验收（2026-06-05）：本轮继续把 source 治理从 planner/retrieval 两向小矩阵扩成三向矩阵。默认开发环境现已再内置 `calculator_suite` source：它基于 `calculator_only` 治理边界，并把 `calc_eval` 的展示标签提升为 `Calculator Suite`。前端 `usage-dashboard.spec.ts` 也同步补了两条真实页面回归：一条锁定 `calculator_suite` 在模型设置校验预览里应直接显示 `calculator_only / calculator_suite / Calculator Suite`，另一条锁定保存后从实时 trace、会话级 Inspector、任务详情页、task export 到 session export 的整链治理信息都会统一透出 `Source calculator_suite` 与 `Calculator Suite`。这样当前命名 source 已覆盖 planner / retrieval / calculator 三种主要内建工具方向。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 任务中心治理摘要首屏补齐（2026-06-09）：本轮继续把 registry/profile/provider source 治理从“详情页与导出可见”前移到历史任务列表首屏。前端 `task-center.tsx` 现已直接复用既有 `trace_json -> governance` 解析，在每条任务行下展示 `Profile / Source / Allowed tools` 摘要，并新增稳定测试锚点 `task-center-governance-summary`；`usage-dashboard.spec.ts` 的 `planning_suite` 整链回归也同步抬高到要求任务中心在打开详情前就直接出现 `planning_only / planning_suite / Task Planner Suite`。这样用户在任务中心里就能快速判断某条历史任务运行时采用了哪套治理边界，而无需先打开任务详情。校验结果：`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "saved planning suite source propagates through runtime and export governance" --list` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 任务响应治理摘要与任务中心治理检索（2026-06-09）：本轮继续把这条治理链往产品化和可操作性两端各推进一步。后端 `GET /api/tasks` / `GET /api/tasks/{task_id}` 现在会正式返回 `TaskResponse.governance(profile / provider_source / allowed_tool_names / allowed_tool_labels)`，前端任务中心则优先消费这组结构化字段，并把关键词检索范围扩展到 `Profile / Source / Allowed tools`，因此输入 `planning_suite`、`Task Planner Suite` 这类治理关键词时也能直接筛到对应任务。这样任务中心已经从“只能看最近任务标题和状态”推进到“可以直接按治理边界查任务”，同时旧数据仍保持 `trace_json` 回退兼容。校验结果：`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "saved planning suite source propagates through runtime and export governance" --list` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`331/331`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 任务中心服务端治理检索（2026-06-09）：本轮继续把上面的治理检索从“已加载任务内可筛”推进成“全量任务可搜”。后端 `GET /api/tasks` 现已新增可选 `query` 参数，并在 `prompt / task id / trace_json` 三处做统一关键词过滤，因此 `planning_suite`、`Task Planner Suite` 这类治理关键词现在会真正下沉到服务端分页列表与总数统计；前端 Workbench 则把 task center 搜索词透传到任务列表请求，现有 `planning_suite` 整链 e2e 也同步抬高到必须观察到 `/api/tasks?...&query=planning_suite` 的真实请求。这样任务中心搜索不再只局限于前端已经拉到内存里的那一页任务，而是已经进入正式列表契约。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`333/333`），`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "saved planning suite source propagates through runtime and export governance" --list` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用量榜单治理摘要补齐（2026-06-09）：本轮继续把 registry/profile/provider source 治理从任务列表与详情页推进到 usage dashboard 的 top tasks 榜单。后端 `GET /api/tasks/usage/dashboard` 现已让每条 `top_tasks` row 带上 `source_kind` 与 `governance(profile / provider_source / allowed_tool_names / allowed_tool_labels)`，前端用量弹窗则在任务榜每行下直接展示这组治理摘要；现有 `planning_suite` 整链 e2e 也同步抬高到在 usage dashboard 中必须看到 `planning_only / planning_suite / Task Planner Suite`。这样“实时 trace -> 任务中心 -> 任务详情 -> 导出工件 -> 用量榜单”几条主要展示链现在都已经能直接看见同一套治理边界。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`336/336`），`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "saved planning suite source propagates through runtime and export governance" --list` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用量会话榜治理摘要补齐（2026-06-09）：本轮继续把同一条治理链从 usage dashboard 的任务榜补到会话榜。后端 `GET /api/tasks/usage/dashboard` 现已让每条 `by_session` row 带上聚合治理摘要 `profiles / provider_sources / allowed_tool_names / allowed_tool_labels`，前端用量弹窗也同步在会话榜每行下展示这组信息；既有 `planning_suite` 整链回归因此进一步抬高到要求 usage dashboard 的会话榜和任务榜都能看到 `planning_only / planning_suite / Task Planner Suite`。这样 usage dashboard 内部两类排行现在都已经和任务中心、任务详情、导出工件对齐到同一套治理口径。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`338/338`），`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "saved planning suite source propagates through runtime and export governance" --list` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用量 dashboard 治理精确过滤（2026-06-09）：本轮继续把 usage dashboard 从“可展示治理摘要”推进到“可按治理边界切分数据”。后端 `GET /api/tasks/usage/dashboard` 现已新增可选 `tool_registry_profile` 与 `tool_registry_provider_source` 查询参数，并在聚合 `summary / trend / by_session / top_tasks` 前按 trace 中的治理元数据做精确过滤；这样后续前端无论是看 `planning_only + planning_suite`，还是看其他 profile/source 组合，都能直接基于同一条正式 REST 契约拿到隔离后的用量数据，而不需要在前端拿全量榜单后再二次裁剪。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`340/340`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 用量 dashboard 页面级治理筛选入口（2026-06-09）：本轮继续把上面的后端治理切片能力产品化到 usage modal。前端 `frontend/app/components/workbench/usage-dashboard-modal.tsx` 现已新增 `Governance filters` 行，通过 `usage-governance-profile-filter` 与 `usage-governance-source-filter` 两个稳定测试锚点把 `/api/settings` 提供的 profile/source 选项接入 `GET /api/tasks/usage/dashboard`；新增的 `frontend/e2e/usage-dashboard.spec.ts` 回归也锁定了页面选择 `planning_only + planning_suite` 后，请求必须真实带上 `tool_registry_profile` 与 `tool_registry_provider_source` 参数。这样 usage dashboard 已从“能看治理摘要”推进到“能按治理边界主动切片查看”。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`340/340`），`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "usage dashboard governance filters drive backend request params" --list` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 任务中心页面级治理精确过滤（2026-06-09）：本轮继续把同类治理产品化从 usage dashboard 推进到 task center。后端 `GET /api/tasks` 现已新增可选 `tool_registry_profile` 与 `tool_registry_provider_source` 查询参数，并让分页 `items` 与 `total` 同时按 trace 中的治理元数据做精确过滤；前端 `frontend/app/components/workbench/task-center.tsx` 也同步新增 `task-center-governance-profile-filter` 与 `task-center-governance-source-filter` 两个下拉，直接消费 `/api/settings` 提供的 profile/source 选项并透传到任务列表请求。新增的 `frontend/e2e/usage-dashboard.spec.ts` 回归会锁定页面选择 `planning_only + planning_suite` 后，`/api/tasks` 请求必须真实带上这两个参数。这样 task center 已从“按治理关键词模糊搜”推进到“按治理边界精确筛任务”。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`343/343`），`cd frontend && npm run lint` 通过，`cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --grep "task center governance filters drive backend request params|usage dashboard governance filters drive backend request params" --list` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 任务治理进入 task 表一等持久化字段（2026-06-10）：本轮继续把上面的治理产品化从“接口能筛”推进到“存储层真正落库”。后端 `tasks` 现已新增 `tool_registry_profile`、`tool_registry_provider_source`、`allowed_tool_names_json`、`allowed_tool_labels_json` 四个治理列，并在流式 trace 持久化与任务完成时同步写入；`GET /api/tasks` 的 profile/source 精确过滤也因此从旧的 `trace_json LIKE` 升级成列级精确匹配，`TaskResponse.governance` 则优先消费这组持久化字段，仅在旧任务缺列时回退到 trace 解析。这样 task center、usage dashboard 与后续导出/统计链路不再把治理信息只当作运行日志文本来依赖。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`347/347`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage dashboard 治理读取链切到持久化字段（2026-06-10）：本轮继续把上面的 task 一等字段真正用到聚合链路里。后端 `get_tasks_usage_dashboard()` 现已在查询阶段直接取回 `tool_registry_profile`、`tool_registry_provider_source`、`allowed_tool_names_json`、`allowed_tool_labels_json`，并在聚合 `top_tasks / by_session` 治理摘要以及 profile/source 精确过滤时优先消费这些持久化列，仅对历史旧任务回退到 `trace_json`。这样 usage dashboard 后续无论展示治理摘要还是按治理边界切片，都不再主要依赖 trace 文本解析。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`350/350`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export governance 也切到持久化字段优先（2026-06-10）：本轮继续把同一条治理字段一等化推进到会话导出链。后端 `backend/app/api/routes/sessions.py` 现已在汇总 `SessionExportGovernanceSummary` 时优先读取每条 task 行上的 `tool_registry_profile`、`tool_registry_provider_source`、`allowed_tool_names_json`、`allowed_tool_labels_json`，仅在旧任务缺少这组字段时才回退解析 `trace_json`；因此 session export JSON 顶层 `governance` 与 Markdown 里的 `Tool Registry Governance` 小节，后续都不再主要依赖 trace 文本。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`352/352`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export governance 也切到持久化字段优先（2026-06-11）：本轮继续把治理字段一等化推进到任务导出链。后端 `backend/app/api/routes/tasks.py` 现已在构建 `TaskExportJsonResponse` 时，先尝试从 trace step 提取 governance；若当前任务 trace 为空，则会直接回退到 task 行上的 `tool_registry_profile`、`tool_registry_provider_source`、`allowed_tool_names_json`、`allowed_tool_labels_json`，并继续产出同形状的 export governance 与 Markdown `Trace Summary` 小节。这样 task export JSON/Markdown 也不再主要依赖 trace 文本，和 task list、usage dashboard、session export 站到了同一套持久化底座上。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`354/354`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage top-task 路由层治理回退也切到持久化字段（2026-06-11）：本轮继续把这条治理字段一等化收口到 usage dashboard 的最后一层路由组装逻辑。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_usage_top_task_row()` 现已在缺少聚合层 `governance` dict 时，优先回退到 task 行上的 `tool_registry_profile`、`tool_registry_provider_source`、`allowed_tool_names_json`、`allowed_tool_labels_json`，而不是只尝试解析 `trace_json`。这样 usage dashboard 的 top-task row builder 也和 task list、usage 聚合、task export、session export 统一到了同一套 task 一等字段入口。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`355/355`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export 任务级摘要也补齐 governance（2026-06-11）：本轮继续把导出链一次多收一段，不只保留会话级 governance 总摘要。后端 `backend/app/api/routes/sessions.py` 现已让 `SessionExportTaskSummary` 直接带上每条任务自己的 governance，并在 Markdown 的每个任务段下同步展示 `Tool Registry Profile / Source / Allowed Tools`；数据来源仍然优先走 task 行上的持久化治理列，仅对历史旧任务回退 trace。这样 session export 现在既有会话级治理总览，也有任务级治理明细，两层都站在同一套 task 一等字段底座上。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`355/355`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export 任务级治理明细已接入后端 e2e 主链校验（2026-06-11）：本轮继续把“治理字段一等化”从实现层推进到回归层。后端新增共享 helper `backend/scripts/e2e_export_assertions.py`，并让 `backend/scripts/e2e_export_consistency.py` 与 `backend/scripts/e2e_main_path.py` 都正式校验 `SessionExportJsonResponse.tasks[].governance` 的 `profile / provider_source / allowed_tool_names / allowed_tool_labels`，以及 Markdown `## Tasks` 段中的 `Tool Registry Profile / Source / Allowed Tools` 行。这样 session export 的任务级治理明细不再只靠 focused slice test 兜底，而是进入了后端 export e2e 的常规主链。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`357/357`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export 治理校验也收口到共享 e2e 断言（2026-06-11）：本轮继续把 export governance 的回归护栏做成统一口径。后端扩展了 `backend/scripts/e2e_export_assertions.py`，新增 task export governance 的 JSON/Markdown 断言，并让 `backend/scripts/e2e_export_consistency.py` 与 `backend/scripts/e2e_main_path.py` 不再各自手写 `trace.governance` 的校验，而是统一复用这组 helper 来验证 `profile / provider_source / allowed_tool_names / allowed_tool_labels` 以及 Markdown `Trace Summary` 里的 `Tool Registry Profile / Source / Allowed Tools`。这样 task/session 两条导出治理校验都进入了同一套共享 e2e 护栏，后续继续推进治理字段一等化时回归口径也更稳。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`359/359`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task 路由与 session export 的治理列解析也收口到 persistence service 共享入口（2026-06-11）：本轮继续把治理字段一等化从“回归口径统一”往“实现入口统一”再收一刀。后端 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 现在都显式复用 `backend/app/services/chat_persistence_service.py` 里的共享 task-governance row parser，不再各自维护一份 `tool_registry_profile / tool_registry_provider_source / allowed_tool_*_json` 的列级解析逻辑。这样 task response、usage top-task fallback 与 session export task/session governance summary 在读取持久化治理列时已经站到同一个 service 入口上，后续继续演进治理字段时漂移风险更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`361/361`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session 的 trace governance 解析也收口到 persistence service 共享入口（2026-06-11）：本轮继续把治理单源化再往里推进一层。后端 `backend/app/api/routes/tasks.py` 的 `trace_json -> TaskGovernanceSummary / TaskExportGovernance` 转换，以及 `backend/app/api/routes/sessions.py` 的 parsed trace steps -> session/task governance 转换，现在都改为复用 `backend/app/services/chat_persistence_service.py` 的共享 trace-governance parser，而不再各自重复遍历 `meta.tool_registry_* / allowed_tool_*`。这样持久化列入口和 trace 入口两条治理读取链都开始站到同一个 service 源头上，后续继续演进治理字段时 task/session/export 的解析漂移风险会再降一层。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`363/363`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export 的会话级 governance summary 也收口到共享 merge helper（2026-06-11）：本轮继续把治理单源化从“单条 task 解析”推进到“会话级汇总聚合”。后端 `backend/app/api/routes/sessions.py` 的 `_collect_session_governance_summary()` 现在不再自己维护一套 `profiles / provider_sources / allowed_tool_*` 的 set 聚合逻辑，而是改为复用 `backend/app/services/chat_persistence_service.py` 的 `_merge_session_governance_summary()`。这样 session export 的会话级治理总览，和 usage dashboard 的会话级治理聚合已经站到同一个 merge 入口上，后续继续演进 governance summary 形状时，两条链的漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`364/364`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- governance builder 单源化继续收口（2026-06-11）：本轮继续把默认工具去 mock 化主线往“路由层只保留最近邻组装壳”推进。后端 `backend/app/api/routes/tasks.py` 现已把 task response、task export fallback 与 usage top-task persisted-row fallback 的 `dict -> TaskGovernanceSummary / TaskExportGovernance` 转换统一收口到共享 builder；`backend/app/api/routes/sessions.py` 也把 session export 的 task-level / session-level governance summary 统一收口到共享 builder，不再在 `_build_session_task_summary()` 与 `_collect_session_governance_summary()` 内联重复组装 Pydantic model。这样 task list、usage dashboard、task export、session export 四条治理展示链在“持久化列或聚合 dict -> outward governance model”这一层也开始真正单源化，同时外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`367/367`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage by-session governance builder 也收口到共享 helper（2026-06-11）：本轮继续把同一条治理单源化主线从 task response / task export / session export 再推进到 usage dashboard 的会话榜路由层。后端 `backend/app/api/routes/tasks.py` 新增 `_build_task_usage_session_governance_summary_from_dict()`，并让 `_build_task_usage_by_session_row()` 不再直接 `TaskUsageSessionGovernanceSummary(**governance_raw)`，而是统一退回到最近邻 builder。这样 usage dashboard 的 `top_tasks[].governance` 与 `by_session[].governance` 两条 outward 组装链现在都开始站到共享 builder 之上，task/usage/session/export 四条治理展示链的路由层重复逻辑又少了一层。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`368/368`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- governance dict 清洗也收口到 persistence service shared normalizer（2026-06-11）：本轮继续把默认工具去 mock 化主线从“共享 builder”再推进到“共享 normalizer”。后端 `backend/app/services/chat_persistence_service.py` 新增 `_normalize_task_governance_dict()` 与 `_normalize_session_governance_summary_dict()`，并让 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 里的 task/session/usage/export governance builder 都统一先复用这两个 helper，再组装 outward Pydantic model。这样 `profile / provider_source / allowed_tool_*` 与 `profiles / provider_sources / allowed_tool_*` 两类 dict 清洗规则不再散落在多个 route builder 中，task、usage、session export、task export 四条治理展示链在“raw dict -> normalized dict -> outward model”这一层也开始真正单源化。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`370/370`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- trace / task-row governance extractor 也退回到 shared normalizer（2026-06-11）：本轮继续把治理单源化从 route builder 再往 service 内部推进一层。后端 `backend/app/services/chat_persistence_service.py` 的 `_extract_task_governance_from_trace_steps()` 与 `_extract_task_governance_from_task_row()` 现在都不再手工返回原始治理 dict，而是统一先走 `_normalize_task_governance_dict()`；这样 trace 入口、持久化列入口、route builder 入口三层现在已经开始共用同一套 task governance dict 清洗规则。对应 focused tests 也新增 2 条回归，锁定这两个 extractor 都必须复用 shared normalizer。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`372/372`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session governance merger 也退回到 shared normalizer（2026-06-11）：本轮继续把治理单源化从 extractor 再推进到会话级聚合层。后端 `backend/app/services/chat_persistence_service.py` 的 `_merge_session_governance_summary()` 现在会先统一复用 `_normalize_task_governance_dict()` 与 `_normalize_session_governance_summary_dict()`，再做 `profiles / provider_sources / allowed_tool_*` 的聚合，而不再对 `current` 与 `task_governance` 两个输入各自隐式假设字段已清洗完毕。这样 session governance 的输入规范化、聚合与 outward builder 三层也开始站到同一套 normalizer 上，task/usage/session export 几条治理链的会话级摘要口径进一步收稳。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`374/374`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task list 治理筛选 route 入口也收口到 shared normalizer（2026-06-11）：本轮继续把治理单源化从“治理摘要构建链”推进到“治理筛选入口链”。后端 `backend/app/api/routes/tasks.py` 的 `GET /api/tasks` 现在不再对 `tool_registry_profile / tool_registry_provider_source` 各自手工 `.strip()`，而是统一复用 `backend/app/services/chat_persistence_service.py` 的 `_normalize_governance_filter()`，再把结果透传给 `list_tasks()` 与 `count_tasks()`。这样 task center 列表与 usage dashboard 在 profile/source 结构化筛选上已经开始站到同一套 filter normalizer 上，后续继续推进 registry/profile/provider-source 治理产品化时，任务列表和用量看板之间的筛选口径漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`375/375`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task list SQL governance filter 也收口到 shared normalizer（2026-06-12）：本轮继续把治理单源化从 route filter 入口再往 service SQL 过滤层推进一刀。后端 `backend/app/services/chat_persistence_service.py` 的 `_build_task_governance_filter_clause()` 现在不再对 `tool_registry_profile_filter / tool_registry_provider_source_filter` 自己做 `strip().lower()`，而是统一复用 `_normalize_governance_filter()` 后再组装 `list_tasks()` / `count_tasks()` 的 persisted-column 精确过滤条件。这样 task center 列表在 route 层和 service 层两段 profile/source 过滤链都开始站到同一套 filter normalizer 上，并与 usage dashboard 的治理筛选口径进一步对齐。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`376/376`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage dashboard task-match compare 侧也收口到 shared normalizer（2026-06-12）：本轮继续把治理单源化从 task list SQL filter 再推进到 usage dashboard 的过滤比较层。后端 `backend/app/services/chat_persistence_service.py` 的 `get_tasks_usage_dashboard()` 现在在把 task governance 与 `tool_registry_profile_filter / tool_registry_provider_source_filter` 做比较时，不再对 task 侧值手工 `strip().lower()`，而是统一复用 `_normalize_governance_filter()`；这样 usage dashboard 的 filter 左右两侧都开始站到同一套 normalizer 上，并和 task center 列表的治理筛选比较口径进一步对齐。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`377/377`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings profile/source 归一化入口也收口到 shared normalizer（2026-06-12）：本轮继续把治理单源化从任务与用量查询链推进到设置保存入口。后端 `backend/app/api/routes/settings.py` 的 `SettingsUpdateRequest` 现在不再对 `tool_registry_profile / tool_registry_provider_source` 各自手工 `strip().lower()`，而是统一复用 `backend/app/services/chat_persistence_service.py` 的 `_normalize_governance_filter()`。这样 settings 保存入口、task center 列表和 usage dashboard 三条 profile/source 治理入口已经开始站到同一套归一化规则上，后续继续推进 registry/profile/provider-source 治理产品化时，设置态与查询态之间的口径漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`378/378`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- provider source detail 的 base_profile 也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 settings 保存入口推进到 settings 展示侧。后端 `backend/app/api/routes/settings.py` 的 `_build_tool_registry_provider_source_option_details()` 现在不再对 `source_spec.profile` 手工 `strip().lower()` 后直接写入 `base_profile`，而是统一通过 clone settings 后复用 `get_tool_registry_profile_name_from_settings()`。这样 settings summary / validate preview 里 provider source detail 的 `base_profile`，也开始和 settings 保存入口、task center、usage dashboard 共用同一套 profile 归一化口径，后续继续抬高模型设置说明回归时，展示态与保存态之间的口径漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`379/379`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- provider source adapter 的 profile 入口也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 settings 说明链推进到 tool runtime provider-source adapter 链。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_provider_adapter()` 现在不再对 `spec["profile"]` 手工 `strip().lower()`，而是统一通过临时 settings 对象复用 `get_tool_registry_profile_name_from_settings()`。这样 provider source 配置从 settings 保存、settings 展示到 runtime adapter 应用三段已经开始真正共用同一套 profile 归一化规则，后续继续推进 registry/profile/provider-source 治理产品化时，配置态与运行态之间的口径漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`380/380`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- loader adapter 的 profile 入口也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 provider adapter 再推进到 sibling 的 loader adapter 链。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_loader_adapter()` 现在不再对 `spec.get("profile", implicit_profile_name)` 手工 `strip().lower()`，而是统一通过临时 settings 对象复用 `get_tool_registry_profile_name_from_settings()`。这样 loader/provider 两条 runtime adapter 链现在都开始和 settings 保存入口、settings 展示侧站到同一套 profile 归一化规则上，后续继续推进 registry/profile/provider-source 治理产品化时，配置态、说明态和运行态三段之间的口径漂移风险会再低一层。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`381/381`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- loader factory profile hint 入口也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 loader adapter 的显式 `profile` 再推进到它的隐式 `profile_name_hint` 来源。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_loader_adapter()` 现在不再对 `loader_factory._tool_registry_profile_name` 手工 `strip().lower()` 后塞进 `implicit_profile_name`，而是统一通过临时 settings 对象复用 `get_tool_registry_profile_name_from_settings()`。这样 loader adapter 的显式 profile 与 factory hint profile 两个入口都开始和 settings 保存、settings 展示、provider adapter 站到同一套 profile 归一化规则上，runtime adapter 链的口径进一步收稳。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`382/382`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- provider factory profile hint 入口也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 loader factory hint 推进到对称的 provider factory hint 链。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_provider_adapter()` 现在不再忽略 `provider_factory._tool_registry_profile_name` 对最终 profile 选择的影响，而是统一通过临时 settings 对象复用 `get_tool_registry_profile_name_from_settings()`，并将结果作为 `spec["profile"]` 缺省值。这样 provider adapter 的显式 profile 与 factory hint profile 两个入口，也开始和 settings 保存、settings 展示、loader adapter 站到同一套 profile 归一化规则上，runtime adapter 链的多来源口径进一步对齐。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`383/383`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- registry file manifest 的 profile 入口也收口到 shared profile helper（2026-06-12）：本轮继续把治理单源化从 runtime adapter 链推进到 registry-file manifest 链。后端 `backend/app/services/tool_runtime.py` 的 `_build_tool_registry_from_file_registry()` 现在不再对 manifest 里的 `payload["profile"]` 手工 `strip().lower()`，而是统一通过临时 settings 对象复用 `get_tool_registry_profile_name_from_settings()`。这样 settings 保存、settings 展示、loader/provider adapter、registry-file manifest 这几条 profile 入口已经开始真正站到同一套归一化规则上，后续继续推进 registry/profile/provider-source 治理产品化时，文件配置态与运行态之间的口径漂移风险会更低。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`384/384`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- registry file manifest 的 `registry_sources` 入口也收口到 shared provider-source helper（2026-06-12）：本轮继续把同一条单源化主线从 manifest `profile` 推进到 manifest `registry_sources`。后端 `backend/app/services/tool_runtime.py` 的 `_build_tool_registry_from_file_registry()` 现在不再对每个 `child_registry_source` 手工 `strip().lower()`，而是统一通过临时 settings 对象复用 `get_tool_registry_provider_source_name_from_settings()`；对应 focused test 也新增一条红灯回归，锁定 file-registry manifest 在 source 解析路径下必须走这条 shared helper。这样 registry-file manifest 的 profile/source 两类运行态入口都开始和 settings 保存、settings 说明、task/usage filter 链共用统一归一化规则，继续降低文件配置态与运行态多入口之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`385/385`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings-backed provider source key 入口也收口到 shared provider-source helper（2026-06-12）：本轮继续把 runtime source 单源化从 file-manifest `registry_sources` 推进到 settings-backed named source 定义本身。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_provider_sources_from_settings_artifacts()` 现在不再对 `source_specs` 里的 `source_name` key 手工 `strip().lower()`，而是统一通过临时 settings 对象复用 `get_tool_registry_provider_source_name_from_settings()`；对应 focused test 也新增一条红灯回归，锁定 settings-backed provider source key 在注册命名 source 时必须走这条 shared helper。这样 settings 保存、settings 说明、settings-backed source 定义、file-manifest source 引用与 task/usage filter 链几条 source 入口已经更完整地共用同一套归一化规则，继续降低配置态与运行态多入口之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`386/386`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- named provider/loader diagnostics reference 入口也收口到 shared reference normalizer（2026-06-12）：本轮继续把 runtime registry 单源化从 source key / source 引用推进到 diagnostics 关联链。后端 `backend/app/services/tool_runtime.py` 新增 shared `_normalize_named_tool_registry_component_name()`，并让 named loader/provider/factory resolver 以及 loaders/providers/provider-sources 三条 artifacts 链里对 `provider` / `loader` reference 的 diagnostics lookup 一起改为复用这条 helper，不再各自手工 `strip().lower()`；对应 focused test 也新增一条红灯回归，锁定 source artifacts 在承接 named provider diagnostics 时必须走这条 shared normalizer。这样 settings-backed source 定义、file-manifest source 引用与 named provider/loader diagnostics 关联几条 runtime reference 入口已经开始更完整地共用同一套归一化规则，继续降低配置态与运行态 sibling 链之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`387/387`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- named factory alias 注册入口也收口到 shared reference normalizer（2026-06-12）：本轮继续把 runtime registry 单源化从 diagnostics 关联链推进到 factory alias 注册链。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_loader_factories_from_settings()` 与 `build_tool_registry_provider_factories_from_settings()` 现在不再对 `factory_name` 和 profile-like `target_name` 各自手工 `strip().lower()`，而是统一复用 `_normalize_named_tool_registry_component_name()`；对应 focused test 也新增一条红灯回归，锁定 loader factory alias 的 key 注册必须走这条 shared normalizer。这样 named loader/provider/factory 的 resolver、diagnostics 关联和 factory alias 注册几条 sibling 链已经开始更完整地共用同一套 reference 归一化规则，继续降低配置态与运行态命名入口之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`388/388`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- adapter named reference lookup 入口也收口到 shared reference normalizer（2026-06-12）：本轮继续把 runtime registry 单源化从 factory alias 注册链推进到 adapter 命名引用链。后端 `backend/app/services/tool_runtime.py` 的 `build_tool_registry_loader_adapter()` 与 `build_tool_registry_provider_adapter()` 现在不再对 `loader_factory`、`provider_factory` 和 fallback `loader` lookup 各自手工 `strip().lower()`，而是统一复用 `_normalize_named_tool_registry_component_name()`；对应 focused tests 也新增两条红灯回归，分别锁定 loader adapter 的 `loader_factory` 引用和 provider adapter 的 named `loader` 引用都必须走这条 shared normalizer。这样 named loader/provider/factory 的 resolver、diagnostics 关联、factory alias 注册和 adapter lookup 几条 sibling 命名链已经开始更完整地共用同一套 reference 归一化规则，继续降低配置态与运行态命名入口之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`390/390`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- profile/source shared helper 入口也收口到同一条 reference normalizer（2026-06-12）：本轮继续把 runtime registry 单源化从 adapter 命名引用链推进到 shared helper 自身。后端 `backend/app/services/tool_runtime.py` 的 `get_tool_registry_profile_name_from_settings()` 与 `get_tool_registry_provider_source_name_from_settings()` 现在不再各自手工 `strip().lower()`，而是统一退回到 `_normalize_named_tool_registry_component_name()` 再做 `default` fallback；对应 focused test 也新增一条红灯回归，锁定 profile/source 两条 shared helper 都必须复用这条 normalizer。这样 named loader/provider/factory 的 resolver、diagnostics 关联、factory alias 注册、adapter lookup，以及 profile/source 两条 shared helper 现在已经更完整地共用同一套 reference 归一化规则，继续降低配置态与运行态主干入口之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`391/391`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings source detail 的 raw spec lookup 也收口到 shared source helper（2026-06-12）：本轮继续把治理单源化从 tool runtime 主干推进回 settings 展示链的一个残余分叉。后端 `backend/app/api/routes/settings.py` 的 `_build_tool_registry_provider_source_option_details()` 现在会先把 `tool_registry_provider_sources_json` 里的 raw source specs 统一按 `get_tool_registry_provider_source_name_from_settings()` 规范化成 map，再按 normalized `source_name` 查 `source_spec`，不再直接 `source_specs.get(source_name)`；对应 focused test 也新增一条红灯回归，锁定 source detail 在 raw spec key 带空白/大小写差异时，也必须复用这条 shared source helper 才能拿到正确 `base_profile`。这样 settings 保存入口、settings 说明链、settings-backed source 定义和 runtime source 解析几条 source 主链已经更完整地共用同一套归一化规则，继续降低展示态与运行态之间的口径漂移风险。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`392/392`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- source/profile 结构化说明产品化（2026-06-05）：本轮继续把 source/profile 从“能切换、能验收”推进成“页面上可解释”。后端 `/api/settings` summary 现在新增 `available_tool_registry_profile_details` 与 `available_tool_registry_provider_source_details` 两组结构化说明，分别提供 profile 对应的启用工具集合，以及 source 对应的基础 profile 与工具标签集合；前端模型设置弹窗也新增了 `model-settings-selected-profile-summary` 与 `model-settings-selected-source-summary` 两块即时说明，用户一旦切换 profile/source，就能直接看到“当前 profile 启用什么”“当前 source 默认基于哪个 profile、暴露哪些工具”。对应的 `usage-dashboard.spec.ts` validate preview 回归也已抬高到锁定这两块说明文本。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- validate/detail 说明口径统一（2026-06-05）：本轮继续把上面的“可解释”链补完整，不再让 `validate` 和 `summary` 走两套解释数据。后端 `SettingsValidateResponse` 现在也会和 `SettingsSummaryResponse` 一样返回 `available_tool_registry_profile_details` 与 `available_tool_registry_provider_source_details`，前端模型设置弹窗的即时说明因此可以在“仅做校验、尚未保存”时也直接复用同一套结构化 detail，而不是只依赖 `GET /api/settings` 的静态摘要。对应 focused test 已抬高到锁定 validate 响应包含这两组 detail，前端 validate preview discover 也继续覆盖 `planning_only / retrieval_suite / calculator_suite` 三条说明链。校验结果：`cd frontend && npm run lint` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`330/330`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task-level governance outward clone 单源化（2026-06-15）：本轮继续把治理单源化从 session-level outward copy 推进到 task-level outward copy。后端 `backend/app/services/chat_persistence_service.py` 新增 shared `_clone_task_governance_dict()`，并让 `backend/app/api/routes/tasks.py` 的 `_build_task_governance_summary_from_dict()` 与 `backend/app/api/routes/sessions.py` 的 `_build_session_export_task_governance_summary_from_dict()` 一起退回到这条 helper；`sessions.py` 里 task governance 的 task-row / trace-steps / trace-json fallback clone 也同步复用同一入口。这样 task list、usage top-task、session export task summary 与 session export fallback 在 task-level governance outward 形状上开始共用同一套 clone 主干，继续降低 sibling route copy、展示态与导出态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`406/406`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export governance outward clone 单源化（2026-06-15）：本轮继续把治理单源化从通用 task-level outward copy 推进到 task export 这条 sibling 导出链。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_export_governance_from_dict()` 现在也退回到 shared `_clone_task_governance_dict()`，不再自己从 normalized dict 手工拷贝 `profile / provider_source / allowed_tool_names / allowed_tool_labels`。这样 task response、usage top-task、session export task summary、session export fallback 与 task export trace governance，在 task-level governance outward 形状上已经更完整地共用同一套 clone 主干，继续降低 sibling route copy、展示态与导出态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`407/407`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export builder 复用 task summary builder（2026-06-15）：本轮继续把治理单源化从 task export clone 主干推进到 route 最近邻 builder 层。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_export_governance_from_dict()` 现在不只复用 shared `_clone_task_governance_dict()`，还会先统一退回 `_build_task_governance_summary_from_dict()`，再由 `TaskGovernanceSummary` 稳定转换成 `TaskExportGovernance`。这样 task export 与 task response 两条 sibling task-level governance builder 链已经开始共用同一条 route-level summary builder 主干，继续降低 export/response 两侧最近邻组装逻辑的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`408/408`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export persisted fallback 也复用 summary helper（2026-06-15）：本轮继续把治理单源化从 task export route builder 推进到 payload fallback 中转层。后端 `backend/app/api/routes/tasks.py` 新增 `_build_task_export_governance_from_summary()`，并让 `_build_task_export_governance_from_dict()` 与 `_build_task_export_payload()` 在 persisted governance fallback 路径下都统一退回到这条 helper，而不再走 `TaskGovernanceSummary -> dict -> TaskExportGovernance` 的重复中转。这样 task export 的 trace path、persisted fallback path 与 task response summary builder 已经更完整地站到同一条 summary/export 主干上，继续降低 sibling fallback、导出态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`409/409`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export trace path 也复用 trace-summary helper（2026-06-15）：本轮继续把治理单源化从 task export 的 persisted fallback 推进到 trace path 本身。后端 `backend/app/api/routes/tasks.py` 新增 `_collect_task_governance_summary_from_trace_steps()`，并让 `_collect_trace_governance_export()` 不再直接走 trace-step dict 到 export builder 的中转，而是统一退回到“trace steps -> TaskGovernanceSummary -> TaskExportGovernance”这条主干。这样 task export 的 trace path、persisted fallback path 与 task response summary builder 已经更完整地站到同一套 summary/export 主干上，继续降低 sibling trace/fallback、导出态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`410/410`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export task summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 task export 主干回收一刀到 session export task summary 这条 sibling 链。后端 `backend/app/api/routes/sessions.py` 新增 `_build_session_export_task_governance_summary_from_clone()`，并让 `_build_session_export_task_governance_summary_from_dict()` 与 `_build_session_task_summary()` 一起退回到这条 helper；这样 session export task summary 在拿到已经 clone 过的 task-level governance dict 时，不再重复走一遍 dict builder。现在 task export、task response、session export task summary 与 session export fallback 在 task-level governance outward 组装上更完整地共用同一类 clone/summary 主干，继续降低 sibling builder、导出态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`411/411`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export governance summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 session export task summary 推进到 session-level governance summary 这条 sibling 链。后端 `backend/app/api/routes/sessions.py` 新增 `_build_session_export_governance_summary_from_clone()`，并让 `_build_session_export_governance_summary_from_dict()` 与 `_collect_session_governance_summary()` 一起退回到这条 helper；这样 session export 在 merge 出 session-level governance summary dict 之后，也不再重复走一遍 dict builder。现在 session export task summary、session export 顶层 governance summary、task export 和 task response 几条治理展示链，在 clone/summary 主干上又对齐了一段，继续降低 sibling builder、聚合态与导出态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`412/412`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage by-session governance summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 session export 的 session-level summary 回收一刀到 usage by-session 这条 sibling 链。后端 `backend/app/api/routes/tasks.py` 新增 `_build_task_usage_session_governance_summary_from_clone()`，并让 `_build_task_usage_session_governance_summary_from_dict()` 与 `_build_task_usage_by_session_row()` 一起退回到这条 helper；这样 usage dashboard 在拿到已经成形的 session-level governance summary dict 时，也不再重复走一遍 dict builder。现在 usage by-session、session export 顶层 governance summary，以及 task/session/export 几条治理展示链，在 clone/summary 主干上又对齐了一段，继续降低 sibling builder、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`413/413`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage top-task governance summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 usage by-session 这条 sibling 链再回收一刀到 usage top-task。后端 `backend/app/api/routes/tasks.py` 新增 `_build_task_governance_summary_from_clone()`，并让 `_build_task_governance_summary_from_dict()` 与 `_build_task_usage_top_task_row()` 一起退回到这条 helper；这样 usage dashboard 的 top-task 榜单在拿到已经成形的 task-level governance dict 时，也不再重复走一遍 dict builder。现在 usage top-task、usage by-session、session export 与 task export/response 几条治理展示链，在 clone/summary 主干上又对齐了一段，继续降低 sibling builder、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`414/414`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task trace governance summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 usage top-task 再推进到 task trace 这两条 sibling 收集链。后端 `backend/app/api/routes/tasks.py` 的 `_collect_task_governance_from_trace_json()` 与 `_collect_task_governance_summary_from_trace_steps()` 现在都会在拿到 persistence service 已经成形的 task-level governance dict 后，直接退回 `_build_task_governance_summary_from_clone()`，不再重复走一遍 dict builder。这样 task trace summary、usage top-task、task export trace path 与 task response 几条治理展示链，在 clone/summary 主干上又对齐了一段，继续降低 trace-path sibling builder、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`416/416`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task row governance summary 也复用 clone-level helper（2026-06-15）：本轮继续把治理单源化从 task trace sibling 链再推进到 task row parser 这条主干入口。后端 `backend/app/api/routes/tasks.py` 的 `_collect_task_governance_from_task()` 现在会在拿到 persistence service 已经成形的 task-level governance dict 后，直接退回 `_build_task_governance_summary_from_clone()`，不再重复走一遍 dict builder。这样 task row summary、task trace summary、usage top-task、task export trace path 与 task response 几条治理展示链，在 clone/summary 主干上又对齐了一段，继续降低 row/trace sibling builder、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`417/417`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session task trace-json fallback 也复用共享 helper（2026-06-15）：本轮继续把治理单源化从 task row 主干推进到 `sessions.py` 的 trace-json fallback 这条 sibling 链。后端 `backend/app/api/routes/sessions.py` 新增 `_collect_task_governance_from_trace_json()`，并让 `_collect_task_governance_from_task()` 在持久化治理列缺失、且 `parsed_steps` 为空时，统一退回到这条 helper，不再内联 `service parser + clone`。这样 session task trace-json fallback、session task trace-steps fallback，以及 task/session/export 几条治理收集链，在 helper 主干上又对齐了一段，继续降低 sibling fallback、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`418/418`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session task governance clone 也复用本地 shared helper（2026-06-15）：本轮继续把治理单源化从 `sessions.py` 的 trace-json fallback 再推进到 task-row / trace-steps / trace-json 三条 sibling clone 链。后端 `backend/app/api/routes/sessions.py` 新增本地 `_clone_task_governance()`，并让 `_collect_task_governance_from_task_row()`、`_collect_task_governance_from_trace_steps()`、`_collect_task_governance_from_trace_json()` 一起退回到这条 helper，不再各自直接调 `chat_persistence_service._clone_task_governance_dict()`。这样 session task governance 的 row/trace-steps/trace-json 三条收集链，在 clone 主干上又对齐了一段，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`419/419`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session governance summary clone 也复用本地 shared helper（2026-06-15）：本轮继续把治理单源化从 session task clone 主干推进到 session-level summary 这一层。后端 `backend/app/api/routes/sessions.py` 新增本地 `_clone_session_governance_summary()`，并让 `_build_session_export_governance_summary_from_dict()` 与 `_collect_session_governance_summary()` 一起退回到这条 helper，不再分别直接调 `chat_persistence_service._clone_session_governance_summary_dict()` 或把 merge 结果直接喂给 clone builder。这样 session governance summary 的 dict-builder 链与 collect-summary 链，在 clone/summary 主干上又对齐了一段，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`420/420`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task governance clone 也复用本地 shared helper（2026-06-15）：本轮继续把治理单源化从 `sessions.py` 的 clone 主干回收一刀到 `tasks.py` 的 task-level governance 收集链。后端 `backend/app/api/routes/tasks.py` 新增本地 `_clone_task_governance()`，并让 `_build_task_governance_summary_from_dict()`、`_collect_task_governance_from_task()`、`_collect_task_governance_from_trace_json()`、`_collect_task_governance_summary_from_trace_steps()` 一起退回到这条 helper，不再分别直接调 `chat_persistence_service._clone_task_governance_dict()` 或把 service parser 结果直接喂给 clone builder。这样 task row、task trace、task export trace path、usage top-task 与 task response 几条治理收集链，在 clone/summary 主干上又对齐了一段，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`421/421`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task usage session governance clone 也复用本地 shared helper（2026-06-15）：本轮继续把治理单源化从 `tasks.py` 的 task-level clone 主干推进到 usage session-level 这一层。后端 `backend/app/api/routes/tasks.py` 新增本地 `_clone_session_governance_summary()`，并让 `_build_task_usage_session_governance_summary_from_dict()` 退回到这条 helper，不再直接调 `chat_persistence_service._clone_session_governance_summary_dict()`。这样 usage by-session governance 的 dict-builder 链，也开始和 `sessions.py` 的 session-level governance summary 一起共用同类本地 clone 入口，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`422/422`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task usage by-session route builder 也复用本地 clone helper（2026-06-15）：本轮继续把治理单源化从 usage session-level dict-builder 再推进到 route-level row builder。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_usage_by_session_row()` 现在会先对 `row["governance"]` 走 `_clone_session_governance_summary()`，再进入 `_build_task_usage_session_governance_summary_from_clone()`，不再把 raw governance dict 直接喂给 clone builder。这样 usage by-session 的 dict-builder 链与 route-level row builder 链，在本地 clone/summary 主干上又对齐了一段，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`423/423`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task usage top-task route builder 也复用本地 clone helper（2026-06-15）：本轮继续把治理单源化从 usage by-session route builder 回收一刀到对称的 usage top-task route builder。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_usage_top_task_row()` 现在会先对 `row["governance"]` 走 `_clone_task_governance()`，再进入 `_build_task_governance_summary_from_clone()`，不再把 raw governance dict 直接喂给 clone builder。这样 usage top-task 与 usage by-session 两条 route-level raw governance 入口，在本地 clone/summary 主干上又对齐了一段，继续降低 sibling clone 入口、聚合态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`424/424`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export task governance dict-builder 也复用本地 clone helper（2026-06-15）：本轮继续把治理单源化从 `tasks.py` 的 usage 榜单回收一刀到 `sessions.py` 的 task export sibling 链。后端 `backend/app/api/routes/sessions.py` 的 `_build_session_export_task_governance_summary_from_dict()` 现在会先对输入走 `_clone_task_governance()`，再进入 `_build_session_export_task_governance_summary_from_clone()`，不再直接调 `chat_persistence_service._clone_task_governance_dict()`。这样 session export task governance 的 dict-builder 链，也开始和同文件里的 task-row / trace-steps / trace-json 三条 clone 链共用同一个本地 clone 入口，继续降低 sibling clone 入口、导出态与展示态之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`425/425`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- trace meta governance raw 值也退回 shared normalizer（2026-06-15）：本轮继续把治理单源化从 route/export sibling 链收回到 persistence service 的 trace meta 抽取入口。后端 `backend/app/services/chat_persistence_service.py` 的 `_extract_task_governance_from_trace_steps()` 现在只在上游做“是否为字符串”的筛选，不再提前吞掉空白 `profile / provider_source` 或空白 `allowed_tool_*` 项，而是统一把 raw trace meta 交还给 `_normalize_task_governance_dict()` 做最终裁剪。这样 trace-step governance 抽取、task-row governance 抽取、task/session/export/usage 几条治理读取链在“raw governance -> shared normalizer”这一层又对齐了一段，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`427/427`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- route-level raw governance 入口退回 dict-builder 主干（2026-06-15）：本轮继续把治理单源化从 clone/builder 主干再往 route-level raw dict 入口收一层。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_usage_top_task_row()` 与 `_build_task_usage_by_session_row()` 现在会把 `row["governance"]` 分别统一退回 `_build_task_governance_summary_from_dict()`、`_build_task_usage_session_governance_summary_from_dict()`；`backend/app/api/routes/sessions.py` 的 `_collect_session_governance_summary()` 也改成在 merge 出 session-level governance summary dict 后，统一退回 `_build_session_export_governance_summary_from_dict()`。这样 usage top-task、usage by-session 与 session export summary 这三条 route-level raw governance 入口，开始和各自已有的 local clone/summary builder 共用同一条 `from_dict` 主干，继续降低 raw dict 入口、clone helper 与 outward builder 之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`430/430`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage dashboard governance 筛选退回 shared matcher（2026-06-15）：本轮继续把治理单源化从 route-level raw dict 入口推进到 service 聚合侧的 profile/source compare。后端 `backend/app/services/chat_persistence_service.py` 新增 `_task_governance_matches_filters()`，并让 `get_tasks_usage_dashboard()` 不再手工拆 `task_governance.profile / provider_source` 再逐项 normalize compare，而是统一退回这条 shared matcher。这样 usage dashboard 的 task-level governance 筛选开始和 shared task-governance normalizer 共用同一条 compare 主干，继续降低 usage 聚合链里 profile/source 匹配口径漂移的风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`431/431`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task governance 落库列写入退回 shared clone helper（2026-06-15）：本轮继续把治理单源化从 usage service compare 回收一刀到持久化列写入链。后端 `backend/app/services/chat_persistence_service.py` 的 `_serialize_task_governance_columns()` 现在会先把 trace-step governance 统一退回 `_clone_task_governance_dict()`，再投影成 `tool_registry_profile / tool_registry_provider_source / allowed_tool_*_json` 四列，不再自己手工拆 governance dict。这样 task governance 的落库列写入链也开始和治理读取链共用同一套 clone 主干，继续降低 trace-step 归一化结果与持久化列投影之间的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`432/432`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 空 governance trace-step 不再短路后续有效治理值（2026-06-15）：本轮继续把治理单源化顺手补成一个真实质量修复。后端 `backend/app/services/chat_persistence_service.py` 新增 `_has_task_governance_values()`，并让 `_extract_task_governance_from_trace_steps()` 在 normalizer 返回“全空治理 dict”时继续跳过，而不是立刻返回这个空结果。这样当前一个 trace step 只有空 `meta`、后一个 step 才有真实 `profile / source / allowed tools` 时，治理提取链不再被前一个空 dict 短路；这也同步降低了落库列写入、usage 聚合和导出链读取到空治理快照的风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`433/433`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 全空 task governance 正规化为 `None`（2026-06-15）：本轮继续把上面的空治理修复往 shared normalizer 自身收一层。后端 `backend/app/services/chat_persistence_service.py` 的 `_normalize_task_governance_dict()` 现在会在 `profile / provider_source / allowed_tool_names / allowed_tool_labels` 全部为空时直接返回 `None`，不再把这类“无效治理值”正规化成空 dict。这样 trace 提取、persisted row 提取、usage matcher、session merge 和落库列写入几条链遇到全空治理值时，都会更稳定地把它视为“缺失”，继续降低空治理快照外溢到展示态和聚合态的风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`434/434`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 全空 session governance summary 正规化为 `None`（2026-06-15）：本轮继续把上面的“缺失语义”对称推进到 session-level。后端 `backend/app/services/chat_persistence_service.py` 新增 `_has_session_governance_values()`，并让 `_normalize_session_governance_summary_dict()` 在 `profiles / provider_sources / allowed_tool_names / allowed_tool_labels` 全部为空时直接返回 `None`，不再把这类“无效会话治理摘要”正规化成空 dict。这样 session merge、usage by-session、session export 顶层 `governance` 和相关 clone/builder 链遇到全空 summary 值时，也会更稳定地把它视为“缺失”，继续降低空 session summary 外溢到展示态和聚合态的风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`435/435`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- persisted-row raw 空白治理值退回 shared normalizer（2026-06-15）：本轮继续把 trace/persisted 两侧治理提取语义再对齐一层。后端 `backend/app/services/chat_persistence_service.py` 的 `_extract_task_governance_from_task_row()` 现在不再上游吞掉空白 `tool_registry_profile / tool_registry_provider_source / allowed_tool_*_json`，而是先把 raw 字符串和值列表统一交给 `_normalize_task_governance_dict()`；只有 normalizer 判定为 `None` 时，才回退 `trace_json` 提取。同时 `_parse_task_governance_json_list_blob()` 也改成只做 JSON/类型解析，不再提前过滤空白字符串。这样 persisted-row 提取链和 trace-side 提取链开始更完整地共用同一套“是否视为有效治理值”的判定主干，继续降低两侧前置裁剪分叉导致的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`436/436`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- governance JSON list parser 兼容 pre-parsed list（2026-06-15）：本轮继续把治理读取链的输入兼容性补齐一格。后端 `backend/app/services/chat_persistence_service.py` 的 `_parse_task_governance_json_list_blob()` 现在除了支持 JSON 字符串，也支持直接接收已反序列化的 list，并复用同一套“仅保留字符串项”的解析语义。这样 persisted-row 治理提取链在遇到预处理过的数据时，不会再把 `allowed_tool_*` 直接吞成空列表，也继续降低存储态与内存态输入形状差异带来的口径漂移风险，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`438/438`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task governance route-level collectors 退回 dict-builder 主干（2026-06-15）：本轮继续把治理单源化从 `tasks.py` 的本地 clone/summary 旁支再收回一层到 route-level collectors。后端 `backend/app/api/routes/tasks.py` 的 `_collect_task_governance_from_trace_json()`、`_collect_task_governance_summary_from_trace_steps()` 与 `_collect_task_governance_from_task()` 现在都会在拿到 persistence service 已经成形的 task-level governance dict 后，直接统一退回 `_build_task_governance_summary_from_dict()`，不再各自先走 `clone -> from_clone`。这样 task response、task trace summary、task export trace path 与 usage top-task 这几条 task-level governance 收集链，又回到同一条 `from_dict` builder 主干上，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`437/437`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session task governance outward builder 退回 dict-builder 主干（2026-06-15）：本轮继续把治理单源化沿着对称路径推进到 `sessions.py`。后端 `backend/app/api/routes/sessions.py` 的 `_build_session_task_summary()` 现在在拿到 `_collect_task_governance_from_task()` 返回的 task-level governance dict 后，会直接统一退回 `_build_session_export_task_governance_summary_from_dict()`，不再绕过公开 builder 直接走 `from_clone`。这样 session export payload、session task summary 和同文件里的 task governance outward builder，又回到同一条 `from_dict` 主干上，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`437/437`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session governance public dict-builders 去依赖 `from_clone` helper（2026-06-15）：本轮继续把治理单源化从 `sessions.py` 的 outward builder 主干再收一层到两个公开 `from_dict` builder 自身。后端 `backend/app/api/routes/sessions.py` 的 `_build_session_export_task_governance_summary_from_dict()` 与 `_build_session_export_governance_summary_from_dict()` 现在都会在拿到本地 clone helper 的标准化 dict 后，直接构造 outward model，不再继续依赖各自的 `from_clone` helper。这样 session export task/session 两条治理 outward builder 在“public dict entry -> outward model”这一层也更集中到了单点入口，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`438/438`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task governance public dict-builders 去依赖 `from_clone` helper（2026-06-15）：本轮继续把同样的收口路径对称推进到 `tasks.py`。后端 `backend/app/api/routes/tasks.py` 的 `_build_task_governance_summary_from_dict()` 与 `_build_task_usage_session_governance_summary_from_dict()` 现在都会在拿到本地 clone helper 的标准化 dict 后，直接构造 outward model，不再继续依赖各自的 `from_clone` helper。这样 task response / usage top-task / usage by-session 这几条治理 outward builder 在“public dict entry -> outward model”这一层也更集中到了单点入口，同时保持外部 SSE / trace / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`440/440`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- route-level dead clone builders 清理完成（2026-06-15）：本轮继续把 governance 单源化从“公开 `from_dict` builder 不再依赖 `from_clone`”再收一层到 dead helper 清理。后端 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 里已经无生产调用的 `_build_*_from_clone` sibling helpers 现已删除，相关 route-level collectors / outward builders 统一停在公开 `from_dict` builder 主干上；`backend/scripts/test_tool_runtime_slice.py` 也同步去掉了这些 dead helper 的旧依赖断言，改为显式锁定模块不再暴露这些 helper，并继续验证本地 clone helper 只负责标准化输入、不再承担额外 outward builder 旁支。这样 task/session governance route 层主干又收干净了一段，而且没有改外部 SSE / trace / e2e 契约。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`436/436`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- route-level dead local clone wrappers 清理完成（2026-06-15）：本轮继续把 governance 单源化从“dead clone builder 已删”再收一层到 route 模块内部那 4 个纯转发本地 `_clone_*` wrapper。后端 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 已删除这些只负责转发到 `chat_persistence_service._clone_*` 的本地 helper，相关 `from_dict` builders 与 session trace/task collectors 现在直接复用 shared clone helper；`backend/scripts/test_tool_runtime_slice.py` 也同步从“复用本地 clone helper”切到“模块不再暴露这些 dead local helpers，并继续锁 shared clone helper 主干”。这样 tasks/sessions 两侧 route-level governance helper 分层又少了一层，且外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`433/433`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- dead trace-json governance collector 清理完成（2026-06-15）：本轮继续把 governance 单源化从 local clone wrapper 再收一层到 `tasks.py` 里只剩测试在用的 `_collect_task_governance_from_trace_json()`。后端 `backend/app/api/routes/tasks.py` 已删除这个不再参与生产主链的 route-level trace-json collector，task trace/export 仍统一走现有 `_collect_task_governance_summary_from_trace_steps()` 与公开 `from_dict` builder 主干；`backend/scripts/test_tool_runtime_slice.py` 也同步把旧的 direct collector focused tests 收口成“模块不再暴露 dead trace-json governance collector”的新护栏。这样 task route governance helper 主干又少一层，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`431/431`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session dead trace-json governance collector 清理完成（2026-06-15）：本轮继续沿同样的路径把 `sessions.py` 里只剩一个生产调用点的 `_collect_task_governance_from_trace_json()` 也收掉。后端 `backend/app/api/routes/sessions.py` 已删除这个 route-level trace-json collector，并让 `_collect_task_governance_from_task()` 在 trace-json fallback 分支里直接退回 shared `chat_persistence_service._extract_task_governance_from_trace_json()` + shared clone helper；`backend/scripts/test_tool_runtime_slice.py` 也同步把旧的 direct collector focused test 收口成“模块不再暴露 dead trace-json governance collector”的新护栏。这样 session task governance fallback 主干又少一层，但外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`431/431`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- dead task export governance dict-builder 清理完成（2026-06-15）：本轮继续把 governance 单源化从 trace-json collector 再收一层到 `tasks.py` 里只剩测试在用的 `_build_task_export_governance_from_dict()`。后端 `backend/app/api/routes/tasks.py` 已删除这个不再参与生产主链的 export dict-builder，task export 继续统一走现有 `TaskGovernanceSummary -> _build_task_export_governance_from_summary()` 主干；`backend/scripts/test_tool_runtime_slice.py` 也同步把旧的 direct focused tests 收口成“模块不再暴露 dead task export governance dict-builder”的新护栏。这样 task export governance helper 主干又少一层，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`431/431`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session dead task-row governance collector 清理完成（2026-06-16）：本轮继续把 governance 单源化从 trace-json collector 再收一层到 `sessions.py` 里只剩一个生产调用点的 `_collect_task_governance_from_task_row()`。后端 `backend/app/api/routes/sessions.py` 已删除这个 route-level task-row collector，并让 `_collect_task_governance_from_task()` 直接复用 shared `chat_persistence_service._extract_task_governance_from_task_row()` + shared clone helper；`backend/scripts/test_tool_runtime_slice.py` 也同步新增 focused 护栏，锁定模块不再暴露这条 dead collector。这样 session task governance row fallback 主干又少一层，但外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`432/432`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session dead trace-steps governance collector 清理完成（2026-06-16）：本轮继续把 governance 单源化从 task-row collector 再收一层到 `sessions.py` 里只剩一个生产调用点的 `_collect_task_governance_from_trace_steps()`。后端 `backend/app/api/routes/sessions.py` 已删除这个 route-level trace-steps collector，并让 `_collect_task_governance_from_task()` 在 parsed-steps fallback 分支里直接复用 shared `chat_persistence_service._extract_task_governance_from_trace_steps()` + shared clone helper；`backend/scripts/test_tool_runtime_slice.py` 也同步新增 focused 护栏，锁定模块不再暴露这条 dead collector。这样 session task governance trace-steps fallback 主干又少一层，但外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`432/432`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export dead trace title helper 已退场（2026-06-18）：本轮继续沿“route 只做最近邻 outward 组装”的主线，把 `backend/app/api/routes/sessions.py` 里已经没有任何生产调用的 `_trace_step_title()` 也正式删掉。`backend/scripts/test_tool_runtime_slice.py` 先补 focused failing test，明确锁定 `sessions.py` 不应再暴露这条 dead helper，随后仅删除 helper 本体，不改 session export 的 trace preview、RAG 统计、SSE、trace 与前端 e2e 契约。这样 session export route 又少了一段遗留 title 推导分叉，focused 基线升到 `472/472`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`472/472`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings route provider-source spec 解析已退回 shared helper（2026-06-18）：本轮继续沿“优先收掉 route 层重复解析分叉”的主线，把 `backend/app/api/routes/settings.py` 中 `_build_tool_registry_provider_source_option_details()` 里还残留的 `tool_registry_provider_sources_json -> json.loads -> normalized_source_specs` 本地解析也收回 runtime 主干。实现上在 `backend/app/services/tool_runtime.py` 新增 `get_tool_registry_provider_source_specs_from_settings()`，统一返回按 source name 规范化后的 named source specs；随后 settings route 直接复用这条 helper 生成 provider-source 说明，不再自己解析 raw JSON。这样 tool registry provider-source 的说明链也少了一段 route-level spec 解析分叉，focused 基线升到 `474/474`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`474/474`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings route provider-source names 不再重复重算（2026-06-18）：本轮继续沿 route 层重复解析/重复求值收口，把 `backend/app/api/routes/settings.py:_build_tool_registry_provider_source_option_details()` 中 “先构建 `named_sources`、再调用 `get_available_tool_registry_provider_source_names()` 重算同一批 source names” 这一段 sibling 分叉也删掉。现在 route 会直接以当前已构建的 `named_sources` keys 生成 `default + named source` 顺序，不再为 provider-source 说明链重复跑一遍 source 枚举主干。这样 settings route 在 source detail 说明路径上又少了一次重复求值，focused 基线升到 `475/475`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`475/475`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings summary / validate / update 已共用 shared option builders（2026-06-18）：本轮继续沿“同类 sibling 链一起收”的路径，把 settings route 里 profile/source option names + details 的重复求值成组收回共享 builder。实现上 `backend/app/api/routes/settings.py` 新增 `_build_tool_registry_profile_options()` 与 `_build_tool_registry_provider_source_options()`，统一一次返回 `names + details`；随后 `settings summary`、`validate preview` 与 `update_settings` 的 profile/source 有效性校验都直接退回这两条 shared builders，不再分别走“names 一次、details 再一次”的双算路径。这样 settings 三条主链在 option 组装这一层进一步单点化，focused 基线升到 `479/479`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`479/479`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings option bundle 与 dead detail wrappers 已一并收口（2026-06-18）：本轮继续沿同一条 settings 主线再收一层，把 `validate preview`、`settings summary` 与 `update_settings` 的 profile/source option 双调也并成单一 bundle，同时把只剩 wrapper 作用的 `_build_tool_registry_profile_option_details()` 与 `_build_tool_registry_provider_source_option_details()` 一起删除。现在 `backend/app/api/routes/settings.py` 统一通过 `_build_tool_registry_options_bundle()` 单次返回 `profile/source names + details`，并让 `*_options()` 成为唯一活主干；旧 detail wrappers 已不再暴露。这样 settings route 在 option 组装这一片的 sibling helper 与双算路径一起退场，focused 基线升到 `483/483`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`483/483`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings intermediate option helpers 也已退场（2026-06-18）：本轮继续沿同一条 settings 主线把上一轮留下的两个中间层 `_build_tool_registry_profile_options()` 与 `_build_tool_registry_provider_source_options()` 也正式收掉。流程上先把 `backend/scripts/test_tool_runtime_slice.py` 中仍围绕这两个 helper 的 direct focused tests 改成直接锁 shared `_build_tool_registry_options_bundle()` 与 “模块不再暴露中间 helper”的红灯；随后实现上把两段 profile/source option 组装逻辑直接内联回 `_build_tool_registry_options_bundle()`，让 `settings summary`、`validate preview`、`update_settings` 与 focused tests 都只盯住仍在生产链上的 bundle 入口。这样 settings route 在 option 组装层已经不再保留 bundle 之下的中间 helper 分层；由于本轮同时删除了围绕 dead helper 的重复 focused cases，后端 slice 基线回落到 `479/479`，属于真实删枝，不是漏测。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`479/479`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session governance outward 组装已退回 plain-dict 边界（2026-06-18）：本轮继续沿“route 只做最近邻 outward 组装”的主线，把 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 里还残留的几处 `governance dict -> nested pydantic model` 手工转换也一起收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 4 条 focused failing tests，分别锁定 task detail、task list、usage dashboard（`by_session` / `top_tasks`）以及 session export 在把 shared service 已提供的规范化 governance 交给 outward model 时，不应再由 route 手工构造 `TaskGovernanceSummary`、`TaskUsageSessionGovernanceSummary`、`SessionExportTaskGovernanceSummary` 或 `SessionExportGovernanceSummary`；随后实现上改成 route 只在边界做一层 `dict(...)` plain-clone，真正的嵌套 model 校验统一留给 `TaskResponse`、`TaskUsage*Row` 与 `SessionExport*` outward model。这样 task/session 两侧 route 在 governance outward 这一层又少了一段 sibling 构造分叉，同时仍兼容现有 GuardedGovernanceDict 护栏与外部 SSE / trace / export / e2e 契约。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`483/483`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task response governance clone 已并回 `_with_status_meta()`（2026-06-18）：本轮继续沿同一条 `tasks.py` 主线再收一小层，把 task list/detail 两条 `TaskResponse` 路径里还残留的 governance plain-clone 也退回现有 `_with_status_meta()`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 3 条 focused failing tests：一条锁定 `_with_status_meta()` 自己必须负责把 service 提供的 governance dict 转成 plain dict，另外两条锁定 `get_tasks()` 与 `get_task_detail()` 在拿到 `_with_status_meta()` 的结果后，不应再继续手动碰第二遍 governance；随后实现上让 `_with_status_meta()` 统一承接 `dict(governance)`，并把 list/detail 两条主链都简化回 `TaskResponse(**_with_status_meta(task))`。这样 `tasks.py` 里 task response 的 status meta 与 governance plain-clone 终于站回同一个 shared helper，focused 基线升到 `486/486`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`486/486`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export task-row governance clone 已并回 `_task_status_meta()`（2026-06-18）：本轮继续沿同一条 session export 主线对齐 `tasks.py` 的做法，把 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里 task summary 这条路径还残留的 governance plain-clone，也退回现有 `_task_status_meta()`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 2 条 focused failing tests：一条锁定 `_task_status_meta()` 自己必须负责把 service 提供的 governance dict 转成 plain dict，另一条锁定 session export task summary 在拿到 `_task_status_meta()` 的结果后，不应再继续手动碰第二遍 governance；随后实现上让 `_task_status_meta()` 统一承接 `dict(governance)`，并让 `SessionExportTaskSummary.governance` 直接复用 `status_meta["governance"]`。这样 session export task-row 的 status meta 与 governance plain-clone 也重新站回同一个 shared helper，focused 基线升到 `488/488`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`488/488`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- route-level plain dict clone helper 已覆盖 usage / export 治理边界（2026-06-18）：本轮继续沿“尽量一次多收”的节奏，把还散落在 route 层几个导出/榜单节点上的 `dict(governance)` / `TaskExportGovernance(**governance)` 也成组收口。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 5 条 focused failing tests，分别锁定 `backend/app/api/routes/tasks.py` 与 `backend/app/api/routes/sessions.py` 都必须提供本地 `_plain_clone_dict()`，并让 `tasks.py` 的 `task export trace.governance`、`usage dashboard by_session/top_tasks`，以及 `sessions.py` 的顶层 `governance` 全部复用这条 helper，而不是继续各自在 route 主链里做最后一层治理 plain-clone / nested model 构造；随后实现上补齐两侧 helper，并把这些 callsite 全部切过去。这样当前 route 层还剩的 usage/export 治理边界已经更集中地站回模块内同一条 plain-clone 主干，focused 基线升到 `493/493`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`493/493`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export trace summary 已退回 shared service 单入口（2026-06-18）：本轮继续沿“优先收 route 层 trace 分叉”的主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 里还残留的 `get_task_trace_steps_from_task(task) + get_trace_rag_export_summary(trace_steps)` 双跳也收成单入口。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 3 条 focused failing tests，分别锁定 `backend/app/services/chat_persistence_service.py` 必须提供 `get_task_trace_export_summary_from_task(task)`，并要求 task export route 直接复用这条 helper，而不再自己直连 parsed trace steps 或 RAG export summary helper；随后实现上在 persistence service 新增统一返回 `steps / step_count / rag_hit_count / rag_knowledge_base_ids / rag_chunks` 的 trace export summary helper，并让 route 只继续负责 outward export model 组装。这样 task export 的 trace 读取与 RAG 摘要链也开始和 session export trace preview 一样退回 shared service 主干，同时保持外部 SSE / trace / export / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`494/494`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- trace preview / export summary duplicate parsing fork 已并回单条 service 主干（2026-06-18）：本轮继续沿“优先收掉 trace sibling 分叉”的主线，把 `backend/app/services/chat_persistence_service.py` 里 `get_task_trace_preview_summary_from_task(task)` 还在自己重复读取 trace steps、重复统计 `rag_hit_count` 的那条旁支也收掉。流程上先把 focused test 改成红灯，锁定 preview summary 必须复用 `get_task_trace_export_summary_from_task(task)`，而不再自己直连 `get_task_trace_steps_from_task(task)`；随后实现上让 preview helper 直接从 export summary helper 复用 `steps / step_count / rag_hit_count`，只保留 preview excerpt 的最近邻组装。这样 session export trace preview 与 task export trace summary 现在已经真正站回同一条 persistence service 主干，同时保持外部 SSE / trace / export / e2e 契约不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`494/494`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export batch trace preview 汇总已退回 shared service（2026-06-18）：本轮继续沿“优先收 route 层 trace sibling 分叉”的主线，把 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里还残留的“逐 task 调 preview helper、再自己累加 `trace_step_total / rag_hit_total`”这段也收回 persistence service。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须提供 `get_task_rows_trace_preview_summary(task_rows)`，批量复用 per-task preview helper 并汇总总 `trace_step_count / rag_hit_count`；另一条锁定 session export route 必须直接复用这条 batch helper，而不再自己逐 task 调 `get_task_trace_preview_summary_from_task(task)`。随后实现上补齐 shared helper，并让 route 直接消费它返回的 per-task preview rows 与总计统计。这样 session export route 的 trace preview / total 汇总链也开始和 governance summary 一样退回 service 单入口，外部 export / SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`495/495`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export combined trace/governance batch summary 已退回 shared service 单入口（2026-06-18）：本轮继续沿“尽量一次多收”的主线，把 `session export` route 里已经退回 service 的两条 batch choreography 再并成单点。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 3 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须提供 `get_task_rows_export_summary(task_rows)`，统一复用 `get_task_rows_trace_preview_summary(task_rows)` 与 `get_task_rows_governance_summary(task_rows)`；另外两条锁定 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 必须直接复用这条 combined helper，而不再分别调用 trace-preview batch helper 和 governance batch helper。随后实现上补齐 combined helper，并让 session export route 只消费它返回的 `tasks / trace_step_count / rag_hit_count / governance`。这样 session export 的 trace preview、totals 与 governance summary 现在已经真正站回同一条 persistence service 单入口，外部 export / SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`496/496`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export / session export per-task summary 已继续上推到更高层 service 单入口（2026-06-18）：本轮继续沿“尽量一次多收、早点把 export 主链收干净”的主线，把两条导出链各自还残留的 service choreography 再往上推一层。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 新增 5 条 focused failing tests：两条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_task_export_summary_from_task(task)` 与 `get_task_rows_session_export_summary(task_rows)`，前者统一复用 `get_task_trace_export_summary_from_task(task)` 与 `get_task_usage_from_task(task)`，后者统一复用 `get_task_rows_export_summary(task_rows)` 与 `get_task_usage_from_task(task_row)`；另外三条锁定 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 必须直接复用这两条更高层 helper，而不再自己分别串 trace export summary、usage helper、trace-preview/governance batch helper。随后实现上补齐这两条 shared helper，并让 task export / session export route 都只消费更高层 export summary。这样两条导出链在 trace / governance / usage 这一层都进一步站回 persistence service 主干，外部 export / SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`498/498`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export task-governance / task trace detail 继续退回 shared summary helper（2026-06-18）：本轮继续沿“优先收 route 层剩余 trace / governance 分叉”的主线，一次再收两个点。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 把 focused case 改成红灯：一条锁定 `backend/app/services/chat_persistence_service.py:get_task_rows_session_export_summary(task_rows)` 必须把每个 task 的 governance 也一起装配回 per-task summary，另一条锁定 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 必须直接复用这条 task-level governance summary，而不再从 `_task_status_meta()` 旁支取 governance；随后再补 1 条 focused 红灯，锁定 `backend/app/api/routes/tasks.py:get_task_trace_detail()` 必须复用 `chat_persistence_service.get_task_trace_export_summary_from_task(task)`，而不再自己直连 `get_task_trace_steps_from_task(task)`。实现上补齐 session export helper 的 per-task governance 透传，并让 session export route 与 task trace detail route 都退回 shared summary helper。这样 session export task-row governance 与 task trace detail 这两条 route-level 残余分叉也继续并回 persistence service 主干，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `498/498`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`498/498`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export status helper 已不再夹带 dead governance clone（2026-06-18）：本轮继续沿 route 层治理分叉删枝，把 `backend/app/api/routes/sessions.py:_task_status_meta()` 里已经脱离主链的 governance plain-clone 也正式收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 把 focused case 改成红灯，锁定 `_task_status_meta()` 在 session export 已完全改走 `get_task_rows_session_export_summary()` 的前提下，不应再继续对 `task["governance"]` 做任何 clone 或透传；随后实现上把 helper 收回只负责 `status_normalized / status_label / status_rank`。这样 session export route 在 task status meta 这一层又少了一段 dead governance branch，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `498/498`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`498/498`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export dead task-status helper 已退场（2026-06-22）：本轮继续沿 route 层删枝主线，把 `backend/app/api/routes/sessions.py` 里只剩单次调用的 `_task_status_meta()` 也正式删除。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `sessions.py` 模块不应再继续暴露 `_task_status_meta()`，另一条锁定 `_build_session_export_payload()` 不应再依赖它，而要把 task status 直接内联到 session export task summary 主链；随后实现上删除 helper 本体，并让 `status / status_normalized / status_label / status_rank` 直接在 session export loop 中组装。这样 session export route 在 status 装配这一层又少了一层 single-call helper 分叉，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `498/498`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`498/498`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export per-task status choreography 已继续上推到 shared helper（2026-06-22）：本轮继续沿“单次多收”的主线，把 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里还残留的 per-task status / prompt / timestamp 编排也一起推回 `chat_persistence_service.get_task_rows_session_export_summary(task_rows)`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py:get_task_rows_session_export_summary()` 必须连同 `prompt / status / status_normalized / status_label / status_rank / created_at / updated_at` 一起产出 per-task session export summary row；另一条锁定 session export route 必须直接消费这些 helper 产出的字段，而不再自己用 raw `task_rows` 重新派生 status 或 timestamps。随后实现上在 persistence service 引入 task-status helper，把这些字段一并装进 `tasks[]` summary，并让 session export route 直接消费 helper rows 构造 outward model。这样 session export route 在 per-task summary 编排这一层又少了一整段 `task_rows + trace_summary_by_task_id + status 派生` 的 sibling choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `498/498`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`498/498`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export task-meta/status choreography 已继续上推到 shared helper（2026-06-22）：本轮继续沿“单次多收”的主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 里还残留的 task meta/status 编排也一起推回 `chat_persistence_service.get_task_export_summary_from_task(task)`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py:get_task_export_summary_from_task()` 必须连同 `task_id / session_id / prompt / status / status_normalized / status_label / status_rank / created_at / updated_at` 一起产出 task export summary；另一条锁定 task export route 必须直接消费这些 helper 产出的字段，而不再自己调用 `_with_status_meta(task)` 重新派生 task meta/status。随后实现上在 persistence service 里把这些字段一并装进 task export summary，并让 task export route 直接消费 helper summary 构造 `TaskExportTask` outward model。这样 task export route 在 task meta/status 编排这一层又少了一整段 route-level choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `499/499`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`499/499`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export nested task/trace summary 已继续上推到 shared helper（2026-06-22）：本轮继续沿“单次多收”的主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 里剩余的 flat export-summary 解包也一起推回 `chat_persistence_service.get_task_export_summary_from_task(task)`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 把 focused case 改成红灯：一条锁定 persistence service helper 必须直接产出嵌套的 `task` / `trace` summary dict，另一条锁定 task export route 必须直接消费这两个 nested blocks，而不再自己从 flat keys 回拼 `TaskExportTask` 与 `TaskExportTrace` 所需字段。随后实现上让 `get_task_export_summary_from_task()` 改为统一返回 `{task, usage, trace}`，其中 `trace` 内部继续承载 `governance / steps / step_count / rag_hit_count / rag_knowledge_base_ids / rag_chunks`；task export route 则退回只消费 nested summary 并构造 outward model。这样 task export route 在 task/trace export 编排这一层又少了一段 flat-key choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `499/499`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`499/499`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage dashboard / session export single-call route helpers 成组清理（2026-06-16）：本轮一次多收了 4 个只剩单点生产调用的 route helper。后端 `backend/app/api/routes/tasks.py` 已删除 `_build_task_usage_top_task_row()` 与 `_build_task_usage_by_session_row()`，把 usage dashboard 的 top-task / by-session outward 组装直接内联回 `get_tasks_usage_dashboard_route()`；`backend/app/api/routes/sessions.py` 已删除 `_build_session_task_summary()` 与 `_collect_session_governance_summary()`，把 session export 的 task summary 与 session governance 聚合直接内联回 `_build_session_export_payload()`。`backend/scripts/test_tool_runtime_slice.py` 也同步把 direct focused tests 收口成“模块不再暴露 dead usage/session assembly helpers”，并把 builder 复用护栏提升到了 usage dashboard route 与 session export payload 主链。这样 usage dashboard 和 session export 两侧 route-level sibling helper 又少了一层，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`430/430`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export single-path governance helpers 清理完成（2026-06-16）：本轮继续把删枝收回到 `tasks.py` 的 export 主链，删除了只服务于 `_build_task_export_payload()` 的 `_collect_task_governance_summary_from_trace_steps()` 与 `_build_task_export_governance_from_summary()`。现在 task export 会直接在 `_build_task_export_payload()` 里复用 shared trace-governance parser、现有 `TaskGovernanceSummary` builder，以及 `TaskExportGovernance` 构造，不再挂两条 sibling export helper。`backend/scripts/test_tool_runtime_slice.py` 也同步把围绕这两个 helper 的 direct focused tests 收口成“模块不再暴露 dead task export governance helpers”，并新增一条更贴近主链的护栏，直接锁定 `build_task_export_payload()` 继续复用 shared trace-governance parser。这样 task export governance route 主干又少了两层包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`429/429`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export governance dead helpers 成组清理（2026-06-16）：本轮继续把删枝推进到 `sessions.py` 的 export 主链，删除了 `_build_session_export_task_governance_summary_from_dict()`、`_build_session_export_governance_summary_from_dict()` 与 `_collect_task_governance_from_task()` 这 3 个只剩单一路径使用的治理 helper。现在 `backend/app/api/routes/sessions.py` 会直接在 `_build_session_export_payload()` 里复用 shared task-row parser、trace-json / trace-steps parser、session governance merger，并直接构造 `SessionExportTaskGovernanceSummary` 与 `SessionExportGovernanceSummary`，不再保留 route-level governance sibling helper。对应 `backend/scripts/test_tool_runtime_slice.py` 也把 direct focused tests 收口成“模块不再暴露 dead session export governance helpers”的新护栏。这样 session export governance 主干又少了一层包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`426/426`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task governance dead collector 清理完成（2026-06-16）：本轮继续把删枝推进到 `tasks.py` 的 task-level governance 主链，删除了只剩包装作用的 `_collect_task_governance_from_task()`。现在 `backend/app/api/routes/tasks.py` 的 `_build_task_response()`、`_build_task_export_payload()` 的 persisted fallback，以及 usage dashboard top-task 的 persisted fallback，都会直接复用 shared `chat_persistence_service._extract_task_governance_from_task_row()` + 公开 `_build_task_governance_summary_from_dict()`，不再挂一条 route-level collector 旁支。`backend/scripts/test_tool_runtime_slice.py` 也同步把 focused 护栏抬高到 response/export/usage 三条主链，并明确锁定模块不再暴露这条 dead collector。这样 task-level governance outward 主干又少了一层包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`428/428`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- usage by-session governance builder 清理完成（2026-06-16）：本轮继续把删枝推进到 `tasks.py` 的 usage dashboard 主链，删除了只剩单一路径使用的 `_build_task_usage_session_governance_summary_from_dict()`。现在 `backend/app/api/routes/tasks.py` 会直接在 `get_tasks_usage_dashboard_route()` 的 `by_session` 组装里复用 shared `chat_persistence_service._clone_session_governance_summary_dict()`，并直接构造 `TaskUsageSessionGovernanceSummary` outward model，不再挂一条 route-level by-session governance builder。对应 `backend/scripts/test_tool_runtime_slice.py` 也把 focused 护栏收口成“模块不再暴露 dead usage session governance summary builder”，并把回归点提升到 usage by-session route 主链。这样 usage by-session governance 主干又少了一层包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`427/427`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task response route builder 清理完成（2026-06-16）：本轮继续把删枝推进到 `tasks.py` 的 task response 主链，删除了 `_build_task_response()` 这条只负责包装 `TaskResponse` 的 route-level helper。现在 `backend/app/api/routes/tasks.py` 会直接在 `get_tasks()` 的 items 组装和 `get_task_detail()` 的 detail path 中复用 `_with_status_meta()`、shared `chat_persistence_service._extract_task_governance_from_task_row()` 与公开 `_build_task_governance_summary_from_dict()`，再直接构造 `TaskResponse` outward model，不再挂一条本地 response builder。对应 `backend/scripts/test_tool_runtime_slice.py` 也同步把 focused 护栏提升到 task list / task detail 两条主链，并新增“模块不再暴露 dead task response builder”的约束。这样 task response route 主干又少了一层包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`429/429`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- route 层 clone-helper 依赖继续收口（2026-06-16）：本轮继续把删枝从 route wrapper 推进到 route 对 shared clone helper 的直接依赖。现在 `backend/app/api/routes/tasks.py` 的 `by_session` 组装不再依赖 `_clone_session_governance_summary_dict()`，而是直接走 shared `_normalize_session_governance_summary_dict()`；`backend/app/api/routes/sessions.py` 的 session export row/trace governance fallback 也不再额外包一层 `_clone_task_governance_dict()`，而是直接消费 shared row parser / trace parser 产出的 normalized governance dict。对应 `backend/scripts/test_tool_runtime_slice.py` 也同步把 focused 护栏抬高到“route 层不再依赖 clone helper，而直接退回 normalizer / parser 主干”。这样 tasks / sessions 两侧 route-level governance 主干又少了一层 clone 包装，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`431/431`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- shared governance clone helper 已退场（2026-06-16）：本轮继续把删枝从 route 层推进回 shared service 主干。`backend/app/services/chat_persistence_service.py` 已删除 `_clone_task_governance_dict()` 与 `_clone_session_governance_summary_dict()`，并让 `_serialize_task_governance_columns()` 直接退回 `_normalize_task_governance_dict()`；同时 `backend/app/api/routes/tasks.py` 的 `_build_task_governance_summary_from_dict()` 也直接改走 shared task normalizer，不再经一层 clone helper。对应 `backend/scripts/test_tool_runtime_slice.py` 先补了红灯，明确锁定 task governance 列写入链和 task governance outward builder 都不能再依赖 clone helper，并新增 shared module 不再暴露这两个 helper 的护栏。这样 task governance 的持久化写入链、route-level outward builder 与 shared normalizer 主干进一步单点化，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`432/432`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- normalized governance dict 直连 outward model（2026-06-16）：本轮继续把删枝从 shared clone helper 退场再往 outward builder 主干收一层。`backend/app/api/routes/tasks.py` 的 `_build_task_governance_summary_from_dict()`、usage by-session route 内的 `TaskUsageSessionGovernanceSummary` 构造，以及 `backend/app/api/routes/sessions.py` 的 `SessionExportTaskGovernanceSummary` / `SessionExportGovernanceSummary` 构造，现在都直接信任 shared normalizer / parser / merger 返回的规范化 dict，改成 `**normalized_dict` 直连 outward model，不再逐字段 `.get()` 再做一遍最近邻兜底。对应 `backend/scripts/test_tool_runtime_slice.py` 先补了 3 条红灯，明确锁定 task builder、usage by-session 和 session export payload 都必须直接消费 normalized dict 形状。这样 shared governance normalizer 到 route/export outward model 的主干又少了一层重复字段清洗，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`435/435`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- shared parser / extractor 输出继续直连主链（2026-06-16）：本轮继续把删枝从 normalized dict outward builder 再往上游收一层。`backend/app/services/chat_persistence_service.py` 的 `_serialize_task_governance_columns()` 现在直接信任 `_extract_task_governance_from_trace_steps()` 已返回的规范化 dict，不再重走 normalizer；同时 `backend/app/api/routes/tasks.py` 的 task list/detail、usage top-task persisted fallback 与 service-governance path，也在拿到 shared row parser 或 usage service 已成形的 governance dict 后，直接构造 `TaskGovernanceSummary`，不再回头经 `_build_task_governance_summary_from_dict()`。对应 `backend/scripts/test_tool_runtime_slice.py` 先补了 4 条红灯，分别锁定列写入链、task detail、task list 和 usage top-task 都必须直接信任 shared parser / extractor 输出。这样 shared governance parser/extractor 到持久化列写入链和 route-level outward model 的主干又少了一层重复 hop，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`435/435`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export governance dict-builder 已退场（2026-06-16）：本轮继续把删枝从 shared parser / extractor 直连主链再推进一层到 `tasks.py` 里只剩 task export 在用的 `_build_task_governance_summary_from_dict()`。实现上，`backend/app/api/routes/tasks.py` 已删除这条 helper，并让 `_build_task_export_payload()` 在 trace path 与 persisted fallback path 下都直接信任 shared `chat_persistence_service._extract_task_governance_from_trace_steps()` / `_extract_task_governance_from_task_row()` 产出的规范化 dict，再直接构造 `TaskExportGovernance`；对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了 3 条红灯，锁定模块不再暴露该 helper，且 task export 不能再把 shared trace parser / row parser 输出回头送进旧 builder。这样 task export governance 主链又少了一层重复 hop，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`434/434`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- shared trace-steps JSON parser 进入 service 主干（2026-06-16）：本轮继续把删枝从 governance 组装层往 trace 读取底座再推进一层。`backend/app/services/chat_persistence_service.py` 新增 `_load_trace_steps_from_trace_json()`，统一承接 `trace_json -> json.loads -> 过滤 dict -> _normalize_trace_steps()` 这条主链；随后 `get_task_trace()`、`get_task_trace_delta_from_task()`、`_extract_task_governance_from_trace_json()`、`backend/app/api/routes/tasks.py:_latest_seq_from_task()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 都已切回这条 shared parser。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了 4 条红灯，锁定 service helper、自身 governance parser、task route latest-seq 和 session export trace path 都必须复用这一入口。这样 task/session export 与 trace 相关最近邻读取点又少了多处重复 `json.loads(trace_json)`，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`437/437`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- shared usage_json parser 进入 service 主干（2026-06-16）：本轮继续把删枝从 trace 读取底座推进到 usage 读取底座。`backend/app/services/chat_persistence_service.py` 新增 `_parse_usage_json_blob()`，统一承接 `usage_json -> json.loads -> dict 校验` 这条主链；随后 `get_tasks_usage_summary()`、`get_tasks_usage_dashboard()`、`backend/app/api/routes/tasks.py:_build_task_export_payload()`、`backend/app/api/routes/sessions.py:_build_session_export_payload()`，以及 reconnect done event 的 usage 回传都已切回这条 shared parser，同时 `tasks.py/_parse_task_usage_blob` 与 `sessions.py/_parse_usage_blob` 两条 dead route helper 已删除。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了 7 条红灯，锁定 shared parser、自身两处 usage 聚合、task/session export，以及两侧 route 模块都必须退回这条入口。这样 usage summary / dashboard / export / SSE 主链又少了多处重复 `json.loads(usage_json)`，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`444/444`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- shared parsed trace-step loader 进入 service 主干（2026-06-16）：本轮继续把单源化从 `trace_json -> list[dict]` 再推进到 `trace_json -> list[TraceStep]`。`backend/app/services/chat_persistence_service.py` 新增 `_load_parsed_trace_steps_from_trace_json()`、`get_task_trace_steps()` 与 `get_task_trace_delta_steps_from_task()`，统一承接 `load trace json -> parse_trace_steps` 这段主链；随后 `backend/app/api/routes/tasks.py` 的 latest-seq、reconnect、task export、trace detail、trace delta，以及 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 都已切回这条 shared parsed-trace 入口。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了红灯，锁定 shared parsed helper、自身 service getter、task export 与 session export trace path 都必须复用这条主链。这样 route/export 层又少了一批重复 `parse_trace_steps(...)`，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`446/446`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- parsed trace-step task helper 继续收口（2026-06-16）：本轮继续把 shared parsed-trace 主干从“按 `task_id` 读取”推进到“直接按 task row / task dict 复用”。`backend/app/services/chat_persistence_service.py` 新增 `get_task_trace_steps_from_task()`，并让 `get_task_trace_steps()` 与 `get_task_trace_delta_steps_from_task()` 都退回这条 helper；随后 `backend/app/api/routes/tasks.py:_latest_seq_from_task()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 也都改成直接复用这条公开入口，不再直接碰私有 `_load_parsed_trace_steps_from_trace_json()`。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了红灯，锁定 service task helper、parsed delta path、task route latest-seq 与 session export trace path 都必须走这条主链。这样 service/route 层又少了一批“task dict 直摸私有 parsed helper”的旁支，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`448/448`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- raw task-trace service helpers 已退场（2026-06-16）：本轮继续把 parsed trace 主干从“默认优先”推进到“独占对外入口”。`backend/app/services/chat_persistence_service.py` 中未再被生产代码使用的 `get_task_trace()`、`get_task_trace_delta_from_task()` 与 `get_task_trace_delta()` 已删除，只保留 parsed `TraceStep` 形态的公开 getter；对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了红灯，锁定 shared module 不应再继续暴露这三条 raw-dict trace helper。这样 service 层对外 trace 读取链正式只剩 parsed 主干，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`449/449`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- parsed trace-step governance extractor 已收口到 service 主干（2026-06-16）：本轮继续把 parsed trace 主干从“只负责读取 `TraceStep[]`”推进到“直接承接 `TraceStep[] -> governance`”。`backend/app/services/chat_persistence_service.py` 新增 `_extract_task_governance_from_parsed_trace_steps()`，并让 [backend/app/api/routes/tasks.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/api/routes/tasks.py) 的 task export 以及 [backend/app/api/routes/sessions.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/api/routes/sessions.py) 的 session export parsed-trace fallback 都退回这条 helper，不再自己把 `TraceStep` dump 回 dict 再走旧 extractor。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了红灯，锁定 shared parsed-governance helper、task export 和 session export 都必须复用这条主干。这样 route/export 层又少了一批 `TraceStep[] -> dict[] -> governance` 的重复中转，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`450/450`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export task-governance fallback 已收口到 service 主干（2026-06-16）：本轮继续把 parsed trace/governance 单源化从“共享 extractor”推进到“共享 fallback 决策”。`backend/app/services/chat_persistence_service.py` 新增 `_extract_task_governance_from_task_with_parsed_trace_steps()`，统一承接 `persisted governance row -> parsed trace steps -> raw trace json` 这段 task-governance fallback 主链；随后 [backend/app/api/routes/sessions.py](/Users/gaobingbing/Desktop/code/SuperPod/InsightAgent/backend/app/api/routes/sessions.py) 的 session export task summary 也直接退回这条 helper，不再在 route 侧自己判断 row parser / parsed steps / trace json 三选一。对应 `backend/scripts/test_tool_runtime_slice.py` 也先补了红灯，锁定 shared fallback helper 和 session export 都必须复用这条主干。这样 session export route 又少了一段 task-governance fallback 分叉，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`451/451`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export trace/task-governance 主干继续收口（2026-06-16）：本轮继续把 parsed trace / governance 单源化从 session export 推进回 task export 主链。`backend/scripts/test_tool_runtime_slice.py` 先补了 4 条 focused failing tests，分别锁定 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 不能再按 `task_id` 重读 trace，必须直接复用 `chat_persistence_service.get_task_trace_steps_from_task(task)`，并且 task export 的治理解析也必须统一退回 `chat_persistence_service._extract_task_governance_from_task_with_parsed_trace_steps(task, parsed_steps)`，不再在 route 侧自己判断 parsed-trace / persisted-row fallback；实现上 `backend/app/api/routes/tasks.py` 已完成这两处收口，外部 SSE / trace / e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`452/452`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export nested task/trace/stats summary 已继续上推到 shared helper（2026-06-22）：本轮继续沿“单次多收”的主线，把 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里剩余的 per-task trace/governance 解包与顶层 task-count/trace-count 汇总也一起推回 `chat_persistence_service.get_task_rows_session_export_summary(task_rows)`。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 把 focused case 改成红灯：一条锁定 persistence service helper 必须直接产出嵌套的 `tasks[].task / tasks[].trace` 与顶层 `stats` summary，另一条锁定 session export route 必须直接消费这些 nested blocks，而不再自己依赖 flat task row 字段或本地重算 `task_count / trace_step_count / rag_hit_count`。随后实现上让 helper 统一返回 `{tasks, stats, governance}`，其中每条 task summary 内部继续承载 `task`、`usage` 与 `trace.{governance, step_count, rag_hit_count, preview}`；session export route 则退回只消费 nested summary 并构造 outward model。这样 session export route 在 per-task trace/governance/stats 编排这一层又少了一整段 flat-row choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `500/500`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`500/500`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session export payload helper 已继续上推到更高层 shared service（2026-06-22）：本轮继续沿“单次多收”的主线，把两条 export route 里还残留的 `messages / usage_summary / tasks / stats` choreography 再往 persistence service 推一层。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 4 条 focused failing tests：两条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_task_export_payload_summary(task, message_rows)` 与 `get_session_export_payload_summary(...)`，分别统一复用已有的 task/session export summary helper 并顺手整理 export message rows；另外两条锁定 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 必须直接消费这两个更高层 payload helper，而不再自己分别串 `get_task_export_summary_from_task()`、`get_task_rows_session_export_summary()` 与本地 message/stats 组装。随后实现上补齐两条 payload helper，并让 task/session export route 都退回只消费 shared payload summary 构造 outward model。这样两条 export route 在 message/task/stats 编排这一层又各少了一整段 sibling choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `504/504`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`504/504`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task trace detail response helper 已继续上推到 shared service（2026-06-22）：本轮继续沿“优先收 route 层 trace/governance 邻近分叉”的主线，把 `backend/app/api/routes/tasks.py:get_task_trace_detail()` 里还残留的 trace/status 编排也一起推回 persistence service。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_task_trace_response_summary_from_task(task)`，统一复用既有 `get_task_trace_export_summary_from_task(task)` 并产出 `steps / status / status_normalized / status_label / status_rank`；另一条锁定 `get_task_trace_detail()` 必须直接消费这条 response helper，而不再自己直连 trace export helper 或本地调用 `normalize_task_status()` / `task_status_label()` / `task_status_rank()`。随后实现上补齐 `get_task_trace_response_summary_from_task()`，并把 task status 派生收成 `_get_task_status_summary_from_task(task)` 供 task trace detail、task export 与 session export per-task summary 复用。这样 task trace detail route 在 trace/status 编排这一层也退回 shared service 单入口，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `507/507`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`507/507`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task detail/list response helper 已继续上推到 shared service，route dead helper 退场（2026-06-22）：本轮继续沿“优先收 route 层 governance/status 邻近分叉”的主线，把 `backend/app/api/routes/tasks.py` 里 task detail/list 还残留的 governance clone 与 status meta 组装也一起推回 persistence service。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 4 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_task_response_summary_from_task(task)` 并在这里完成 governance plain-clone 与 `status / status_normalized / status_label / status_rank` 汇总；两条锁定 `get_task_detail()` 与 `get_tasks()` 必须直接消费这条 shared response helper，而不再依赖 route-level `_with_status_meta()`；最后一条锁定 `tasks.py` 模块不应再继续暴露 dead helper `_with_status_meta()`。随后实现上补齐 `get_task_response_summary_from_task()`，让 task detail/list route 都退回只消费 shared summary 构造 `TaskResponse`，并删除 `_with_status_meta()` 本体。这样 task detail/list route 在 governance/status 编排这一层也退回 shared service 单入口，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `508/508`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`508/508`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task usage dashboard response helper 已继续上推到 shared service（2026-06-22）：本轮继续沿“优先收 route 层 governance 邻近分叉”的主线，把 `backend/app/api/routes/tasks.py:get_tasks_usage_dashboard_route()` 里 `by_session / top_tasks` 的 governance plain-clone 与行级 response 组装也推回 persistence service。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_tasks_usage_dashboard_response_summary(payload)`，统一完成 `window_days / summary / trend / by_session / top_tasks` 的 response-friendly 整理，并在这里 plain-clone session/top-task governance dict；另一条锁定 usage dashboard route 必须直接消费这条 shared response helper，而不再自己调用 `_plain_clone_dict()` 处理 governance。随后实现上补齐 `get_tasks_usage_dashboard_response_summary()`，并让 usage dashboard route 退回只消费 shared summary 构造 `TaskUsageDashboardResponse`。这样 usage dashboard route 在治理 summary 编排这一层也退回 shared service 单入口，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `509/509`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`509/509`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session export governance response helper 已继续上推到 shared service，route plain-clone dead helper 退场（2026-06-22）：本轮继续沿“优先收 export route 层 governance 邻近分叉”的主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里还残留的 governance plain-clone 也一起推回 persistence service。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 4 条 focused failing tests：两条锁定 `backend/app/services/chat_persistence_service.py:get_task_export_response_summary()` 与 `get_session_export_response_summary()` 必须在这里 plain-clone trace/session governance dict；另外两条锁定 task/session export route 必须直接消费这些 shared response helper 产出的 governance，而不再自己调用 route-level `_plain_clone_dict()`。随后实现上把 clone 责任收进两条 export response helper，并让 task/session export route 直接透传 helper governance；因为两处 `_plain_clone_dict()` 已完全脱离主链，`tasks.py` 与 `sessions.py` 里的 dead helper 本体也一起删除。这样 task/session export route 在 governance 编排这一层继续退回 shared service 单入口，route 层又少了两条遗留 clone branch，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `511/511`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`511/511`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session export outward-model 组装继续收回顶层 response model（2026-06-23）：本轮继续沿“优先收 export route 层 trace/governance 邻近分叉”的主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 里还残留的 `TaskExportTask / TaskExportTrace / SessionUsageSummaryResponse / SessionExportTaskSummary / SessionExportStats / SessionExportMessage` 手工构造也一起收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补并改 5 条 focused failing tests：两条锁定 task export route 必须直接把 shared `task / trace` summary dict 交给 `TaskExportJsonResponse(...)`，三条锁定 session export route 必须直接把 shared `usage_summary / tasks / stats / messages / governance` summary 交给 `SessionExportJsonResponse(...)`，而不再在 route 层逐段手工 new 子模型。随后实现上让两条 export route 都退回“shared response summary + 顶层 response model 校验/coerce”这条更薄主干。这样 export route 在 outward-model 编排这一层又少了一整段 sibling choreography，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `512/512`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`512/512`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task trace detail outward-model 组装继续收回顶层 response model（2026-06-23）：本轮继续沿 trace 邻近分叉主线，把 `backend/app/api/routes/tasks.py:get_task_trace_detail()` 里还残留的 `TaskTraceResponse(...)` 字段手工拆包也一并收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 1 条 focused failing test，锁定 route 不应再通过 `trace_summary.get(...)` 二次拆 shared summary，而要直接把 `chat_persistence_service.get_task_trace_response_summary_from_task(task)` 返回块交给 `TaskTraceResponse(task_id=..., **trace_summary)`；随后实现上把 route 收成单次透传。这样 task trace detail route 在 outward-model 编排这一层也继续退回 shared response summary 主干，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `513/513`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`513/513`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task trace delta response/outward-model 组装继续收回 shared summary（2026-06-23）：本轮继续沿 trace 邻近分叉主线，把 `backend/app/api/routes/tasks.py:get_task_trace_delta_detail()` 里还残留的 tuple 拆包、`lag_seq` 计算与 `TaskTraceDeltaResponse(...)` 字段手工组装也一起收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 2 条 focused failing tests：一条锁定 `backend/app/services/chat_persistence_service.py` 必须新增 `get_task_trace_delta_response_summary_from_task(task, after_seq, limit)`，统一复用既有 `get_task_trace_delta_snapshot_from_task(task, after_seq, limit)` 并产出 `steps / next_cursor / has_more / lag_seq / dropped`；另一条锁定 route 不应再直连 snapshot helper 或通过 `delta_summary.get(...)` 二次拆 shared summary，而要直接把这块 summary 交给 `TaskTraceDeltaResponse(task_id=..., server_time=..., **delta_summary)`。随后实现上让 delta route 退回“shared response summary + 顶层 response model 校验/coerce”主干。这样 task trace delta route 在 response/outward-model 编排这一层也继续退回 shared service 单入口，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `515/515`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`515/515`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session 列表与 usage dashboard outward-model 组装继续收回顶层 response model（2026-06-23）：本轮继续沿 route 薄层删枝主线，把 `backend/app/api/routes/tasks.py:get_tasks()`、`get_tasks_usage_dashboard_route()`、`backend/app/api/routes/sessions.py:get_sessions()` 与 `get_session_messages_detail()` 里还残留的 `TaskResponse / TaskUsageSummaryResponse / TaskUsageTrendPoint / TaskUsageBySessionRow / TaskUsageTopTaskRow / SessionResponse / MessageResponse` 手工构造也一起收掉。流程上先在 `backend/scripts/test_tool_runtime_slice.py` 补 4 条 focused failing tests，分别锁定任务列表、usage dashboard、session 列表与 session messages route 必须直接把 shared/raw summary 交给 `TaskListResponse(...)`、`TaskUsageDashboardResponse(...)`、`SessionListResponse(...)` 与 `SessionMessagesResponse(...)`，而不再在 route 层逐段 new 内层模型；随后实现上统一改成顶层 response model 负责校验/coerce。这样列表、榜单与消息明细几条 route 在 outward-model 编排这一层也进一步退回 shared summary / raw payload 主干，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `519/519`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`519/519`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings validate 与 task create/cancel 状态响应继续收回 shared summary（2026-06-23）：本轮继续沿 route 薄层删枝主线，一次收两类相邻分叉。`backend/app/api/routes/settings.py:_apply_tool_registry_preview_to_validate_response()` 现在会用 `SettingsValidateResponse(...)` 重新校验 preview update 后的完整 payload，确保 shared option bundle 即使返回 raw dict detail rows，也会被顶层 response model coerce 成 `ToolRegistry*OptionResponse`；同时 `backend/app/services/chat_persistence_service.py` 新增 `get_task_create_response_summary(...)` 与 `get_task_cancel_response_summary_from_task(...)`，让 `POST /api/tasks` 与 `POST /api/tasks/{task_id}/cancel` 不再在 route 层自行派生 `status_normalized / status_label / status_rank`。流程上先补 5 条 focused failing tests 覆盖 validate raw option dict、create/cancel service summary 与 create/cancel route 复用，再做最小实现。这样 settings validate preview 与 task create/cancel 两条状态响应也进一步退回 shared summary 主干，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `524/524`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`524/524`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings option bundle 与 session export session outward-model 继续收回顶层校验（2026-06-23）：本轮继续沿 tool registry/profile/provider source 治理产品化和 export route 薄层删枝主线，一次收两个相邻残留点。`backend/app/api/routes/settings.py:_build_tool_registry_options_bundle()` 现在只返回 raw option detail dict，不再在 helper 内部构造 `ToolRegistryProfileOptionResponse` / `ToolRegistryProviderSourceOptionResponse`，由 `SettingsSummaryResponse` / `SettingsValidateResponse` 统一负责校验与 coerce；同时 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 的 `session` 字段也改为直接传 raw session dict 给 `SessionExportJsonResponse(...)`，不再手工 new `SessionResponse(...)`。流程上先补 3 条 focused failing tests 锁定 option bundle/raw session 边界，再做实现与内部 helper 测试断言对齐。这样 settings option detail、settings validate preview 与 session export payload 在 outward-model 编排上继续退回顶层 response model 主干，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `526/526`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`526/526`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- settings selection 校验与 export list outward-model 继续收回 shared/top-level 主干（2026-06-23）：本轮继续沿 tool registry/profile/provider source 治理产品化和 export route 薄层删枝主线，一次多收三个点。`backend/app/api/routes/settings.py` 新增 `_validate_tool_registry_selection()`，并让 `update_settings()` 与 `validate_settings()` 共用同一条 profile/provider source membership 校验；validate 遇到无效 selection 时返回带 preview/options 的 `SettingsValidateResponse(ok=False, error_code="tool_registry_selection_invalid")`。同时 `_build_tool_registry_options_bundle()` 的 profile option detail 现在复用 `_build_tool_registry_preview_fields()` 已经解析好的 label map，不再对排序后的工具名重新走默认 label 分叉；`backend/app/api/routes/tasks.py:_build_task_export_payload()` 与 `backend/app/api/routes/sessions.py:_build_session_export_payload()` 也不再对 shared helper 返回的 `messages/tasks` 做 dict-only 过滤，改为直接交给顶层 export response model 校验/coerce。流程上先补 5 条 focused failing tests 锁定这三类边界，再做最小实现。这样 settings selection、option label 展示与 task/session export list payload 在 route 层又少了几条重复分叉，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `529/529`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`529/529`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export task/trace 子块 outward-model guard 继续收回顶层校验（2026-06-23）：本轮继续沿 export route 薄层删枝主线，把 `backend/app/api/routes/tasks.py:_build_task_export_payload()` 中最后残留的 `task_summary / trace_summary` dict-only guard 也收掉。流程上先强化 2 条 focused tests，让 shared `get_task_export_response_summary(...)` 返回已经构造好的 `TaskExportTask` / `TaskExportTrace` 子模型实例，锁定 route 必须把它们原样交给 `TaskExportJsonResponse(...)`，而不是因为 `isinstance(..., dict)` guard 把合法子模型吞成 `{}`；随后实现上直接透传 `export_summary.get("task", {})` 与 `export_summary.get("trace", {})`。这样 task export 的 `task / trace / messages` 三类 outward 子块都已统一退回顶层 export response model 校验/coerce，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `529/529`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`529/529`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- export response helper payload 透传继续收回 shared 主干（2026-06-23）：本轮继续沿 shared service response helper 薄层化主线，把 `backend/app/services/chat_persistence_service.py:get_task_export_response_summary()` 与 `get_session_export_response_summary()` 里还残留的 payload 重读/过滤分叉也一起收掉。流程上先补 2 条 focused failing tests：一条锁定 task export response helper 不应再重读 payload `task` 字段或过滤 payload `messages`；另一条锁定 session export response helper 不应再过滤 payload `messages`。随后实现上让 task export response helper 直接透传 payload helper 返回的 `task/messages`，session export response helper 直接透传 payload `messages`，同时保留 trace governance plain-clone 与 stats/task summary 既有整理。这样 task/session export response helper 与 route 顶层 response model 之间的 payload 边界更一致，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `531/531`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`531/531`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export response task-row normalization 继续收回 shared helper（2026-06-23）：本轮继续沿 shared export response helper 薄层化主线，把 `backend/app/services/chat_persistence_service.py:get_session_export_response_summary()` 内部 per-task row 的 nested/flat 形状分叉收成 `_get_session_export_task_response_summary_from_payload_row()`。流程上先补 1 条 focused failing test，锁定当 payload helper 已经返回 response-ready flat task row 时，session export response helper 不应再读取 `task / trace` nested keys，也不能重算或丢失 `trace_step_count / rag_hit_count / trace_preview / governance`；随后实现上让 helper 对 response-ready row 直接透传字段并 plain-clone governance，对旧 nested `{task, trace}` row 继续保持兼容整理。这样 session export response helper 对 payload task row 的转换边界更清楚，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `532/532`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`532/532`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task export response trace block normalization 继续收回 shared helper（2026-06-23）：本轮继续沿 shared export response helper 薄层化主线，把 `backend/app/services/chat_persistence_service.py:get_task_export_response_summary()` 里 trace 子块的 dict-only 读取分叉收成 `_coerce_export_payload_block_to_dict()`。流程上先补 1 条 focused failing test，锁定当 payload helper 已经返回 response-ready trace model（提供 `model_dump()`）时，task export response helper 不应把它当成空 trace，也不能丢失 `step_count / rag_hit_count / rag_knowledge_base_ids / rag_chunks / governance`；随后实现上让 trace block 支持 dict 与 model_dump 两类已成形输入，并继续在 response helper 内 plain-clone governance。这样 task export response helper 对 payload trace 子块的边界也和顶层 response model 更一致，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `533/533`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`533/533`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- trace/export step shape coercion 继续收回 shared helper（2026-06-23）：本轮继续沿 trace/export shared service 薄层化主线，把 service 内部几处 `TraceStep`-only list 过滤统一收成 `_coerce_export_trace_steps()`。流程上先补 4 条 focused failing tests，分别锁定 task export response、task export summary、trace preview summary、trace detail response 在拿到 `model_dump()` 或 upstream summary 产出的 dict step 时，不能把 `steps` / `trace_preview` 吞成空；随后实现上让 `_coerce_export_trace_steps()` 同时接受 `TraceStep` 实例和 dict，并让 `get_task_trace_preview_summary_from_task()`、`get_task_trace_response_summary_from_task()`、`get_task_export_summary_from_task()`、`get_task_export_response_summary()` 共用这条 helper。这样 trace/export service 层的 step shape 边界不再分散在多个 list comprehension 里，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `537/537`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`537/537`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- session export 与 usage dashboard response-ready model 兼容继续收尾（2026-06-23）：本轮继续沿 service response helper 收尾主线，把剩余更贴近前端/e2e 的 response-ready model 边界一次收掉。流程上先补 4 条 focused failing tests，分别锁定 `get_session_export_response_summary()` 不能丢弃 payload `tasks[]` 里的 `model_dump()` task row，也不能在 nested `task / trace` 子块是 model 时吞掉字段；同时锁定 `get_tasks_usage_dashboard_response_summary()` 必须支持 response-ready `summary / trend / by_session / top_tasks` rows，并把 by-session/top-task governance model coerce 成 plain dict。随后实现上让 session export task row loop 复用 `_coerce_export_payload_block_to_dict()`，nested task/trace 子块也走同一条 coerce；usage dashboard response helper 则复用 `_coerce_export_payload_block_list_to_dicts()` 和 `_coerce_payload_mapping_or_original()` 收口 rows 与 governance。这样 session export 与 usage dashboard 两条前端重点链路对 response-ready model / raw dict 的兼容边界更一致，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `541/541`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`541/541`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task/session export governance model coercion 继续收尾（2026-06-23）：本轮继续沿 service response/export helper 收尾主线，把 governance 子块的 response-ready model 兼容也集中收齐。流程上先补 4 条 focused failing tests，分别锁定 `get_task_export_response_summary()`、`get_session_export_response_summary()`、`get_task_response_summary_from_task()`、`get_task_export_summary_from_task()` 在收到带 `model_dump()` 的 governance model 时，不能吞成 `None` 或原样外溢，而要 coerce 成 plain dict。随后实现上新增 `_coerce_payload_mapping_or_none()`，并让 task response summary、task export summary、task export response summary、session export task/top-level governance 输出复用 `_coerce_payload_mapping_or_original()` / `_coerce_payload_mapping_or_none()`。这样 task/session/export/usage 几条 service response 边界在 governance model/dict 兼容上进一步对齐，外部 SSE / trace / export / e2e 契约保持不变，slice 基线升到 `545/545`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`545/545`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- task route dead status imports 清理完成（2026-06-23）：本轮继续做最后轻量扫尾，清掉 `backend/app/api/routes/tasks.py` 里已经不再生产使用的 `task_status_label / task_status_rank` import，仅保留 cancel route 仍需要的 `normalize_task_status`。同步把 3 条 focused route tests 的 poison 护栏改成“原本没有 attr 时临时注入、finally 删除”，继续锁定 create/cancel/trace detail route 不会回退本地 status label/rank 派生。这样 route 层 status 派生 dead import 与旧测试假设一起收干净，外部 SSE / trace / export / e2e 契约保持不变，slice 基线保持 `545/545`。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`545/545`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- runtime provider-source specs 与 usage-dashboard 关键 e2e 收尾验收（2026-06-23）：本轮先复核备份计划 untouched、route 层未重新长出 trace/governance fallback 分叉、tasks/sessions route import 无新增未使用项；随后针对 `frontend/e2e/usage-dashboard.spec.ts` 实跑关键用例。验收中先用 focused failing test 暴露 `get_stored_settings()` 在用户已有 settings row 时没有继承默认 `tool_registry_provider_sources_json`，导致 runtime 只拿到 `planning_suite` 名称却丢失 source provider 规格、trace allowed labels 回退成 `Task Planner`；实现上扩展 `StoredSettings` 承载 registry runtime specs，并让 DB 行存在时从默认 settings 克隆后覆盖用户保存字段，确保 runtime / preflight / export 都能拿到同一份 provider-source 定义。同时修稳 usage-dashboard e2e 的 AntD Select helper 与 Task Center 幂等打开，补齐 `API_BASE_URL` 导入。关键验收命令 `cd frontend && npx playwright test e2e/usage-dashboard.spec.ts --project=chromium --grep "usage dashboard governance filters drive backend request params|task center governance filters drive backend request params|saved planning suite source propagates through runtime and export governance|task detail replay preserves planning_only registry trace metadata"` 通过（`4 passed`）。完整校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`546/546`），`cd frontend && npm run lint` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 前后端 e2e 收尾修复（2026-06-24）：本轮先复核阶段 5 收尾是否真的完成，随后按真实报错继续收口前后端 e2e。后端侧补上 `backend/scripts/e2e_main_path.py` 与 `backend/scripts/e2e_export_consistency.py` 的 repo-root bootstrap，修复从仓库根目录执行时 `ModuleNotFoundError: No module named 'scripts'` 的主因；同时把 tool registry 的 calculator source label 透传补齐成“仅当 source/override 标签不同于默认注册标签时，settings preview 与 runtime/export governance 才显示 override label”，从而让 `calculator_suite` 的 `Calculator Suite` 在 validate/save/runtime/export 主链真正打通，但默认 `calculator_only` 仍保持既有 `calc_eval` 展示契约。前端侧则修稳 workbench 两个 e2e 时序问题：发送中的 session 不再因旧流回调把 `activeSessionId` 抢回，running-task recovery notice 在恢复连通后会立即升级成 success，同时对应 Playwright 断言也放宽为兼容“先 info 后 success”或“快速直达 success”。完整校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`549/549`），`cd frontend && npm run lint` 通过，`bash scripts/ci_run_backend_e2e.sh --phase main --base-url http://127.0.0.1:8000` 通过，`cd frontend && PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:8000 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npx playwright test e2e/usage-dashboard.spec.ts e2e/workbench-edge-cases.spec.ts --project=chromium --grep "model settings validate previews retrieval suite enabled tools|model settings validate previews calculator suite enabled tools|saved calculator suite source propagates through runtime and export governance|cross-session switch keeps cancel and export scoped to active session|running task recovery notice covers info to success states"` 通过（`5 passed`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 前后端 e2e 最终验收（2026-06-24，真正收尾完成）：在继续跑完整前端 chromium 全量 Playwright 后，唯一剩余红灯收敛到 `frontend/e2e/workbench-edge-cases.spec.ts` 的 mock cancel 场景。根因不是后端契约回归，而是该 spec 自己的 `waitForContextCancelButton()` 比主路径用例更脆，Inspector 在流式刷新过程中偶发停留在 Trace tab，导致测试误以为已经进入 Context 并开始等待不可见的取消按钮。当前已把这段 helper 收敛成与稳定用例一致的“持续重试点击 Context，直到 `inspector-task-cancel` 真正可见”为止；未改 SSE / trace / export / e2e outward 契约。最终验收结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`549/549`），`cd frontend && npm run lint` 通过，`bash scripts/ci_run_backend_e2e.sh --phase main --base-url http://127.0.0.1:8000` 通过，`bash scripts/ci_run_frontend_e2e.sh --phase full --api-base-url http://127.0.0.1:8000 --frontend-base-url http://127.0.0.1:3001` 通过（`47 passed`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。当前这一块可以视为真正收尾完成，可进入下一模块。
 - CI 红灯补修（2026-06-24，backend export diagnostics + frontend smoke build）：继续对照真实 CI 日志核对后，又补到两处非业务逻辑型回归。其一，`backend/scripts/ci_export_consistency_summary.sh` 的 `task/session export` 正则仍停留在旧文案，只识别 `consistency + download`，而当前主日志已经升级为 `consistency + governance + download` 与 pass banner 的 `schema/stats consistency` 表述，于是 workflow guard 会把“实际通过”的 export consistency 误报成 `P1`；当前已把正则放宽为兼容旧/新两套文案，并在 `backend/scripts/test_ci_export_consistency_summary.sh` 中把 fixture 更新到现网日志格式。其二，前端 `task-center` 与 `usage-dashboard-modal` 已接入治理筛选文案，但 `frontend/lib/i18n/types.ts` 里漏了 `governanceProfileFilterAll / governanceSourceFilterAll / governanceProfilesLabel / governanceSourcesLabel / governanceFilterTitle` 等 `Messages` 类型字段，导致 `npm run build` 与 CI smoke matrix 在 type-check 阶段失败。当前已补齐类型定义。最新本地校验：`bash backend/scripts/test_ci_export_consistency_summary.sh` 通过，`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`549/549`），`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。补充说明：在当前 Codex 沙箱里继续实跑 `scripts/ci_run_backend_e2e.sh` / `scripts/ci_run_frontend_e2e.sh --phase smoke` 会被本地 socket 权限限制拦住（`Operation not permitted` / `listen EPERM 127.0.0.1:3001`），因此这里无法再复现 CI 的网络型执行环境，但代码层面的两处真实报错已完成修复。
- usage-dashboard 治理 suite 下拉 CI 红灯补修（2026-06-24）：继续对照 GitHub Actions 前端 e2e 失败日志后，7 条 `planning_suite / retrieval_suite / calculator_suite` 相关用例根因是 CI 没有 gitignored 的 `backend/.env`，`available_tool_registry_provider_sources` 只剩 `default`，页面下拉因此找不到 suite 选项。当前已在 `backend/app/config.py` 把三向 suite 定义收成默认值，并同步加强 `frontend/e2e/usage-dashboard.spec.ts` 的 AntD 选择与 export 重试。关键验收：`cd frontend && PLAYWRIGHT_BROWSERS_PATH=$HOME/Library/Caches/ms-playwright PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:8000 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 npx playwright test e2e/usage-dashboard.spec.ts --project=chromium --grep "usage dashboard governance filters drive backend request params|task center governance filters drive backend request params|model settings validate previews retrieval suite enabled tools|model settings validate previews calculator suite enabled tools|saved planning suite source propagates through runtime and export governance|saved retrieval suite source propagates through runtime and export governance|saved calculator suite source propagates through runtime and export governance|task detail replay preserves retrieval_only registry trace metadata"` 通过（`8 passed`），`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`549/549`），`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 默认运行策略去 mock 化继续推进（2026-06-25）：当前开始转入“默认工具去 mock 化、真实工具接入”这一下一模块的第一刀，并坚持不改外部 settings/SSE/trace/e2e 契约。后端 `backend/app/services/settings_service.py` 新增默认设置归一逻辑：当用户尚未保存个人 settings，但运行环境已经给齐真实 `provider/model/api_key` 时，`get_stored_settings()` 会把默认运行模式自动提升为 `remote`；如果环境配置不完整，则继续回退到规范的 `mock / mock-gpt` 组合，不再暴露 `mode=mock` 但 `provider=openai` 之类不一致默认态。这样默认运行策略不再被硬锁在 mock，但范围仍控制在后端默认值推导层，前端页面、导出工件与治理链契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`551/551`）。
- remote 默认值继承链继续收口（2026-06-25）：在上一条“默认自动 remote”基础上，本轮继续把 settings 主链一起收顺，避免默认值只在读取时生效、但在 validate/save 时又被拉回半配置态。后端现在会让已有 remote 用户行在 `base_url/api_key` 缺失时继续继承 runtime 默认连接配置；`/api/settings` 的 save/validate 也统一让 `base_url` 和 `api_key` 都能继承现有设置，而不是只继承 API Key。前端模型设置弹窗的必填判断也同步对齐：如果后端 summary 已声明 `base_url_configured`，用户就不需要重复填写 base URL 才能校验或保存。这样“默认 remote”已经从只读 summary 推进到了设置编辑链，且外部 settings/SSE/trace/e2e 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`554/554`），`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- 执行主链 provider identity fallback 继续去 mock（2026-06-25）：本轮继续沿“默认工具去 mock 化、真实工具接入”主线，把执行主链里残留的 provider/model mock 兜底也一起收口。后端 `backend/app/services/chat_execution_service.py` 新增 shared provider identity resolver，`stream_task_execution()` 的 start 事件、planning trace meta、tool-registry preflight、tool iteration 与 final answer trace 现在都会优先使用 provider 自身字段，其次回退到 runtime settings 的 `provider/model`，最后才退到兼容 `mock/mock-gpt`。这样即使真实 provider 适配层一时没有显式挂 `provider/model` 属性，实时 SSE 与 trace 观测面也不会又回落成 mock 文案。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`555/555`），`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- planner 决策权与 task_plan 输出口径继续去 mock（2026-06-25）：本轮继续沿“默认工具去 mock 化、真实工具接入”主线，把默认规划链里还会覆盖真实 planner 决策的一处分叉一起收掉。后端 `backend/app/services/tool_runtime.py` 现在会把真实 `tool_plan` 的 `planned_tool_names/planned_tool_labels` 注入 `task_plan` 输入，让 `task_plan` 工具输出优先按实际计划生成，而不是继续只靠 prompt 关键字 heuristic 猜测；同时当真实 provider 明确返回 `{"tools":[]}` 时，系统会尊重这次“无额外工具”决定，不再回退到本地 heuristic 硬塞 `task_retrieve/calc_eval`。这样真实 planner 已开始真正接管“是否调用额外工具”的决策面，而 `task_plan` 观测内容也与实际执行计划保持一致；外部 settings/SSE/trace/e2e outward 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`557/557`）。
- planner payload 兼容形状继续去 mock（2026-06-25）：本轮继续沿“默认工具去 mock 化、真实工具接入”主线，把真实 planner 接入里另一处容易 silently 回退 heuristic 的兼容缺口一起收掉。后端 `backend/app/services/tool_runtime.py` 现在除了原本严格的 `[{name,input}]` 外，也能接受 provider 返回的 `["task_retrieve","calc_eval"]` 字符串工具列表，以及 `{tool, arguments}` / `{tool, args}` 这类更常见的 planning payload 形状，并继续复用现有 query/expression 默认补全与 canonical tool normalization。这样真实 provider 就算没有完全按内部最严格 schema 出牌，也更容易真正接管规划链，而不是又被本地 heuristic 静默接手；外部 settings/SSE/trace/e2e outward 契约保持不变。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`559/559`）。
- planner payload 平铺字段兼容继续去 mock（2026-06-25）：继续沿同一条真实 planner 接入主线，本轮再把另一类常见 payload 形状一起收掉。后端 `backend/app/services/tool_runtime.py` 现在除了兼容 `["tool"]` 与 `{tool, arguments}` 外，也能识别 `{name, query, top_k, knowledge_base_id}`、`{name, expression}` 这类把工具参数直接平铺在 item 顶层的 provider 输出；这样 `task_retrieve/calc_eval` 不会再因为缺少内层 `input` 包装而退回默认 query/default kb 或直接被判成无效计划。这样真实 provider 返回的 planning payload 就算更贴近自然 JSON，也更容易真正接管规划链，而不是局部退化回本地默认值或 heuristic。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`561/561`）。
- planner label 兼容继续去 mock（2026-06-25）：继续沿同一条真实 planner 接入主线，本轮再把“provider 返回工具显示标签而不是内部名”这一类常见 payload 差异一起收掉。后端 `backend/app/services/tool_runtime.py` 现在在规划归一时，除了认 canonical tool name，也会认当前 registry 下的 `registration.label` 与 `display label`；因此像默认 `Knowledge Retrieval`、默认 `Calculator`，以及 source override 出来的 `Calculator Suite` 这类标签，也能继续被反解回 canonical tool name，而不是被当成无效计划静默回退到 heuristic。这样真实 provider 返回更贴近人类可读标签的 planning payload 时，也更容易稳定进入 canonical tool plan。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`563/563`）。
- planner 动态候选与 extra tool 展示继续去 mock（2026-06-25）：继续沿同一条真实 planner 接入主线，本轮把 planning 候选集合正式从硬编码 `task_retrieve/calc_eval` 推进到当前 registry/source 的动态工具集。后端 `backend/app/services/tool_runtime.py` 现在会按 registry 里已启用工具的 `kind` 动态选择 planning 候选：rule-based fallback 和 provider-assisted planning 都能规划额外的 retrieval/calculator template tools，例如 `calc_eval_fast`、`task_retrieve_hot`；planning prompt 也会同步透出这些 tool name 与 label。与此同时，非内建 extra tool 的 display name 现在优先显示其配置 label，而不是内部名。这样 registry/source 已接入的真实扩展工具，终于不只是“执行层能跑”，而是开始真正进入规划面和可见观测面。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`566/566`），`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
- planner extra tool 优先级继续去 mock（2026-06-25）：继续沿同一条真实 planner 接入主线，本轮再把“extra tool 虽已进入 planning 候选，但 built-in 仍总是先被选中”的偏差一起收掉。后端 `backend/app/services/tool_runtime.py` 现在会把 planning optional tools 排序成“extra tools 在前、built-ins 在后”，并让 rule-based fallback 按这条优先级选择同 kind 的 retrieval/calculator 工具。这样当 `calc_eval_fast`、`task_retrieve_hot` 与内建 `calc_eval/task_retrieve` 同时存在时，规划链会优先使用 extra tool，而不是又退回默认 built-in。这样 registry/source 想真正切向扩展/真实工具时，不再需要先把 built-in 明确禁掉才能生效。校验结果：`backend/.venv/bin/python backend/scripts/test_tool_runtime_slice.py` 通过（`568/568`），`cd frontend && npm run lint` 通过，`cd frontend && npm run build` 通过，`bash scripts/test_ci_e2e_tooling.sh common` 通过。
